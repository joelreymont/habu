\ ir-id-proof-host.f - test/compiler/ir-id-proof.f on the capture host.
\
\ SUBJECT: source-loading the chain. The parity gate reopens the package that
\ owns the frozen identity schema and binds it to the Rocq model, walking the
\ registry rows id.f's load declares. Seeded, that load is a no-op and the
\ walk finds nothing (E-CID-FAMILY); the host performs it.
require test/host-run-lib.f
s" test/compiler/ir-id-proof.f" HOST-RUN:HOST-RUN
