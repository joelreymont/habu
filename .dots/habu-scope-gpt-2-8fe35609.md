---
title: Scope GPT-2 loader imports
status: active
priority: 1
issue-type: task
created-at: "2026-07-28T01:46:45.336038+02:00"
---

Problem: the newly renamed GPT-2 checkpoint loader is a changed multi-call consumer but repeats qualified public calls from sixteen required packages, violating the staged package-consumer rule. Owner and result: package GPT2LOAD remains the sole code owner in maki/infer/gpt2-load.f; this leaf changes only that file and adds balanced lexical using scopes for CAD-NUM, GPT2TENSOR-GLOBAL--ROLE, GPT2TENSOR-LAYER--ROLE, GPT2TENSOR-TENSOR--ID, GPT2TENSOR, SAFET, MODEL-FAMILY, WSTORE, MEM, MDLCFG-CFGKEY, MDLCFG, GPT2LOAD-GPT2--MODEL, GPT2LOAD-PREPARE--RESULT, GPT2LOAD-MAPPED--CHECK--RESULT, GPT2LOAD-COPY--CHECK--RESULT, and WSTORE-STORE. Imports must be narrow and LIFO, never cross ;package, and preserve every definition, stack effect, FIELD selector, MATCH selector, call order, and runtime result. Retain qualification only where executable imports collide: GPT2TENSOR:COPY-NAME? against SAFET; GPT2TENSOR:COUNT against SAFET:COUNT and global COUNT; GPT2TENSOR:SLOT against global SLOT; MDLCFG-CFGKEY:MAKE and UNMAKE while the GPT2LOAD-GPT2--MODEL scope overlaps. Close each result package around only its owning PREPARE, mapped-check, or copy-check definition so REJECTED and READY remain unambiguous and bare. After the config-key scope closes, the remaining GPT2LOAD-GPT2--MODEL MAKE and UNMAKE calls are bare. Preserve qualified type and effect names, FIELD and MATCH selectors, comments, and strings. Dependency: accepted loader rename commit ae1f14633232efba954e04e76f82b3b73e9e56be. Checkpoint: the exact source load is green; a token-aware executable-call census proves the sixteen repeated consumers and the named collisions; a representative import diff passes the package gate. Forbidden: API changes, aliases, global definitions, package reopen, broad source migration, comments used as waivers, qualifier removal at a collision, changed loader behavior, test edits, metadata edits, or files beyond maki/infer/gpt2-load.f. Acceptance: exact load of maki/infer/gpt2-load.f plus prepare, mapped, copy, and payload owning loads pass; the same exact diff artifact passes typed-local and package gates; imports are balanced and a hostile removed/misordered ;using fails closed; review confirms no executable repeated qualifier remains except the named collisions. Smallest owning proof: the exact loader and four owning test loads pass byte-for-byte unchanged behavior after the lexical import rewrite.

Claim: agent=gpt2-loader-using workspace=.jj-ws/habu-scope-gpt-2-8fe35609
