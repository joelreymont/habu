---
title: Migrate linearity+layout readers to unified ENUM payload source
status: active
priority: 2
issue-type: task
created-at: "\"2026-07-22T14:04:11.623103+02:00\""
---

Problem: two un-migrated readers of the legacy positional payload schema make ENUM front-end payloads invisible. src/core/type-family.f:1351-1360 TFAM-CONCRETE-LINEAR? iterates SUMV-SCH-COUNT@ (0 for front-end variants), so DEFLINEAR payloads declared via ENUM-DECL/decl-event are unseen: checker certifies dup/drop of linear resources (proven live: '( nlin -- ) drop' certifies, legacy twin rejects). Cascades through LAYOUT-ARG-LINEARISH?/LAYOUT-MAYBE-LINEAR?/LAYOUT-LINEAR-COUNT/LAYOUT-MEM-OK? (checker.f:1326-1414) and FIELD-FAM?/TDECL-PAY-FAM? nesting gates. Same defect in tools/../src/core/layout-valid.f:189-210 QUEUE-VARIANT/VARIANT-OFF: front-end variants queue no payload walk tasks, so nested-sum inner-tag CHECK, guard rows are never emitted and absence is treated as nothing-to-validate (fail-open). Expected fix: route both readers through the unified source (SUMV-PAY-N / SUMV-PAY-ROOT ... SCHEMA-ROOT@ TFCL-NODE-XT), exactly as the HEAD commit did for construct/MATCH/width readers. Acceptance: negative checked regressions pinning (1) '( nlin -- ) drop' -> reject and '( nlin -- nlin nlin )' dup -> reject for an ENUM-DECL family with a DEFLINEAR FIELD, (2) guard-row parity: lowering certificate for legacy vs front-end twin declarations emits identical CHECK, rows for a nested-sum payload. Reproducers: scratchpad hb-audit-checker/lin-repro.f and lv-repro.f (session 94a7f15f). Scope update (2026-07-22, after master moved to 703e0f8b): the reader migration itself landed on master independently — both the linearity walk and layout-valid now read through the unified payload source, and all three linearity reproducers reject on a fresh probe of master. What remains of this dot: test/lower-cert.f is still orphaned (test/candidate-validation.f declares four diagnostics and never runs it), and the legacy-versus-front-end twin descriptor parity regression is absent. Remaining files: test/lower-cert.f, test/candidate-validation.f only. Acceptance: candidate-validation runs five diagnostics including lower-cert, and a parity regression proves a legacy SUMTYPE and its ENUM front-end twin produce identical lowering descriptors. Depends: none. Ownership: lowering-certificate test wiring. Claim: agent=claude workspace=.jj-ws/habu-lower-cert-wire.
