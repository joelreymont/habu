---
title: Name the cell when a data address leaves the span
status: open
priority: 2
issue-type: task
created-at: "2026-09-17T14:42:02.581563+03:00"
---

Problem: src/habu/aot-closure.f:73 DATA-ADDRESS! dies 74 'aot: address refers to data outside the restored span' for a recorded DATA address cell whose value lies below BLOB-SRC or above BLOB-END, naming neither the cell nor the value nor the bounds. Tender's server image (server/entry.f at Tender bb987366 through scripts/build.f, report /tmp/aspen-aot-server.md, aspen 2026-09-17) hits it, while Tender's src/main.f hits the named refusal at src/habu/aot-lib.f:80-82 (hb-build: stripped AOT persistent data holds a code/dict pointer, exit 70); the lane bisected the difference to Tender's own persistent data holding execution tokens stored through a setter or bound to a defer, so the same class of cell is classified two ways depending on which check runs first (CELL-TEXTPTR? by live extents in aot-lib.f, or the span bound in aot-closure.f), and the 74 reads as an engine fault rather than a refusal. Acceptance: a persistent cell holding a code or dictionary pointer gets one answer whichever check meets it first, the exit-70 refusal naming the owning word, the cell's DATA offset and the value; DATA-ADDRESS! keeps 74 for a genuine data pointer outside the span but names the cell, the value and both bounds; a regression under test/compiler builds stripped images whose persistent data holds an xt through a defer, one through ' word , and one data address just past the span, and asserts each diagnostic; Tender's server/entry.f build then reports the named refusal. Files: src/habu/aot-closure.f, src/habu/aot-lib.f, tools/hb-build*.f, a test under test/compiler. Verify: the regression; hb-build of Tender server/entry.f; test/run.f. Depends: none. Ownership: AOT. Claim: unassigned.
