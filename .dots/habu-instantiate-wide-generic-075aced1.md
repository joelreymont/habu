---
title: Instantiate wide generic STRUCTURE UNMAKE
status: open
priority: 2
issue-type: task
created-at: "2026-07-24T15:12:39.618302+02:00"
---

A public generic STRUCTURE family's generated UNMAKE currently stores the open one-cell-per-parameter effect fam<a>, and the checker rejects consuming a concrete wide instantiation because no arg-aware lane instantiates the input side: the stable open-form-versus-concrete-wide mismatch. Construct has an arg-aware escape (checker.f:469 CTOR-STEP-XT routes a generated constructor whose declared OUTPUT instantiates a family parameter at a multi-cell layout argument through the arg-aware construct step, so an OUTPUT-annotated wide MAKE like ( xpginr n -- xpg<xpginr> ) XPG:MAKE certifies), but UNMAKE has no matching input-side lane; concrete arity-0 structures round-trip wide fields only through the hidden-field expansion (type-family.f:2067), which does not cover a generic family instantiated wide. Required result: implement an input-side arg-aware generated-UNMAKE step at the checker/type-family owner that instantiates the field schema and hidden run at the concrete layout width (the destructure dual of CTOR-STEP-XT), preserving field order, nominal roles, linearity, and ordinary concrete and generic behavior. FORBIDDEN: family or name special cases, raw casts, aliases, value heuristics, trusted runtime guards, duplicate constructor authority. The closed sibling dot habu-checker-type-structure-d996215b claims generated MAKE/UNMAKE certification is complete; that claim is contradicted for the generic-wide UNMAKE case and this dot supersedes it for that case only (do not reopen or edit d996215b). Acceptance: a checked native xpg<xpginr> MAKE->UNMAKE round trip recovers 1/2/3 exactly; flattened, foreign-family, and linear instantiations stay fail-closed; no new trust boundary is added; and TWX-XPG-CHECK is deleted when the capability lands. Files: src/core/checker.f, src/core/type-family.f, src/core/structure-make.f, and test/type-ctor-suite.f. Verify: type-ctor/type-match/structure-certify suites, typed-local/package/dot lints, and the full native gate. Depends: none. Ownership: generic-instantiated UNMAKE lowering.
