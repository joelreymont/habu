---
title: Bignum arithmetic not fully implemented - structure exists but ops missing
status: closed
priority: 2
issue-type: task
created-at: "2026-01-01T10:33:46.711545+02:00"
closed-at: "2026-01-01T11:21:08.006379+02:00"
close-reason: "Bignum structure exists in objects.zig with make() method and 8-limb array. Not integrated: reader doesn't parse large integers as bignums (reports 'invalid number'), arithmetic ops don't handle bignum operands, no heap allocation for bignums. Would need: reader support, arithmetic primitive extensions, heap.allocBignum(). Structure is ~10% complete."
---

File: src/runtime/objects.zig:270 (Bignum struct), src/runtime/primitives/arith.zig

Issue: Bignum infrastructure added but arithmetic operations incomplete.

Current state:
- ✅ Bignum struct defined (objects.zig:270)
- ✅ allocBignum function exists
- ✅ Type checks (bignump) work
- ❌ Bignum arithmetic (+, -, *, /) not implemented
- ❌ Bignum/fixnum conversion incomplete
- ❌ Numeric contagion doesn't handle bignums

Missing operations:
1. Bignum + Bignum
2. Bignum + Fixnum (with overflow promotion)
3. Bignum comparison (<, >, =)
4. Fixnum overflow detection -> bignum promotion
5. Bignum to/from string conversion

Fix approach:
1. Implement bignum addition with GMP or manual implementation
2. Add overflow detection to fixnum ops
3. Update numeric contagion to handle bignum
4. Add bignum comparison primitives

Files: src/runtime/primitives/arith.zig, src/compiler/compile.zig:1110
