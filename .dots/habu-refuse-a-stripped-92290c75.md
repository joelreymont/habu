---
title: Refuse a stripped persistent cell that points into memory the build mapped
status: open
priority: 2
issue-type: task
created-at: "2026-09-22T11:28:30.617571+03:00"
---

Problem (reported by aspen from Tender, engine 6dc6cd7b on a345f609; notes, images, sources and straces under ~/.cache/tender/habu-gaps/stripped-serve-crash/ and ~/.cache/tender/habu-gaps/stripped-determinism/): a stripped tenderd (2,031,808 B) links rc 0 and runs `migrate`, then `serve` SIGSEGVs before any clone - SEGV_MAPERR si_addr=0xffff59f78be0, the crash dump's x16 holding that value - and the address is identical across runs of one image under ASLR but differs per build (the 10:03 image faults at 0xffff892b8be0). Three builds of one tree differ in 24 bytes at offsets 1,970,720-1,970,771: the middle bytes of eight LEB128 window cell values (S-WVALS, src/habu/aot-file.f) that decode in tenderd-a to 0xffff5a18af80, 0xffff5a128be0, 0xffff5a12a4c0, 0xffff5a129ba0, 0xffff5a179980, 0xffff5a12b4e0, 0xffff5a12bea0 and 0xffff5a12dbec: interior addresses of anonymous mappings the linking process made (MEM-ALLOC's mmap in lib/memory.f, or a foreign allocator) and stored in persistent data cells at load time. The stripped walker (src/habu/aot-closure.f, REFUSE-UNDECLARED-CELL and its siblings) reads a cell's value only as a code, dictionary-record or DATA address, so a pointer into a mapping the build created passes as an integer: the image carries it, the link output depends on ASLR, and the program dereferences a dead address at run time. Small programs (scope-hello.f, stripped-engine-data/hello.f) are byte-identical over two builds. Acceptance: (1) the stripped link refuses by name a persistent data cell whose value lies inside a mapping the linking process created after the engine started - word, data-off, value and the mapping's extent through REFUSE-DATA-CELL's two writers, the suggestion naming run-time allocation (in MAIN or an IMAGE-LIFECYCLE reset hook) or --repl; the predicate is the process's own record of live mappings, kept where every Habu allocation passes (the engine's mmap primitive or lib/memory.f's MEM-MMAP-RC / MEM-ALLOC-GUARDED, dropped at MEM-UNMAP / MEM-RELEASE-GUARDED), never a value-range guess; a pointer a foreign allocator handed out is the stated untested boundary if the record cannot see it. (2) An HBT-STRIPPED-* fixture in tools/hb-build-test.f whose program MEM-ALLOCs at load and stores the pointer in a VARIABLE pins the measured refusal text; a sibling that allocates in MAIN links and runs rc 0. (3) Two stripped links of one program in one hb-build-test row are byte-identical (cmp), pinned. (4) The hand-back names the owners of the eight cells in Tender's server build (driver and app under ~/.cache/tender/habu-gaps/stripped-serve-crash/, Tender at ~/Work/Tender/.jj-ws/Tender-stripped): a Habu lib owner gets its own dot (lib/task.f PREPARE stores four MEM-ALLOC-GUARDED / MEM-ALLOC-CELLS pointers into a TCB; lib/process-env.f and lib/source.f allocate lazily), a Tender owner goes to aspen, who re-runs Tender's verify list (~5 min) once the engine integrates. Files: src/habu/aot-closure.f, lib/memory.f or the engine's mmap, tools/hb-build-test.f. Verify: tools/hb-build-test.f, test/gate-aot-positive.f, tools/engine-size-test.f, the engine chain. Depends: none. Ownership: hazel. Claim: unassigned.
