---
title: Rebase only the xt in imgdump --pc
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-16T14:18:00.803379+03:00\""
---

Problem: tools/imgdump.f:127 E-E adds XTBASE to dictionary slot 1, which in a no-trailer baked image is the __text-relative END (src/habu/xref.f XREF-LEN-SLOT; src/habu/habu2.f:5417 converts end to length only at boot), so PC-HIT? (tools/imgdump.f:236) gets a ~4 MiB window and --pc answers the first seed record for nearly every pc. Measured on the 13:20 bin/hb: `--pc bin/hb <xt of evaluate>` prints `+ $40fb68 $40fb88`, and the second column is an absolute end where the tool documents `$len`. tools/imgdump-test.f:236-242 cannot catch it: it queries `+`, which is record 0 (the record the broken window always returns), and matches `s" + "` with CONTAINS?. tools/imgdump.f:254-263 NO-SNAP-XTBASE reads e_type/e_entry without checking the ELF magic, class or machine, and HB-TARGET-LINUX? is the tool own build target, not a property of the image, so a Mach-O or truncated file yields a fabricated base instead of a refusal. ENT? (:190-191) shares the rebased accessor with the address readers. Acceptance: E-E returns the raw field; one named word computes the span end per image class (no-trailer: XTBASE + raw end; snapshot: start + CODE-SPAN:BYTES as today); .ENT prints the documented `name $start $len` columns in both classes; NO-SNAP-XTBASE dies by name (same "refusing to guess" wording) unless the file starts with 7f 45 4c 46, EI_CLASS 2, e_machine 183 and e_type ET_EXEC; ENT? plausibility does not read through the rebased accessor. Regression in tools/imgdump-test.f: assert the whole --pc line for `+` (printed start equals the queried xt and the third column is the length), add a non-record-0 word (evaluate) whose line names evaluate, and a refusal case for a non-ELF file carrying 2 at offset 16. Files: tools/imgdump.f, tools/imgdump-test.f. Verify: bin/hb --load tools/imgdump-test.f; bin/hb --load tools/imgdump.f -- --pc <engine> <xt> for + and evaluate. Depends: none. Ownership: tools/imgdump.f tools/imgdump-test.f. Claim: agent=hazel-audit-imgdump workspace=.jj-ws/hazel-audit-imgdump. Source: Opus audit of 3972b7e0 (2026-09-16), findings C1 M1 M6.
