---
title: Write the x86_64 assembler with encoder tests
status: active
priority: 2
issue-type: task
created-at: "2026-09-17T17:32:42.535414+03:00"
---

Problem: docs/x86-64.md (campaign habu-campaign-c6-targets-86bb56bb) fixes the backend layers; the first is src/arch/x86-64/asm.f, an encoder in the discipline of src/arch/arm64/asm.f and the arm32 encoders (docs/embedded-encoders.md): nominal operand types (typed registers, immediates, memory operands with base, index, scale and displacement), ENC-* constructors, variable-length encoding with REX, ModRM and SIB, and mov r64, imm64 as the one relocatable literal form the design names. Acceptance: package X64ASM with the integer instruction set the engine primitives and the selector need (moves, loads and stores of 8/16/32/64 bits, arithmetic, logic, shifts, compare and test, conditional jumps and sets, call, ret, push and pop, lea, imul, div, syscall), every encoder a checked word whose operand types make an ill-formed instruction a checker refusal; a test under test/ that compares emitted bytes against fixed expected byte strings for each form (verified once against objdump on an x86_64 machine and recorded in the test), including the REX-required forms (r8-r15, sil/dil), 32-bit zero-extension, rip-relative addressing and the imm64 literal; docs/embedded-encoders.md gains the x86_64 row. Design fixed at the 2026-09-17 checkpoint: there is no byte-granular sink in the tree (icode.f EMITW and emit.f APPEND are fixed 4-byte word sinks), so X64ASM encoders take a lib/byte-buffer.f BUF header as their last operand and append through BUF:APPEND-BYTE, the asm/mnem split collapses for this backend, the memory operand is one packed nominal cell with typed constructors (MEM-B, MEM-D, MEM-X, MEM-RIP), branch displacements are measured from the end of the instruction with rel8 and rel32 as separate words, widths are deterministic so the emitter sizes code by encoding into a scratch buffer, MOV-RI64-IMM-OFF names the imm64 offset, and the error block is -8830..-8839 in lib/errors.f (baked, so the lane rebuilds the engine). Files: src/arch/x86-64/asm.f, test/compiler/x86-64-asm.f (the peer convention, registered as SUITE compiler-x86-64-asm in test/gate-stdlib-cases.f), lib/errors.f, docs/embedded-encoders.md. Verify: bin/hb --load test/compiler/x86-64-asm.f on the arm64 engine (the encoder is target-free Habu); the expected bytes come from llvm-mc -triple=x86_64 -show-encoding and llvm-objdump on the arm64 host (both installed there), so the objdump verification happens before the Intel machine exists. Depends: none. Ownership: hazel runs it on the arm64 host (Joel 2026-09-17: the four cross-buildable dots run here; the Intel agent starts from INTEL.md). Claim: agent=hazel-x64-asm workspace=.jj-ws/hazel-x64-asm.
