---
title: Give the recovery engine the harness words its suites need
status: open
priority: 2
issue-type: task
created-at: "2026-09-16T21:32:56.066282+03:00"
---

Problem: the no-binary recovery chain (tools/bootstrap.sh, HABU_BOOTSTRAP_CHECK_ONLY=1) reaches `bootstrap check OK` on the release tree b6db6557 and fails on the head: bisected with the prefix strip overlaid, e7293c81 is the last good tree and 5ac31ae4 (Check declaration-suite fixture helpers, the first test-tree TRUSTED sweep) the first bad, dying in the suites the chain runs on hb-stdin with `hook: non-certified definition: pkg-public at 'CHECKER-PACKAGE-PUBLIC'` (rc 70); the later sweeps move the first refusal to `E-UNDEFINED habu: in check-quiet-candidate!: undefined word 'DIAG-QUIET'` in test/type-ctor-suite.f via test/checker-assert.f (head f624ae2d, 2026-09-16). The sweeps turned TRUSTED: helpers into checked definitions that name engine-internal words (src/core/checker.f DIAG-QUIET, CHECKER-PACKAGE-PUBLIC); the native-build gate engine certifies them (it carries the full signature store, unsealed), the recovery engine hb-stdin and install-class engines (build-fixpoint) do not, so the same helpers are whitebox probes there (the class dot habu-whitebox-suites-7fe05e62 names). The chain was already red for the prefix overflow (fixed by 17c29e93) and the NULL$ site (fixed by 9e4f6807), which hid this. Acceptance: the harness facilities the chain's suites need are public checker words with signatures every engine class carries (a quiet candidate check that replaces bumping DIAG-QUIET; a package publicity probe that replaces CHECKER-PACKAGE-PUBLIC), test/checker-assert.f and the declaration-suite helpers use them, no TRUSTED: comes back, and the chain reaches `bootstrap check OK` on the head; the five suites the chain runs (engine-error-package, catch-frame, type-ctor-suite, top-row-hook-test, compile-preflight-recovery) also pass on a build-fixpoint install-class engine. Files: src/core/checker.f (public surface), test/checker-assert.f, test/type-decl-suite.f and the declaration-suite helpers, tools/bootstrap.sh (only if the suite list changes). Verify: HABU_ALLOW_BOOTSTRAP=1 HABU_BOOTSTRAP_CHECK_ONLY=1 HABU_TARGET=linux-aarch64 HB_TMP=<private> tools/bootstrap.sh -> bootstrap check OK; test/run.f green. Depends: none (habu-whitebox-suites-7fe05e62 is the wider class). Ownership: checker surface and the test harness. Claim: unassigned.
