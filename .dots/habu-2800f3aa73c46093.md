---
title: Integrate register allocation into ARM64 codegen
status: open
priority: 2
issue-type: task
created-at: "2026-01-08T11:49:40.256388+02:00"
---

Files: src/ir/pipeline.zig:105, src/ir/arm64.zig
Current: Register allocation runs (src/ir/regalloc.zig GraphColorAlloc) but results not used
Problem: arm64.generate() uses virtual register numbers from RegFunc directly
Solution:
1. Extract color mapping from regalloc: vreg -> physreg (nodes[vreg].color)
2. Create RegisterMap struct to hold mapping + spill info
3. Pass RegisterMap to arm64.generate()
4. Update arm64.generate() to map virtual->physical regs using map
5. Handle spilled registers: load/store from stack slots
Impact: Critical for JIT performance - enables real register allocation
