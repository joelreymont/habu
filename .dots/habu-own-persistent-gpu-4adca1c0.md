---
title: Own persistent GPU buffer
status: active
priority: 2
issue-type: task
created-at: "2026-08-03T04:48:35.843571+02:00"
---

Problem: GPU:session cannot retain one model allocation without exposing a raw CUDA handle. Result: package GPU adds public opaque DEFLINEAR GPU:buffer and only three words: ALLOC ( GPU:session CAD-NUM:alloc-byte-len -- GPU:session result<GPU:buffer,n> ), UPLOAD ( GPU:session GPU:buffer CAD-NUM:byte-off ptr u8 CAD-NUM:byte-len -- GPU:session GPU:buffer result<n,n> ), and FREE ( GPU:session GPU:buffer -- GPU:session result<n,n> ). ALLOC binds the session context, makes one cuMemAlloc, and publishes only after both device and private host state exist; every returned code or actual throw releases the acquired prefix and preserves the session. UPLOAD proves offset+length is inside the allocation, then performs the exact synchronous cuMemcpyHtoD; every refusal preserves both owners. FREE consumes once, attempts bind and cuMemFree even after the first failure, frees private host state, and returns the first exact code. Add only the cuMemFree injection seam needed to prove this. Acceptance: pre-change packaged signature probe rejects; host injection covers returned codes and actual throws at bind/alloc/upload/free, overflow and boundary spans, exact destination offset/length, reusable owners after failed upload, and once-only cleanup; real DGX Spark opens two sessions, allocates/uploads/frees one buffer in each, closes either order. Files: lib/ptx/cuda-driver-test.f only if binding coverage changes, maki/cuda-run.f, maki/cuda-run-fake.f as needed, maki/gpu-session.f, maki/gpu-buffer-test.f, existing suite rows only. Verify: bin/hb --load maki/gpu-buffer-test.f; driver test if changed; package/typed diff gates; full Maki and ptx-stdlib before merge. Depends: landed habu-own-device-runtime-5e7d559e. Ownership: persistent GPU buffer lifetime only. Forbidden: stream/sync/download/query, callback, raw public handle, descriptor table, generation, registry, module/model state, special result type, compatibility, manifest, lint, or framework. Claim: agent=codex-gpu-buffer workspace=.jj-ws/habu-own-persistent-gpu-4adca1c0.
