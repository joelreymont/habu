---
title: Make a pointee under a family argument invariant
status: open
priority: 2
issue-type: task
created-at: "2026-09-18T19:56:21.631069+03:00"
---

Problem (adversarial review of 534734bb..a6ce705f, MEDIUM, pre-existing, probe widen2.f): a family argument unifies with the integer widening lattice even when it occurs only as a POINTEE, so a value built over a byte pointer widens into the same family over a cell pointer and the cell is read out of the byte: 'STRUCTURE bx 1 FIELD p ptr a ;STRUCTURE : RD ( bx<cell> -- n ) BXT-BX:UNMAKE @ ; : MK ( ptr u8 -- bx<u8> ) BXT-BX:MAKE ; s" h" drop MK RD .' prints 1224978999860527208 - a one-byte literal read as a cell. lib/span.f survives only by its byte reach (SPAN:CELL-AT checks i < reach/8 and every accessor is byte-bounded; verified), which no other family has; lib/span-test.f STW-MINT now pins '( ptr u8 n -- span<cell> ) SPAN:MAKE' as accepted (the placement lane made the mint follow the boundary), so the fixture must not be read as a language rule. Acceptance: a pointee under a family argument is INVARIANT - bx<u8> does not unify with bx<cell> in either direction, and neither does span<u8> with span<cell> - refused by name at the boundary with the checker's existing mismatch diagnostic; SPAN:BYTES stays the explicit crossing (byte-view + MAKE); lib/span-test.f's STW-OPEN, STW-MINT/STW-MINT2 and ST-TINY CELL-READ assertions flip to the invariant rule with the reason rewritten; a sweep of lib/ tools/ test/ and the consumer trees (loom, maki, kiba, radar, Tender - read-only rg for span<cell> / span<u8> crossings) with the count in the commit body and each tree site converted through SPAN:BYTES; red-first cases for widen2.f and the span pair in test/compiler/ (the suite that owns family-argument unification) ; docs/forth.md's span section and lib/span.f's header state invariance. Files: src/core/checker.f (family-argument unification), lib/span.f, lib/span-test.f, test/compiler/, docs/forth.md. Verify: the probes; three generations with cmp; test/run.f. Depends: none. Ownership: checker. Parent: habu-bound-pointers-with-719ba3f4. Claim: unassigned.
