---
title: Rename TYPEFAMILY definer to NEWTYPE
status: active
priority: 2
issue-type: task
created-at: "2026-07-26T16:21:44.671939+02:00"
---

Joel's ruling 2026-07-26: the arity-0 nominal wrapper definer is ratified as the permanent third declaration word and renamed to NEWTYPE now - the current name collides with type-theory jargon (a type family is a type-level function) while the semantics are exactly a Haskell newtype (zero-cost nominally-distinct wrapper; with a private TRUSTED mint it is a newtype with a hidden constructor). HARD rename, no alias, no compatibility shim. Measured blast radius: 218 occurrences across 51 .f files, 105 across docs. Engine sites, all in one atomic commit: the definer : TYPEFAMILY at src/core/sumtype.f:1796 and its PRIM: row at :1883 become NEWTYPE; the TDECL-CTX name string at sumtype.f:886; the case-insensitive token matches at src/core/type-family.f:1263 and src/core/checker.f:6276; the UNSAFE-NAME-ADD entry at checker.f:6380; the verify-source replay arm at src/habu/verify-source.f:735 including the RECORD-TYPEFAMILY word's own name; the lint token tables at tools/reserved-name-lint-core.f:185 and tools/package-diff-lint-core.f:558; the fixture at test/internal-word-gate.f:367; every declaration site in lib/, maki/, tools/, test/; every docs/ mention (token rename ONLY - the stale-prose repairs stay with their own dot). All token strings are matched case-insensitively, so the replacement string is newtype lowercase where the source uses lowercase. After the rename: a negative regression proves TYPEFAMILY is a rejected/unresolvable token on the real declaration path (this SATISFIES the census-type-dsl-cutover completion contract's zero-TYPEFAMILY-occurrence line by rename rather than deletion); docs/census-type-dsl-cutover.md's completion contract is edited to ratify NEWTYPE as permanent alongside STRUCTURE and ENUM, citing this ruling. Rebuild discipline: this renames an engine word, so run the documented fixpoint refresh (tools/build-fixpoint-refresh.f -- install) and re-derive every parity artifact - snapshot/AOT parity, bootstrap-mirror lint including its live-axiom ratchet (the PRIM row name changes; if the gforth recovery mirror cannot follow mechanically per docs/bootstrap.md, STOP and report), verify-source suites. Registry facts must be IDENTICAL under the new name: same family ids, kinds, widths, arities for every declared newtype - prove with a whole-registry fingerprint before/after (the migration lanes' method), and constructor-package spellings for proof mints are UNAFFECTED (they derive from package+family tail, not the definer word - verify, do not assume). Acceptance: rg for TYPEFAMILY over .f/.fs/md returns zero outside the tombstone-negative fixture and historical dot/LESSONS records (dots and LESSONS are records, do not rewrite history there); full master gate list green on the exact tree including test/run.f, bootstrap-mirror, refine-lint, trusted-inventory (TRUSTED.md prose rows mentioning arity-0 TYPEFAMILY update to NEWTYPE - those are live classifications, not history); fixpoint refresh clean with the certified count unchanged. STOP conditions: persisted-image or replay identity not re-derivable by the refresh; mirror surgery beyond the documented path; any site where the rename changes semantics rather than spelling. Claim: agent=newtype workspace=.jj-ws/habu-newtype-rename
