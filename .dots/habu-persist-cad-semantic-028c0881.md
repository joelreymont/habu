---
title: Persist CAD semantic effects in checker
status: open
priority: 1
issue-type: task
created-at: "2026-07-13T13:16:45.394355+02:00"
blocks:
  - habu-seal-cad-effect-49cac404
---

Full context: src/core/checker.f stored words, primitives, and quotations retain stack, return-stack, linear, and control facts but no CAD semantic row; a balanced pure declaration can therefore call IO or device authority invisibly. Fix: persist sealed static CAD-EFFECT rows in primitive, stored-word, and quotation metadata as typed opaque arena handles, never through raw variable/create/constant cells; validate every primitive declaration, stored-word ingress, quotation construction/open, snapshot/replay record, and checked lookup. Callee bindings are relative to declared inputs, attributes, capabilities, and captures; checker-owned stable lexical site ordinals form path segments, and each word call or quotation construction/open substitutes through CAD-EFFECT:REMAP before canonical UNION. Local handle numbers, arena offsets, allocation order, source addresses, and internal hashes never enter metadata identity: snapshot, replay, AOT, diagnostics, and fixpoint serialize and compare canonical row contents. Union only successful calls and preserve latent quotation effects and transactional rollback. Acceptance: malformed, forged, stale, cross-owner, or unsealed rows reject before metadata mutation; two callees that both use local slot zero remain distinct after composition, while reapplying the same remapped binding is idempotent; capture, nested quotation, higher-order propagation, failed-overload rollback, missing boundary declarations, and pure-calls-device mutations reject or compose exactly; insertion/allocation order changes local handles but not canonical bytes or fresh-process identity; current checked sources remain certified only after every primitive boundary is explicitly classified. Files: src/core/checker.f plus focused checker suite and bootstrap mirrors required by the native model. Verify: red-first ingress/substitution/nominal-storage mutations, canonical fresh-process replay and AOT, checker suites, bootstrap, fixpoint, full native gate. Ownership: checker metadata, stable lexical site ordinals, and call-site substitution only; no runtime artifact/capability resolution.

Design-reference (2026-07-18, tfinite): implements `docs/effects.md` § R8-2 (checker propagation). Acceptance: the sealed `effect-row` rides the existing usig scheme metadata as an opaque nominal handle; every call and quotation open composes by `CAD-EFFECT:REMAP`-then-`UNION`; the `NP-CHECK`/`NP-MINT-CHECK`-style post-body seal rejects a body whose composed row diverges from its declared row (a balanced `( -- )` word reaching device/IO rejects), and the `TVK-RAW` nominal-storage seal rejects any row stored through a raw `variable`/`create`/`constant` cell — mutation-matrix rows 11, 12, 15, 16.
