---
title: Fence an offset null the way the DATA base is fenced
status: active
priority: 2
issue-type: task
created-at: "2026-09-18T19:55:32.749521+03:00"
---

Problem (adversarial review of 534734bb..a6ce705f, CRITICAL, probes null5/null6/null7 under /tmp/hazel-review-home/p/): the null's permissive pointee arm is judged at the binding (src/core/checker.f BASE-BLOCK?, the 'kind TVK-NULL = CUR-STRICT @ 0 <> and IF RES-FALSE EXIT' arm; src/core/cell-effects.f NULL-PTR row) but the arithmetic rows (+, cell+, char+, 1+, 1-, - with n) keep the null's pointee var, so a computed offset still wears TVK-NULL and certifies as a typed pointer to any DATA word. Certified and ran on engine cee4c05e: 'variable V variable W 777 V ! V BYTE-VIEW NULL-PTR BYTE-VIEW - W ! : F ( -- ptr ptr n ) NULL-PTR W BYTE-VIEW NULL-PTR BYTE-VIEW - + ; : G ( -- n ) F @ @ ; G .' prints 777 (a raw variable the program wrote, read back as an address and dereferenced); the same offset stored through 'TYPED-VARIABLE PP ptr ptr n' then 'PP @ @ @' prints 777; parametric '( n -- ptr ptr n ) NULL-PTR swap +' prints 777. DATA-VA is a fixed mapping and 'X BYTE-VIEW NULL-PTR BYTE-VIEW -' yields any cell's integer address in checked code, so this is the data-base forgery c6c1d71d closed, reopened through the null. The quotation-parameter route is refused (E-NONPARAMETRIC at FEED); the output-row and typed-cell routes are open. docs/effects.md 'The null keeps one residue' claims the null cannot reach a writable attacker-chosen word - it can. Acceptance: a TVK-NULL pointee that rides an arithmetic row is raised to TVK-DBASE (TVK-RAISE-TO on the row's pointee var in FENCE-WHY's 'var is met with kind' path, or wherever the arithmetic rows bind the pointee), so an OFFSET null loses the permissive arm while '=', '!', distance '-', '0=' and a store of the literal NULL-PTR keep certifying (the 89 null stores in the tree, RELEASE's null store through the input-only excuse); red-first cases in test/compiler/base-pointer-arith-refusals.f for the three probes (depth-2 pointer, typed cell, parametric) plus the certifying controls; docs/effects.md's residue paragraph rewritten to the rule; the rule-hosted generation build proves no baked file is refused. Files: src/core/checker.f, test/compiler/base-pointer-arith-refusals.f, docs/effects.md. Verify: the probes; three generations with cmp; test/run.f. Depends: none. Ownership: checker. Parent: habu-campaign-c2-mem-c3d7662b. Claim: agent=hazel workspace=.jj-ws/hazel-null-offset.
