---
title: "CLOS defmethod not working - defmethod gives 'unbound variable' error"
status: closed
priority: 2
issue-type: task
created-at: "2026-01-01T10:32:47.812785+02:00"
closed-at: "2026-01-01T10:48:31.631253+02:00"
close-reason: "Partial fix: defmethod now compiles lambda with proper environment (params in scope). However, CLOS dispatch system incomplete - defmethod just defines function as single method lambda, doesn't create dispatcher. Need runtime method dispatch based on argument types using generic_functions table. This requires VM-level support for method selection."
---

File: src/compiler/compile.zig:4648

Issue: The CLOS system is implemented (defclass, make-instance, slot-value) but defmethod is broken. When defining methods with (defmethod name ((arg class) ...) body), it fails with 'unbound variable'.

Root cause: Need to check if defmethod compilation is incomplete or if there's a runtime registration issue for generic functions.

How to reproduce:
(defclass point () (x y))
(defmethod initialize-instance ((p point) x y) ...) ; fails here

Expected: defmethod should register the method with the generic function
Actual: Throws 'unbound variable' error

Fix approach: Check compileDefmethod in compile.zig to ensure it properly handles method specializers and registers with generic function table.
