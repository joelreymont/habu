---
title: Delete FILEMAP gate
status: active
priority: 2
issue-type: task
created-at: "2026-07-28T10:47:07.971349+02:00"
---

Why: FILEMAP.md is a 2,784-line manual index that duplicates repository search, omits Maki from its source policy, and creates a maintenance gate unrelated to inference correctness. Owned result: delete FILEMAP.md, tools/filemap-lint.f, and tools/filemap-lint-test.f; remove only their gate enrollment, suite classification, checked-boundary registration, host-lint fixture, and FILEMAP-specific comments. Replace tests that merely need an existing root file or invalid non-Forth include with README.md while preserving their exact production behavior. The required path changes expose two legacy global test files: put all definitions in lib/process-cwd-test.f inside private package PROCESS-CWD-TEST and all definitions in tools/host-lint-test.f inside private package HOST-LINT-TEST; each file still runs its private main word before closing the package, publishes no API, and changes no test behavior. In tools/lint/intern.f, change only the two preceding comment lines that say filemap-lint and FILEMAP.md are current capacity owners; leave the INTERN-MAX definition line byte-for-byte, including its truthful historical growth note, so this leaf does not absorb the blocked interner package migration. Exact code/test owners: test/gate-stdlib-cases.f, test/gate-stdlib-lint-tools.f, test/gate-stdlib-lib.f, tools/checked-boundary-lint-test-lib.f, tools/host-lint-test.f, tools/lint/intern.f, tools/lint/clobber-wrap-fixture.f, tools/lint/shadow-string-fixture.f, lib/process-cwd-test.f, test/compile-preflight-recovery.f, test/top-row-hook-test.f, and test/gate-size-attribution-test.f. Forbidden: replacement manifest, generated inventory, weakened host/package/trust checks, skipped suites, new host automation, interner API or storage changes, or unrelated cleanup. Checkpoint: current filemap suite is green; deleting only the three owned files makes exact registered loaders fail, proving the real enrollment paths. Acceptance: no executable source or test references either deleted tool or FILEMAP.md; the replacement fixture tests fail before their path edits and pass after; both test packages pass the package ownership gate without public words; host lint, checked-boundary lint, suite-coverage, lint-tools/status, process cwd, compile preflight, top-row hook, and the standard-library gate remain green. Ownership: executable gate and fixture removal plus the two required private test owners only.

Claim: RELEASED 2026-07-29 by the stale-claim audit. Agent `codex-filemap-gate` and workspace `.jj-ws/habu-delete-filemap-gate-32e7261d` are both gone: the directory does not exist and `jj workspace list` has no record of it. The work has not landed - `FILEMAP.md`, `tools/filemap-lint.f` and `tools/filemap-lint-test.f` are all still present. The dot stays active and is free to claim.
