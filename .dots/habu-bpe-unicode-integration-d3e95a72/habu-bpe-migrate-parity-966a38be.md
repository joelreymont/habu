---
title: "BPE: migrate parity consumers"
status: open
priority: 1
issue-type: task
created-at: "2026-07-23T11:01:08.791450+02:00"
---

Problem: the generated-vocabulary module still exports BPR-S1..16, BPR-I1..16, and BPR-N1..16, while production parity tests consume those writable globals. Landing BPE-PARITY alone would leave two authorities and make its immutable contract false.

Required result: remove every legacy corpus definition from the co-loaded closure: `BPR-S1..32`, `BPR-I1..32`, `BPR-N1..32`, `BPR-C17..32`, `BPR-U-N`, `BPR-U-ROW`, their descriptor table, `BPR-ASCII-HI`, and `BPR-FIX-HI`. Current master contains only the first sixteen source/id/count rows; rows 17..32 and the Unicode descriptor names survive only in rejected candidate `6d62573c78e0` and must remain absent rather than be carried forward. Preserve the compact vocabulary arrays, BPR-D-LOAD, and the distinct divergence, cafe, round-trip, and full-table fixtures. Correct the header so it truthfully separates the currently hand-committed compact vocabulary from curated parity evidence without claiming a missing generator. Require BPE-PARITY from the owning production tests and rewrite the 16-row encode/round-trip paths in bpe-test.f to use fixed caller-owned typed byte/cell spans filled by BPE-PARITY:FIXTURE and consumed by the typed BPE codec. In bpe-split-test.f, delete the duplicated definitions and source sites `C-ASCII`, `C-THAI`, `C-DEVANAGARI`, `C-GREEK`, `C-CYRILLIC`, `C-HEBREW`, `C-ARABIC`, `C-CJK`, `C-HIRAGANA`, `C-KATAKANA`, `C-FULLWIDTH`, `C-ASTRAL`, `C-COMBINING`, and `C-PUNCT`; drive all 16 canonical Unicode rows from BPE-PARITY:UNICODE through the typed production BPE-SPLIT:CHUNK-LEN. Retain the distinct whitespace, malformed-input, prefix, contraction, trailing-space, nested, interleaving, long-run, and routing fixtures. No forwarding aliases, raw backing pointers, copied rows, ordinal name table, consumer-local role cast, or compatibility globals. The later habu-bpe-generate-compact-2d82e099 leaf still owns deterministic compact-table generation.

Blockers: `habu-bpe-install-unicode-3c84e7a1`, `habu-bpe-type-splitter-1b611cd9`, and `habu-bpe-type-codec-3daa2fd6` must be reviewed and landed before this workspace is created. The exact reviewed BPE-PARITY commit from `habu-bpe-own-parity-00afdbd5` is its stacked implementation prerequisite: claim this leaf only after that candidate is immutable, build the migration directly on that candidate plus the verified blockers, and land exactly parity plus migration together. Neither parity nor migration may reach master alone. Owned result: removal of legacy and splitter-local corpus authorities and migration of every consumer only. It does not own corpus values, splitting, compact vocabulary generation, artifact parsing, role conversion, or BPE behavior.

Acceptance: the co-loaded production closure resolves all seven BPE-PARITY public words and cannot resolve any removed `BPR-S*`, `BPR-I*`, `BPR-N*`, `BPR-C*`, `BPR-U-*`, `BPR-ASCII-HI`, `BPR-FIX-HI`, or old descriptor-table word; no duplicated Unicode corpus source or chunk vector remains in bpe-split-test.f; fixture rows 0..15 still encode to exact copied identifiers; rows 10..15 still round-trip; and all 16 Unicode rows drive production BPE-SPLIT:CHUNK-LEN with exact copied chunks. Mutating a caller copy changes neither a second fetch nor the pinned corpus digest. Deleting either BPE-PARITY require, restoring any legacy definition, or restoring one Unicode duplicate fails the owning suite or structural absence check. Production BPE outputs and compact table bytes remain unchanged. Files: maki/examples/nanogpt/bpe-real-data.f, maki/examples/nanogpt/bpe-test.f, and maki/examples/nanogpt/bpe-split-test.f only. Smallest owning-path check: co-load both production BPE suites, prove old-name and duplicate-row absence structurally, run fixture rows 0..15 through real encode, rows 10..15 through round-trip, and all Unicode rows through production splitting. Also run typed-local, package, suite-coverage, and host gates.
