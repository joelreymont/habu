---
title: "BPE: type splitter spans"
status: open
priority: 2
issue-type: task
created-at: "2026-07-23T11:32:54.830125+02:00"
---

Problem: BPE-SPLIT:CHUNK-LEN and its production callers still conflate source byte lengths, cursors, and returned chunk byte lengths as raw n values, so the checker cannot reject length/index swaps before memory access. Required result: after accepted Unicode splitter commit 20de7db5f130a2f9085e3ebc4d982d7197cc6970 lands, change the sole public splitter API to CHUNK-LEN ( ptr u8 CAD-NUM:byte-len CAD-NUM:index -- CAD-NUM:byte-len ). Package BPE-SPLIT owns exactly two private audited representation projections for byte-len and index because its UTF-8 scanner and pointer arithmetic still consume raw cells. Each source boundary carries that rationale, `habu-nominal-storage-raw-a3430ef2` as retirement owner, and a focused production-path test; export neither projection nor any raw wrapper. Preserve the existing scanner, all Unicode semantics, and the single production route. Convert every direct caller and guard child through public CAD-NUM validators; no caller reopens CAD-NUM or defines a role-to-n bridge. Runtime cursor-at/after-end remains the named input error; negative/raw/role-swapped arguments become checker rejections. Owned result: splitter interface and direct caller migration only. It does not own corpus data, BPE codec interfaces, UTF-8 behavior, or new type-system features. Acceptance: the exact public inventory contains only typed CHUNK-LEN; raw length, raw cursor, byte-len/index swap, and return-role relabel candidates fail checking; every existing grammar, malformed-input, boundary, nested, interleaving, long-run, route, and 64K guard test passes through the production API; deleting either bounds check or either private projection's role constraint fails a focused negative or mutation; typed-local, package, host, and owning Maki suites pass. Files: maki/examples/nanogpt/bpe-split.f, bpe-split-test.f, bpe-split-guard-child.f, bpe.f only at its direct call seam, and focused tests. Smallest owning-path check: load the real splitter tests and guard suite, certify the four checker negatives, then mutate cursor bounds and terminal-apostrophe depth. Blocked by habu-bpe-install-unicode-3c84e7a1.

Blocker sweep 2026-08-21 (tracker GC): the blocks: list is gone because every entry in it was already closed - habu-bpe-install-unicode-3c84e7a1. The prose above still names them as prerequisites; they are satisfied, and nothing in the tracker blocks this leaf now.
