---
title: "BPE: own parity corpus"
status: open
priority: 1
issue-type: task
created-at: "2026-07-23T09:46:40.909881+02:00"
---

Problem: `maki/examples/nanogpt/bpe-real-data.f` claims to be generated by a missing Python script but also contains hand-curated reference strings, chunk boundaries, token identifiers, tool version, and source-artifact provenance. That mixes generated tables with review-owned test evidence and makes later edits unauditable.

Required result: create package `BPE-PARITY` as the sole owner of the small immutable GPT-2 parity corpus. Its public surface is `ENCODER-SHA$`, `VOCAB-SHA$`, `REFERENCE$`, `FIXTURE-N`, `FIXTURE`, `UNICODE-N`, and `UNICODE`. `FIXTURE ( n -- ptr u8 n ptr a n )` returns one source and exact real token-id vector for all existing ASCII and multilingual rows. `UNICODE ( n -- ptr u8 n ptr a n ptr a n )` returns one Unicode source, its exact chunk-length vector, and its exact real token-id vector. Invalid indexes throw one package-owned named range error. Tables and descriptor storage remain private and immutable. Pin tiktoken 0.13.0 plus exact encoder.json and vocab.bpe sizes and SHA-256 values in executable accessors and prose. This module is curated reference evidence, never generated output.

Prerequisites: none; retain the already reviewed strings and vectors from candidate `6d62573c78e0`, correcting only proven data errors. Owned result: corpus identity, provenance, descriptors, and bounds only. It does not own tokenization, the compact vocabulary, artifact parsing, or generation.

Acceptance: every retained row is represented exactly once; descriptor counts and spans are structurally validated; low/high indexes reject; source, chunk, id, version, size, or digest mutations fail the focused corpus test; old `BPR-S*`, `BPR-I*`, `BPR-C*`, and `BPR-U-*` fixture names are absent from the public dictionary. Files: `maki/examples/nanogpt/bpe-parity-data.f`, its focused test, `FILEMAP.md`. Smallest owning-path check: load the real corpus module and focused test through `bin/hb`, including descriptor mutation and public-name rejection. Also run exact typed-local, package, host, and file-map checks. Claim: unassigned.
