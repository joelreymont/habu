---
title: "BPE: own parity corpus"
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-23T09:46:40.909881+02:00\""
---

Problem: `maki/examples/nanogpt/bpe-real-data.f` claims to be generated by a missing Python script but also contains hand-curated reference strings, chunk boundaries, token identifiers, tool version, and source-artifact provenance. That mixes generated tables with review-owned test evidence and makes later edits unauditable.

Required result: create sealed package `BPE-PARITY` as the review-owned source of the small immutable GPT-2 parity corpus. Its exact public surface is `ENCODER`, `VOCAB`, `REFERENCE`, `FIXTURE-N`, `FIXTURE`, `UNICODE-N`, and `UNICODE`. `ENCODER ( ptr u8 CAD-NUM:byte-len -- CAD-NUM:byte-len CAD-NUM:byte-len )` and `VOCAB ( ptr u8 CAD-NUM:byte-len -- CAD-NUM:byte-len CAD-NUM:byte-len )` copy the pinned SHA-256 text into a caller-owned byte span and return `(digest-length artifact-size)`. `REFERENCE ( ptr u8 CAD-NUM:byte-len -- CAD-NUM:byte-len )` copies `tiktoken 0.13.0` and returns its length. `FIXTURE-N ( -- CAD-NUM:item-count )` returns exactly 32. `FIXTURE ( CAD-NUM:index ptr u8 CAD-NUM:byte-len ptr a CAD-NUM:item-count -- CAD-NUM:byte-len CAD-NUM:item-count )` consumes `(row source-destination source-capacity identifier-destination identifier-capacity)`, copies the canonical row, and returns its source length and identifier count. `UNICODE-N ( -- CAD-NUM:item-count )` returns exactly 16. `UNICODE ( CAD-NUM:index ptr u8 CAD-NUM:byte-len ptr a CAD-NUM:item-count ptr a CAD-NUM:item-count -- CAD-NUM:byte-len CAD-NUM:item-count CAD-NUM:item-count )` consumes `(Unicode-row source-destination source-capacity chunk-destination chunk-capacity identifier-destination identifier-capacity)` and returns the three copied lengths. Unicode row `i` is the same source and identifier descriptor as fixture row `16+i`, plus that row's chunk descriptor; no source or identifier is stored twice.

Package `BPE-PARITY` owns exactly three private audited representation projections for `CAD-NUM:byte-len`, `CAD-NUM:item-count`, and `CAD-NUM:index`. They exist only because its descriptor table, pointer arithmetic, and fixed-span copies still consume raw cells. Each source boundary carries that rationale, `habu-nominal-storage-raw-a3430ef2` as retirement owner, and a focused production-path test. Export no projection or raw wrapper. Callers construct roles through public `CAD-NUM` validators; no caller may reopen `CAD-NUM` or add its own cast.

Every copy entry follows this exact preflight and error precedence before changing a destination byte: project and range-check the index, throwing `E-BPEP-RANGE`; read all six descriptor cells into locals; validate all descriptor relations and arena extents, throwing `E-BPEP-DATA` for an impossible internal inconsistency; project capacities and prove each `item-count * cells` without multiplication overflow; reject insufficient capacity with `E-BPEP-CAP`; reject a null destination for a positive result or pointer-plus-write-length overflow with `E-BPEP-SPAN`; then reject every exact or partial pairwise overlap between the actual destination write spans with `E-BPEP-ALIAS`. The metadata accessors use the same capacity, null, and address-end ordering but have one destination and therefore no alias case. Copying and big-endian decoding then have no remaining fallible or allocating step. A validated index at or above the row count rejects without changing destinations; raw or negative indexes and byte-length/item-count role swaps are checker rejections.

No public word returns a pointer into authoritative storage. Use one private byte arena. For each fixture row 0..31, append source bytes, then its chunk values as unaligned unsigned 64-bit big-endian integers when present, then identifier values in the same encoding; empty chunk spans use offset zero. Store one six-cell descriptor per row in this exact order: source byte offset, source byte length, chunk byte offset, chunk item count, identifier byte offset, identifier item count. Derive every value from compiler-known start/end labels rather than repeated numeric counts. After the validated row selects one in-bounds descriptor, read all six cells before deriving any arena pointer. Validate source and identifier extents; require chunk count zero exactly when chunk offset is zero; prove each nonzero item count can be multiplied by eight; and validate chunk and identifier byte extents against the arena with overflow-safe subtraction. `FIXTURE` and `UNICODE` decode every stored big-endian unsigned 64-bit chunk or identifier into one native destination cell; their capacities are item counts and their actual cell write spans are `count * cells`.

Before sealing, verify this exact canonical serialization: ASCII `BPE-PARITY`; unsigned 64-bit big-endian format version `1`; reference length and bytes; encoder artifact size, digest length, and digest bytes; vocabulary artifact size, digest length, and digest bytes; fixture count `32`; Unicode first row `16`; Unicode count `16`; arena byte length and complete raw arena bytes; descriptor count `32`; then all six descriptor cells for rows 0..31 in the field order above, each encoded unsigned 64-bit big-endian. Its SHA-256 is `dbf80894938c286fccbf629ee3a4d7288ae61eefd5ed33deae3dcd2cc9e3106d` (4,816 serialized bytes; 3,039 arena bytes). Row 31 is an 87-byte source whose 50 chunk lengths sum to 87; deleting four source bytes to preserve the earlier stale totals is forbidden. Seal both private and public package wordlists. Pin tiktoken 0.13.0 plus exact encoder.json and vocab.bpe sizes and SHA-256 values in executable accessors and prose. This module is curated reference evidence, never generated output. Do not add `PRODUCT`, `SUMTYPE`, legacy payload `ENUM`, a public raw constructor, or a second mutable registry.

Prerequisites: package `CAD-NUM`; retain the already reviewed strings and vectors from candidate `6d62573c78e0`, correcting only proven data errors. Owned result: corpus identity, provenance, descriptors, immutable copy-out, and bounds only. It does not own tokenization, the compact vocabulary, artifact parsing, generation, or removal of legacy fixture exports. The separately claimable `habu-bpe-migrate-parity-966a38be` leaf removes every duplicate fixture and migrates production consumers after the Unicode splitter and typed BPE codec land; this commit must not reach `master` without that dependent commit in the same verified integration stack.

Acceptance: every retained row is represented exactly once inside this owner; row 0..31 order and the `UNICODE(i)==FIXTURE(16+i)` relation are pinned; descriptor counts and spans are structurally validated; raw-index and length/count role swaps fail checker negatives; the count index, null positive span, every undersized destination, every identical destination, every partial overlap, item-count multiplication overflow, and address-plus-write-length overflow reject in the pinned precedence before destination contents change; copies may be mutated and refetched without changing the corpus; a checked hostile reopen fails; mutations of every descriptor offset/count, source byte, chunk, identifier, format version, provenance string, artifact size, or pinned digest fail the focused corpus test; and the `BPE-PARITY` public inventory is exact. Enroll the focused test in both `maki/test-core.f` and `maki/test.f`, then prove enrollment by making its final assertion fail. This leaf does not edit compact vocabulary data or existing callers. Files: `maki/examples/nanogpt/bpe-parity-data.f`, its focused test, and both suite inventories. Smallest owning-path check: load the real corpus module and focused test through `bin/hb`, including raw-arena/descriptor digest mutation, copy mutation, sealed-package rejection, alias/capacity/role rejection, and package public-inventory rejection. Also run exact typed-local, package, suite-coverage, and host checks. Claim: agent=bpe_parity_data workspace=.jj-ws/habu-bpe-own-parity-r2.
