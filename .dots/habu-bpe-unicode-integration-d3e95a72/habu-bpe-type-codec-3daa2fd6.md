---
title: "BPE: type codec spans"
status: open
priority: 2
issue-type: task
created-at: "2026-07-23T11:33:10.066884+02:00"
blocks:
  - habu-bpe-type-splitter-1b611cd9
---

Problem: MAKI:BPE-ENCODE, BPE-DECODE, BPE-TRAIN, BPE-MERGES and the real GPT-2 BPR codec still expose byte lengths, item counts, capacities, and identifier counts as interchangeable raw n values. Required result: on top of the typed splitter leaf, change the public codec surfaces to BPE-ENCODE ( ptr u8 CAD-NUM:byte-len ptr a CAD-NUM:item-count -- CAD-NUM:item-count ), BPE-DECODE ( ptr a CAD-NUM:item-count ptr u8 CAD-NUM:byte-len -- CAD-NUM:byte-len ), BPE-TRAIN ( ptr u8 CAD-NUM:byte-len CAD-NUM:item-count -- CAD-NUM:item-count ), BPE-MERGES ( -- CAD-NUM:item-count ), BPR-INSTALL with a CAD-NUM:item-count merge count, BPR-MERGES ( -- CAD-NUM:item-count ), BPR-ENCODE with the same typed encode effect, and BPR-DECODE with the same typed decode effect. Packages MAKI and the real-codec owner may each keep only the minimal private audited byte-len/item-count projections required by their existing fixed-array and pointer loops; inventory them in TRUSTED.md, bind retirement to habu-nominal-storage-raw-a3430ef2, export no projection or raw compatibility wrapper, and do not duplicate the codec. Convert every production caller and focused test through public CAD-NUM validators. Negative raw/negative/role-swapped sizes become checker rejects; insufficient valid capacities retain their named runtime errors. Owned result: one behavior-neutral public span/count cutover. It does not own tokenization, corpus values, buffer resizing, compact vocabulary generation, or type-system changes. Acceptance: old raw signatures no longer certify; byte-len/item-count/capacity swaps and return relabels reject; exact GPT-2 ids, round trips, training merges, capacity failure atomicity, canary preservation, Unicode routing, and real codec outputs remain byte-identical; no public raw converter or duplicate wrapper resolves; focused mutation removes each capacity guard and is killed; typed-local, package, strict trust, host, file-map, and owning Maki suites pass. Files: maki/examples/nanogpt/bpe.f, bpe-test.f, bpe-real.f, bpe-real-data.f only where install/count signatures require it, dependent production tests, TRUSTED.md, FILEMAP.md. Smallest owning-path check: certify the typed API inventory and role negatives, then run real encode/decode/train/canary suites on the exact production path. Blocked by habu-bpe-type-splitter-1b611cd9; neither stacked type commit may land alone if the first leaves a raw adapter at the bpe.f call seam.
