---
title: Prove ENUM kind warm snapshot
status: open
priority: 1
issue-type: task
created-at: "2026-07-24T03:11:51.011542+02:00"
blocks:
  - habu-finalize-enum-kind-0fda746b
---

Why: the finalizer must rely on existing type-family snapshot persistence, not a second publication registry or restore hook. Owner and production seam: add package ENUM-KIND-SNAPSHOT in test/enum-kind-snapshot.f. It creates one private build root, sets test/enum-kind-snapshot-source.f as the sole BUILD-EXT keep source, invokes the existing BF-BUILD-STDIN-FRESH then BF-BUILD-SNAP-FROM-STDIN path, clears BUILD-EXT on success and failure without replacing the primary throw, and runs the emitted hb-new through its real warm startup with --load test/enum-kind-snapshot-check.f. The kept source declares one full payloadless ENUM named snapkind through ENUM-DECL:ED-RUN, so FINALIZE-ENUM chooses TK-ENUM before SNAP-RETIRE-GO writes the image. The warm checker resolves snapkind through the production signature registry, proves its kind is TK-ENUM, opens a real GENERATED-DECL scope, emits a new DECL-EVENT frame for the restored family, and proves FINALIZE-ENUM rejects E-DEV-FAMILY-SCOPE 7173 before kind or event publication changes because the restored family predates the new savepoint. It then declares and finalizes a new payload ENUM successfully to prove warm declaration state remains usable. No raw kind store, forged image, copied snapshot writer, owner-WID fixture edit, build-tool API change, persisted latch, registry, restore hook, or production code change. Checkpoint: on the exact finalizer tree, run the normal payloadless and payload ENUM cases, build one unmodified real snapshot, and prove the existing owner-WID warm harness path and package gates accept the representative new test packages. Stop on any need to change tools/build-fixpoint.f, src/habu snapshot code, type-family persistence, finalizer authority, or a file outside the list. Acceptance: cold source sees snapkind TK-ENUM before snapshot; real hb-new exits zero and reports the restored TK-ENUM, exact 7173 rejection with unchanged family kind and event high-water, and successful publication of a new TK-SUM family; changing the kept source to a payload ENUM, skipping SNAP-RETIRE-GO, removing the range guard, mutating the restored kind, using the cold engine instead of hb-new, or failing to clear BUILD-EXT makes an owning-path regression fail. Two independent private-root builds produce byte-identical warm images and both checks pass. Files: test/enum-kind-snapshot.f, test/enum-kind-snapshot-source.f, test/enum-kind-snapshot-check.f, test/gate-stdlib-cases.f, test/run-files.f, and FILEMAP.md. Smallest check: bin/hb --load test/enum-kind-snapshot.f. Run exact package, typed-local, trust, file inventory, snapshot, candidate, fixpoint, and touched native gates; root batches the full native gate after integration. Claim: unassigned.
