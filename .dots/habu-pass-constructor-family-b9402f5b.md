---
title: Pass constructor family explicitly
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-23T06:59:22.740284+02:00\""
---

Problem: src/core/sumtype.f constructor generation reads ambient TDECL-FAM-REG, so a caller cannot prove which family the complete generated plan belongs to. This refactor preserves existing publication behavior and therefore does not depend on the separate total-release correction; named ENUM constructor publication still may not merge before habu-make-txn-release-c9892c89 closes the late-failure hole. Owner and scope: the existing engine-private generated-constructor owner in src/core/sumtype.f only; add no global or package definition. Change the existing TDECL-CTOR-WORDS-BODY to ( n -- n ). It receives one live family id, runs the complete existing render/preflight/evaluate/protect path inside its own catch boundary, returns that same id on every normal branch, always clears pending constructor authority, and rethrows the exact code on failure. It never reads or writes TDECL-FAM-REG. Change the existing TDECL-CTOR-WORDS to the sole legacy adapter: read TDECL-FAM-REG, consume the -1 multi-error refusal without generation, otherwise call the explicit body and drop its returned family. Existing SUMTYPE, legacy ENUM, and PRODUCT remain the only callers of that adapter. No new definition, public generator, hook, staging variable, checker-effect change, grammar change, ENUM-DECL hookup, alias, or field work. Acceptance: the three declarations preserve constructors, effects, order, rollback, protected wordlists, snapshots, ahead-of-time images, and fixpoint bytes; a test inside the existing generator owner calls the explicit body for a live non-last family and proves it generates only that family's plan; render, preflight, evaluator, and protection failures clear pending authority and preserve the exact family/error contract. Red proof: changing the body effect before migrating the adapter makes native self-certification reject; after the adapter is migrated, it certifies. Verify: constructor, type-declaration, and generated-transaction suites; native fixpoint twice; exact typed-local and package diff lints; trust, host, and file-map gates. Claim: agent=ctor_explicit_family workspace=.jj-ws/habu-pass-constructor-family-b9402f5b.
