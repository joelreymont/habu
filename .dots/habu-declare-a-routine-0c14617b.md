---
title: "Declare a routine's calling convention instead of inferring it"
status: active
priority: 2
issue-type: task
created-at: "\"2026-08-09T23:10:26.137489+02:00\""
---

ALL 327 E-A64SEL-CALL census refusals are ONE cause (measured 2026-08-09 by per-site instrumentation on 2d133ac2, every other bucket byte-identical): select.f:837 CONTRACT-CK's DSTACK? proxy is vacuous for a ( -- ) routine, so any arity-zero definition with a call is refused - while the lowering already works (: RP-VOID-CALL with five call sites migrates rc 0 and EXECUTES correctly). No inference can fix it: a64-effect.f:657 documents SEQ-NONE as meaning BOTH 'no values' and 'values, placement unconstrained', and the probe predicate that guessed was falsified by mutation (uncaught throw at select.f:3623 DENTRY-IN in the select suite). FIX: a declared convention enum (DSTACK | REGISTER) on A64EFF:routine beside control and link - one writer for the fact that select.f DSTACK?/DSTACK-CK, regalloc.f REG-POSITIONS/LOWERED-CK (:623,:672) and regalloc-verify.f REG-POSITIONS/ARG-CK/OUT-CK/VDSTACK-CK (:723,:2092) each independently re-infer today. Touches: a64-effect.f (structure :244, SLOTS 14->15 :529, digest :914, SAME? :888), the four NABI constructors in abi.f (:95,:114,:147,:161 - the only production writers), the six readers above; 32 A64EFF:ROUTINE test sites gain an argument; digest pins in test/compiler/a64-effect.f:553 move; nothing in formal/ reads it. DSTACK-CK then refuses a mixed convention unconditionally - strictly stronger than today. MEASURED VALUE: census compiled 1923 -> 2186 (+263, 55.6% -> 63.2%); the knock-on E-A64RA-POOL +43 is deliberately NOT this dot's to fix (see habu-price-the-pool dot; the post-cut register convention deletes that work). Acceptance: a ( -- )-that-calls compiles AND runs (execute the published word, check side effects); a REGISTER contract naming a data-stack slot refused; a DSTACK contract naming a register refused; codegen-compare BYTE-IDENTICAL on all five committed tables (measured, not argued - for everything compiling today the declared convention equals the inferred one, and emit.f:1853 PUT-DMOVE writes nothing for zero distance); census E-A64SEL-CALL 0. Gates: five native suites, a64-effect, migrate/publish, maki, full gate. Downstream consumer: habu-pass-args-in-da01bd62 (this field is its first brick). Files: src/compiler/a64-effect.f, src/compiler/native/{abi,select,regalloc,regalloc-verify}.f, tests. Depends: none.

Claim: agent=selcall workspace=.jj-ws/habu-selcall-decomp
