---
title: Centralize Maki suite slices
status: open
priority: 1
issue-type: task
blocks:
  - habu-lowering-hash-unified-586f7881
created-at: "2026-07-19T22:44:08.031164+02:00"
---

Current master gate-source bloat after d1f42dba split the Maki suite. maki/test.f lists 169 TEST:SUITE members; test-core/db/eval/eval-emit repeat exactly the same 169 members as 126+27+14+2 entries. test-core.f still claims 120 suites while listing 126. test/run-lib.f then hand-maintains three parallel four-way index dispatches for loader path, label, and temporary directory, each silently mapping every index >=3 to eval. maki-dep-lint-core.f further allows any maki/test-*.f token rather than the exact declared set. test-harness.f opens MAKI-TEST but exports RUN-A/RUN-U/START-NS and every RUN-*/INCLUDE/NUM helper even though consumers only need its load-time installer effect. One suite addition therefore requires synchronized edits across master, one slice, and runner metadata; the new positional-embedding suite repeated both master and slice entries again while leaving the stale core count farther behind. A slice-count change can silently alias loader/label/temp identities. Define one canonical package-owned STRUCTURE suite-entry {file,slice} and STRUCTURE slice {id,loader,label,tmp,order}; use a closed ENUM slice-id. Derive the full standalone inventory, filtered slice entrypoints, and runner launch rows from that one table without generated committed copies. Bounds-check every slice lookup and remove default dispatch. The dependency lint must admit only canonical declared loader paths. Make the harness implementation private with short names and replace accidental import side effects with one deliberate installer boundary. Preserve exact 169-member order, four current partitions, heaviest-first launch order, labels, temporary roots, candidate routing, standalone full run, outputs, and timing budgets. Add compile-negative field/id swaps, unknown/duplicate/missing slice/member cases, count-change mutation, exact canonical inventory/partition snapshot, and proof every member runs once in the parallel gate and once in standalone full mode. Measure source lines, definitions, public dictionary surface, JIT/DATA/CODELEN, and gate wall before/after; require substantial duplication reduction. Files: maki/test*.f, test/run-lib.f, tools/maki-dep-lint-core.f. Blocker: habu-lowering-hash-unified-586f7881. Coordinate generic suite row work with habu-structure-test-suite-f58530bb without duplicating lib/test representation.

2026-07-20 inherited from the decoupling landing (4ffe4da6): the 22 TEST:SUITE registrations of maki/examples/nanogpt test files inside maki/test.f + maki/test-core.f are the LAST library-side references to the example; relocating them needs this dot's aggregator/partition rewire.
