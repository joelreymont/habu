---
title: Parse STRUCTURE binder heads
status: active
priority: 2
issue-type: task
created-at: "2026-07-30T21:00:57.007064+02:00"
---

Problem: src/core/structure-decl.f requires a separate decimal arity token and resolves parameters by positional alphabet, while the reviewed hard cut requires STRUCTURE name<binders> and one shared declaration-head authority. Exact interface: package DECL-HEAD is added to the existing checked post-hook declaration-intake owner src/core/generated-declaration.f immediately after DECL-REPLAY and before CHECKER-DECL-FRAME. It owns PARSE ( ptr u8 n -- ptr u8 n n bool ), returning the borrowed bare family tail, binder count, and whether an explicit binder list was present, and PARAM? ( ptr u8 n -- n bool ), resolving only the current head's binder to its declaration ordinal. PARSE copies binder bytes into one fixed-capacity transient map owned by DECL-HEAD; it never retains the borrowed source pointer. Binders are distinct one-byte lowercase members of TFAM-DECL-PARAM-ALPHABET; f, n, and r are concrete types and cannot be binders. Binder order defines schema ordinals. A bare name has arity 0; name<> is explicit arity 0. Malformed delimiters, empty entries, duplicates, uppercase or multi-byte binders, concrete binders, and the retired numeric arity form reject through the declaration transaction with full registry and dictionary rollback. STRUCTURE-DECL:SD-RUN and STRUCTURE-DECL:SD-REPLAY both call this owner, register the stripped tail with arity equal to the binder count, and resolve FIELD binder tokens through DECL-HEAD:PARAM? after n, f, and r. Acceptance: production fixtures cover pair<e,a> with e at schema ordinal 0 and a at 1, phantom<e> with arity 1 and no e field, bare plain with arity 0, explicit empty<>, hostile heads, retired STRUCTURE pair 2 syntax, replay parity, byte-identical rollback, and source-buffer reuse before PARAM? lookup. Forbidden: parser copies in consumers, retained borrowed pointers, persistent binder metadata or ABI fields, inferred arity, compatibility aliases, accepting naked counts, a test-only parser, PPRIM: rows, checker axioms, TRUST boundaries, forwarding shims, a new source file, or load-mirror edits. Checkpoint seam: before the change, a checked qualified caller of a DECL-HEAD word fails because no post-hook owner exists; after the move, the post-hook probe certifies and calls successfully. Files: src/core/generated-declaration.f, src/core/structure-decl.f, test/structure-decl-suite.f, and test/decl-replay-verify-source.f. Verify: generate one jj diff artifact; run bin/hb --load tools/typed-local-diff-lint.f and bin/hb --load tools/package-diff-lint.f on it; inspect the new-grammar and retired-numeric rg census. The production fixtures are authored here and executed by the single M17 gate battery required by TYPE-FIXES-PLAN.md. Depends: none. Ownership: DECL-HEAD parser and STRUCTURE live/replay binder-head consumption only. Claim: agent=e5a_impl workspace=.jj-ws/habu-parse-structure-binder-1da50c1c.
