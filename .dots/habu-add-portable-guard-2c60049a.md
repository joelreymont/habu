---
title: Add portable guard-page test scope
status: active
priority: 1
issue-type: task
created-at: "2026-07-28T01:36:54.433852+02:00"
---

Problem: production-path byte-span tests need a readable lower page followed by an inaccessible page. The JSON test currently ignores munmap results and invents half-allocation ownership. Owner and interface: package GUARD-PAGE in lib/test/guard-page.f owns public WITH-BYTES ( R [ R ptr u8 CAD-NUM:byte-len -- S ] -- S ). It allocates one 128 KiB mapping, retains the exact pointer and CAD-NUM:alloc-byte-len as sole whole owner until splitting, releases the upper 64 KiB, and exposes only the lower 64 KiB. A private throwing UNMAP defer defaults to raw munmap and throws E-MEM-UNMAP on nonzero. Exactly one private TRUSTED: SCOPE has the same row-polymorphic effect; its source carries the rationale, this dot as retirement owner, and the focused production-path test; the checked public wrapper owns no raw state. Cleanup contract: if upper unmap fails, do not call the body; attempt full-range cleanup and preserve the first error. If upper unmap succeeds, catch the body, unmap only the lower half, preserve a body error over cleanup failure, and otherwise propagate cleanup failure. Never release the full range after a successful split, call MEM:RELEASE-BYTES on a half, or mint half allocation lengths. Tests in lib/test/guard-page-test.f script private UNMAP while every scripted success performs the real unmap. Child processes prove live and inaccessible ranges after normal return, body throw, upper failure plus full cleanup failure, lower cleanup failure, and body throw plus lower cleanup failure; tests then explicitly clean any deliberately retained mapping. Exact call order, pointers, lengths, body suppression, first-error priority, and cleanup propagation are asserted. Linux and macOS process outcomes are graded structurally. Migration: replace only the local fence helper in test/json-read-perf-phase-test.f with GUARD-PAGE:WITH-BYTES; preserve the real truncated JSON parser case. Register the package and focused test in test/gate-stdlib-cases.f, test/gate-stdlib-inline-lib.f, docs/stdlib.md, and LESSONS.md, beside memory-test in the standard-library tail; do not add it to Maki. Forbidden: ignored syscall results, Linux-only cleanup, public raw unmap or pointer ownership, leaked mappings, fake allocation owners, aliases, copied parser logic, behavior changes, a second trusted boundary, or callers beyond the named JSON test. Dependency: none. Checkpoint: record the live munmap drop and forged half-owner defect through the owning JSON test and prove package ownership on a representative diff before implementation. Acceptance: focused guard-page and JSON production tests, hostile syscall scripts and operating-system fault probes, typed-local, package, and standard-library tail gates pass. Mutations that ignore either unmap result, omit cleanup, clean across the hole, or fabricate half ownership fail.

Claim: agent=guard-page workspace=.jj-ws/habu-add-portable-guard-2c60049a
