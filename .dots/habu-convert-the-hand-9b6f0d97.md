---
title: Convert the hand-judged raw pointer sites
status: open
priority: 2
issue-type: task
created-at: "2026-09-17T18:44:23.885163+03:00"
---

Problem: the raw-storage census (/home/joel/.cache/hazel/scout-pun/census.md) marks 105 rows 'other' (the classifier could not name a mechanical shape) and 78 rows AMBIGUOUS (the reported definition name resolves to more than one declaration), and over-counts rows whose token is cell+, char+ or + (armed, RAW-OK? admits the pointer row where the live rule certifies the scalar row). Acceptance: each row inspected in its body and either converted to a declared form, or recorded as certifying under the live rule (measured on ~/.cache/hazel/engines/raw-rule-gen1), or handed to its directory lane with the form named; the census annotated per row; no TRUSTED: or unchecked seam as a conversion. Files: per the census rows. Verify: raw-rule-gen1 loads of each closure; the affected suites; test/run.f. Depends: the definer lane. Ownership: whoever owns each file. Parent: habu-refuse-a-ptr-5ad2734e. Claim: unassigned.
Amendment 2026-09-18 (engine-source lane): the here-latched family is this lane's one design decision, not a mechanical row: src/habu/aot-arm.f D0/D1 (latch-open, window-close; read by AOT-ARM:WINDOW$ ( -- n n n n n n ), AOT-CAPTURE:CAPTURE, PRELUDE-MARK, tools/native-build-core.f:132,133,274,290, tools/aot-chain-capture.f:298-305), src/habu/aot-closure.f BLOB-SRC/BLOB-END (aot-closure.f:507 DATA-ADDRESS! compares a cell VALUE v:n against them: genuinely numbers) and src/habu/aot-lib.f DSCAN; here is PE-PTR-A-RAW and there is no checked pointer-to-integer direction, so either the span API becomes pointer-typed end to end or the numeric comparisons go through the EXISTING boundary word PTR>N (a TRUSTED row already in the tree; using it adds no seam) at the few comparison sites while the cells themselves are declared pointers. Decide once, convert the family, and list any new TRUST row as a refusal. The four Tender words SCRATCH, P-CELL, OPTIONS, EXPECTED are the parameter shape of habu-refuse-ptr-field-331a9731, not this lane's.
Amendment 2026-09-18 (test lane): one mechanical leftover, tools/build-fixpoint-test.f:700 'variable DEF-A' (suite build-fixpoint-fixtures), same shape as the converted cells; take it with the here-latched family.
