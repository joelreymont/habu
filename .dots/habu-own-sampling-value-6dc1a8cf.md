---
title: Own sampling value types
status: closed
priority: 1
issue-type: task
created-at: "2026-07-30T01:41:05.560963+02:00"
closed-at: "2026-08-04T16:05:02+02:00"
close-reason: "Dissolved: neither proposed public type has a public consumer; SAMPLE:config's sole consumer is future INFER:OPEN-SEQ and public MAKE defeats its validation, while stop matching stays entirely inside planned INFER model dispatch/NEXT-MANY. Rejected candidate 54165d8eba2e19d0a492381ff8185f0c91ad6a87 was discarded, not landed."
---

Why: OPEN-SEQ and closed model arms need immutable sampling inputs before NEXT-MANY exists. Result: package `SAMPLE` owns one `config` record containing finite temperature `r`, validated positive `CAD-NUM:item-count` top-k, and finite top-p `r`, plus one `stop-set` sum with exact `one(CAD-NUM:index)` and `two(CAD-NUM:index,CAD-NUM:index)` arms. `CONFIG ( r CAD-NUM:item-count r -- result<SAMPLE:config,n> )` accepts temperature at least zero, top-k at least one, and top-p greater than zero and at most one; it rejects NaN and either infinity with named sampling error codes. `STOP` and `STOPS` construct the one- and two-token arms from already validated `CAD-NUM:index` values and preserve order; model arms later prove each index is below their vocabulary count. Use the existing generic `result`; no configured/refused result family and no new token-identifier nominal type.

Owner: `maki/infer/sampling-types.f` declarations and construction only. Dependencies: existing `CAD-NUM:item-count`, `CAD-NUM:index`, the existing finite-float predicate pattern, and generic `result`. Production red: the tree has no `SAMPLE` package. Acceptance: zero and finite positive temperature, top-k one and greater, top-p minimum/one, and finite interior values publish the exact record; negative temperature, zero top-k, zero/above-one top-p, NaN, and both infinities reject with the pinned error. `STOP` and `STOPS` preserve exact nominal identifiers and order, and raw `n`, byte length, and item count cannot substitute for an index. Public inventory is exactly the declared value types, constructors, accessors, and named errors needed to eliminate their results; no model constant is stored. Forbidden: selection algorithm, random state, engine row, tokenizer, output publication, model registry, default configuration, version, compatibility path, special result type, extra nominal type, cast framework, lint, manifest, suite, or scaffold. Smallest owning check: `bin/hb --load maki/infer/sampling-types-test.f`, including checker negatives for role substitution and one mutant per rejected float class.
