---
title: Rename CAD-NUM to NUM and close its proof-erasure reopeners
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-16T18:14:16.994366+03:00\""
---

Problem: lib/cad-num-types.f and lib/cad-num-arithmetic.f are Habu's generic nominal numeric roles (byte-len, item-count, cell-count, index, byte-off, cell-off, alignment, positive-divisor, alloc-*) and the numeric-result ADT; nothing in them is CAD, the name is a leftover of where they were first written, and they are used by lib/string.f, lib/memory.f, lib/float.f, lib/elf32.f, lib/serial.f, lib/net/udp4.f, lib/process-env.f, lib/object.f, tools and by Loom and Maki. Joel (2026-09-16): CAD is Maki and Loom, why is CAD-NUM in Habu. Worse, 18 files reopen `package CAD-NUM` (lib/float.f, lib/process-env.f, lib/object.f, tools/build-fixpoint.f, tools/codegen-role.f, tests) to reach its PRIVATE proof-erasure projections (*>N), which is the pattern the sealing rule forbids and what blocked the private-record drop (habu-ship-no-dictionary-2fee2dea WIP). Acceptance: (1) the two files become lib/num-types.f and lib/num-arithmetic.f with package NUM (a rename, no semantic change), every consumer in Habu updated in the same commit, and the Loom and Maki consumers listed with the exact edits they need (do not edit those trees; send the list to willow); (2) no file outside the two reopens the package: each reopener either gets a public bounded projection with a stated reason or its raw-cell word moves into the package's own files; the six private casts stay private; (3) whether NUM is engine-provided follows the manifest rule (habu-bake-only-what-4039004a): if lib/memory.f and lib/string.f keep using the roles it stays in the compiler's closure, otherwise it leaves the engine with the rest of lib; (4) full gate green, Radar/Tender/Loom/Maki launchers audited for the old name. Files: lib/cad-num-*.f (renamed), the consumer files above, docs/stdlib.md, docs/forth.md if it names CAD-NUM. Verify: the affected suites; tools/native-build.f fixpoint; test/run.f; downstream. Depends: none; coordinate with habu-bake-only-what-4039004a (same lane). Ownership: numeric roles. Claim: agent=hazel-private-words workspace=.jj-ws/hazel-private-words.
