---
title: Refuse a pointer laundered through a plain variable
status: active
priority: 1
issue-type: task
created-at: "2026-09-17T15:59:08.087876+03:00"
---

Problem: a checked word can read and write memory at any integer address with no TRUST row by passing the integer through raw storage: 'variable V  : PEEK ( n -- n ) V ! V @ @ ;  : POKE ( n n -- ) V ! V @ ! ;' certify and run on the integrated engine (probe 2026-09-17 on 6fb809de; scout report ~/.cache/hazel/scout-pun/report.md on d5e871c0). Raw storage publishes a pointer to a cell of a fresh TVK-RAW type per mention (variable, create and constant through trust-raw, here and data-base through PE-PTR-A-RAW), and RAW-OK? (src/core/checker.f:2107-2113) admits a pointer into that cell, so the n stored by one mention comes back from the next as a value @ and ! accept as an address. The same pun works through a create'd cell, a constant with no store at all ('A constant K  : F ( -- n ) K @ ;'), here, data-base, a quotation body and 'V @ c@'; and ptr-field on a raw base publishes a fresh TVK-ANY pointee, so 'NEWTYPE thing 0  variable V  : FORGE ( n -- thing ) V ! V 0 ptr-field @ @ ;' certifies and forges a nominal value past the seal test/raw-storage-load-seal-test.f pins. TASK:+USER rows, locals, TYPED-VARIABLE and PTR-VARIABLE cells are already refused. The engine's own sources lean on the pun: src/habu/aot-lib.f 'DSCAN @ @' and src/habu/aot-closure.f DATA-CELL@ 'DCELL ! DCELL @ @'. docs/effects.md:330 claims a pointer cannot be laundered into a cell pointer and then cell-loaded; both routes contradict it. Pointer arithmetic from NULL-PTR or data-base ('NULL-PTR + @') reaches any address too and is a different rule: habu-bound-ptr-arithmetic-8bf6b54a (child of the campaign). Campaign: habu-campaign-c2-mem-c3d7662b. Acceptance: the value of a raw storage cell is never an address: RAW-OK? refuses T-PTR (a TVK-RAW variable admits scalars, roles and atoms and never binds to a ptr) and ptr-field refuses a TVK-RAW base, each with a named diagnostic (an MD- code with prose in src/core/render.f beside MD-EXEC-OPAQUE); rejected-program regressions for PEEK and POKE through variable, create, constant, here and data-base, for 'V @ c@' and for the ptr-field FORGE, with a TASK:+USER control that stays accepted, in test/raw-storage-load-seal-test.f and test/compiler; a cell that holds an address is declared (PTR-VARIABLE, PERSISTED-PTR-VARIABLE, TYPED-VARIABLE NAME ptr t, TYPED-BUFFER) and 'create BUF n allot  BUF k type' keeps certifying; the engine's pun sites move to typed pointer cells (DSCAN, BLOB-SRC, BLOB-END are fed by here) and DATA-CELL@ reads through data-base plus the offset, with no new TRUST row and no new primitive unless the checkpoint shows a typed form cannot express a site; every site in src lib tools test the rule rejects is converted first, each conversion certifying before and after (census by the checker, not by grep: the scout's grep counted 236 sites and misses indirect pointer stores, cross-file names and indexed raw tables); consumers (kiba 7 measured sites, loom 3, plus the unmeasured indirect stores) get the diagnostic text and the migration forms before the rule integrates; docs/effects.md states the rule and docs/forth.md:872 links it. Files: src/core/checker.f (RAW-OK?, the ptr-field row, the MD- code), src/core/render.f, src/habu/aot-lib.f, src/habu/aot-closure.f, the converted sites, test/raw-storage-load-seal-test.f, test/compiler/, docs/effects.md, docs/forth.md. Verify: the regressions; test/raw-storage-load-seal-test.f, test/pointer-storage-test.f, test/typed-storage-test.f, test/internal-word-gate.f, test/compiler/aot-data-cell-refusals.f; byte fixpoint; tools/bootstrap.sh check; test/run.f; the five consumer suites on the result. State 2026-09-17: the rule is implemented and held as commit 1204d189 in .jj-ws/hazel-raw-ptr (RAW-OK? refuses T-PTR; RAW-FIELD-BASE?/RAW-FIELD-TOK? refuse ptr-field on a TVK-RAW base; MD-RAW-PTR 26 and MD-RAW-FIELD 27 under E-RAW-CELL-PTR, class declare_pointer_cell; fixtures test/compiler/raw-cell-pointer-refusals.f and test/raw-storage-load-seal-test.f); a read-only engine carrying it is ~/.cache/hazel/engines/raw-rule-gen1. The census is ~/.cache/hazel/scout-pun/census.md: 710 sites (habu 483, maki 200, Tender 11, loom 10, kiba 6, radar 0), 404 of them accessors generated by definers; its method (a census engine loading every file, including the consumers' test files with the real HOME) executed kiba's test main against Joel's credential store and is NOT to be rerun; consumer counts come from that one run, and the consumer owners verify their own trees on raw-rule-gen1. No engine with the rule builds until every habu site converts (the native build installs the tree's checker before compiling the rest), so the conversions land first, each on the current engine through the normal chain: habu-fix-the-definers-cc06e107 (hazel-raw-definers) and habu-convert-the-raw-8052992f (lib and tools, hazel-raw-libtools) now; then habu-convert-the-raw-7b5ce4f3 (src/core, src/os, src/habu, including checker.f's ~52 unmeasured sites), habu-convert-the-raw-d12eec95 (src/compiler and src/arch, after habu-parameterise-the-alloc-7efbe7a1), habu-convert-the-raw-5af7de05 (test), habu-convert-the-hand-9b6f0d97 (the 105 other and 78 AMBIGUOUS rows); habu-migrate-the-consumers-5fb770f1 (kiba and Tender notified 2026-09-17; maki and loom owners pending). Then the rule lands last with docs/effects.md:330 corrected, on its own chain, followed by a whitebox re-census with tools/check.f --all-errors inside the tree only. data-base @ @ stays open under habu-bound-ptr-arithmetic-8bf6b54a. Diagnostic-machinery seam: habu-latch-value-pos-373cad6b. Depends: the children above. Ownership: checker. Claim: agent=hazel (the rule commit waits in .jj-ws/hazel-raw-ptr).
