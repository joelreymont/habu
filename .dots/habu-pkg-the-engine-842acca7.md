---
title: Package the engine primitive emitters
status: open
priority: 3
issue-type: task
created-at: "2026-08-04T11:09:28.725693+02:00"
---

Full context: src/habu/habu1.f is the other half of the engine trunk - the arm64 assembler vocabulary, the register aliases, the label helpers and the primitive emitters (EMIT-FIND, EMIT-HIDX, EMIT-FLUSH, the B* primitive bodies) that src/habu/habu2.f's passes call bare while the engine is being built. It already opens five real packages - ENGINE-BUILD, ENGINE-HELPER, GUARD, PROT-GUARD and OWNER-WID-EMIT - and everything else in it is global. On 2026-08-04 the file was added as the fourth ENGINE-TRUNK row of tools/package-diff-lint-core.f, admitted for exactly one shape: a change to the BODY of a global word the file already defines. The measurement that earned it: adding a trailing comment to the body of the existing global EMIT-FLUSH, defining no new word and changing no behaviour, reported E-PACKAGE-OWNERSHIP src/habu/habu1.f:2714:3, so as configured the gate rejected every possible change to the primitive emitters, including the dictionary-lookup repair (dot habu-compile-shaped-cost-4e74a181) that found it. Task: extend the file's existing seams over its remaining global surface the same way dot habu-cont-habu2-emitter-493363e7 is doing for habu2.f - membership with unchanged text first, so the changed-caller cascade terminates - and then delete the habu1.f row from ENGINE-TRUNK-AT, drop ENGINE-TRUNK-N back, and delete the four habu1 fixtures in tools/package-diff-lint-test.f. Note the assembler vocabulary is the hard part: habu2.f resolves LDR, CBZ, MOVZHW and the rest bare from this file, so the seam that owns them either stays global or lands behind a using, which is the same blocker layout.f has (dot habu-add-using-to-d815f0ab). Acceptance: package-diff-lint 0 findings with the row removed; engine build slice green; fixpoint census unchanged; bin/hb byte-identical or the image delta explained. Depends: tools/package-diff-lint-core.f ENGINE-TRUNK-AT; src/habu/habu1.f. Ownership: engine primitive emitter packaging. Claim: unassigned.
