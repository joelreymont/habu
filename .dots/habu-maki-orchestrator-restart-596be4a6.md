---
title: "Maki orchestrator restart: queue + protocol"
status: open
priority: 2
issue-type: task
created-at: "2026-07-10T15:49:29.124317+02:00"
---

RESTART CONTEXT for the maki/Model-CAD orchestrator. Start every session in ~/Work/habu-fable (a jj workspace of ~/Work/habu; the branch is the fable bookmark; bin/hb here must match the tree — if stale, rm bin/hb then cp a fresh fixpoint or rebuild via the docs/bootstrap.md FULL prelude invocation, never bare --load tools/build-fixpoint.f). TWO-AGENT SPLIT: a separate type-system agent owns src/core, src/habu, test/type-*, bootstrap/ per the handoff contract in .dots/habu-epic-type-system-b88c9ecc/ (TFAM tail 11/13/16, capability campaign derive-eq + wide-store + LAYOUT-BUFFER, switchover waves); it advances fable directly. I own maki/* + docs/paper + tools lints. MERGE PROTOCOL: workers implement in .jj-ws/fable-<name> workspaces off fable and NEVER move bookmarks or push; I review hunk-by-hunk, rebase the stack onto the live fable tip, run the owning gates on the exact tree (maki/test.f 77 suites + focused suites + namespace/error-code/host/filemap/dot-dep lints + typed-local-diff-lint; full test/run.f for anything wider; lib/process-test flakes - retry once), freshness-check the bookmark, fast-forward, push, then forget+rm the workspace in the same merge window. QUEUE (updated 2026-07-10, second revision): (1) model-ir FAMILY-TYPED migration - ACTIVE, executable NOW. Master's corrected plan in .dots/habu-cad-adt-swap-7bf0bb1f.md (§ "MODEL-IR PLAN CORRECTION - destruction review", commit kuuvmtty) declares the old G-TAG "enum behind stable n accessors" design INVALID for Model IR (a caller passing layout where dtype is expected still certifies - proven by probe). A G-TAG attempt was implemented against the stale fable copy of the plan, gated green, pushed, then REVERTED via append-merge (b8936c4e). The corrected plan's BLOCKER paragraph OVERSTATES: enum-typed locals rejecting does NOT block the family-typed migration. Probe-proven 2026-07-10 on the current fixpoint: family-returning accessors, MATCH renders/predicates, dup-based multi-use, and families flowing as word args through factored helpers ALL certify; only `{: x:fam :}` binding rejects, and stack-discipline style (mandated by docs/forth.md anyway) never needs it. b06b6707 is a later code-quality improvement, not a gate. Execute per the corrected plan's commit sequence: descriptor trio (dtype/layout/align declarations, Model IR storage/API, every reader; ~10 files) as one green stack, then opkind (21 reader files) as the next. Re-apply the fusion-plan reason `layout`->`layout-conflict` variant rename (collides with the layout family; rename is proven and documented in the ATTEMPT LOG). LESSON: fable lags master and can carry a stale plan - diff the dot vs master and rebase BEFORE implementing; and never accept a BLOCKER claim without a feasibility probe. GATING NOTE: master moves fast (Wave A/B option/result switchovers); rebuild bin/hb from the rebased tree (docs/bootstrap.md refresh) before gating any maki change; script loads need < /dev/null. (2) ONNX residuals in .dots/habu-maki-onnx-graph-51adfd39.md: Slice/Gather import (int64 executor-index mismatch), int64_data field 7, Reshape -1/0 dims; onnxruntime real-model golden is user-gated. (3) SKEY typed schedule key + evidence rows: BLOCKED on the other agent's derive-eq and LAYOUT-BUFFER steps - check .dots/habu-checker-capability-derive-23788e95.md and -typed-a480c423.md S-status before dispatching. (4) Zed device backlog when the Orin returns ~2026-07-14: .dots/habu-infra-zed-unreachable-c3d8c991.md checklist + pending-zed queue (broadcast/gather/Rx1/fold-staged goldens, PTX-optimizer flip, MMA rungs, cad-6-tune, roofline, latency vs torch.compile, packaged device-stack reverify). (5) Paper device rows + TFAM ablation row once adoption lands. macOS gotcha: cp over an existing bin/hb SIGKILLs at exec - always rm first.
