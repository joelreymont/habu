---
title: "Maki orchestrator restart: queue + protocol"
status: open
priority: 2
issue-type: task
created-at: "2026-07-10T15:49:29.124317+02:00"
---

RESTART CONTEXT for the maki/Model-CAD orchestrator. Start every session in ~/Work/habu-fable (a jj workspace of ~/Work/habu; the branch is the fable bookmark; bin/hb here must match the tree — if stale, rm bin/hb then cp a fresh fixpoint or rebuild via the docs/bootstrap.md FULL prelude invocation, never bare --load tools/build-fixpoint.f). TWO-AGENT SPLIT: a separate type-system agent owns src/core, src/habu, test/type-*, bootstrap/ per the handoff contract in .dots/habu-epic-type-system-b88c9ecc/ (TFAM tail 11/13/16, capability campaign derive-eq + wide-store + LAYOUT-BUFFER, switchover waves); it advances fable directly. I own maki/* + docs/paper + tools lints. MERGE PROTOCOL: workers implement in .jj-ws/fable-<name> workspaces off fable and NEVER move bookmarks or push; I review hunk-by-hunk, rebase the stack onto the live fable tip, run the owning gates on the exact tree (maki/test.f 77 suites + focused suites + namespace/error-code/host/filemap/dot-dep lints + typed-local-diff-lint; full test/run.f for anything wider; lib/process-test flakes - retry once), freshness-check the bookmark, fast-forward, push, then forget+rm the workspace in the same merge window. QUEUE (2026-07-10): (1) model-ir enum commits 2-3 - full designs + reader census + G-TAG-precedent verdicts are in .dots/habu-cad-adt-swap-7bf0bb1f.md (commit 2 = dtype/layout/align descriptor trio, commit 3 = 31-variant opkind; stable-n public signatures, one parse + one render boundary each, swapped-family negatives; enum-typed locals reject so stores are stack-only; script loads need < /dev/null). (2) ONNX residuals in .dots/habu-maki-onnx-graph-51adfd39.md: Slice/Gather import (int64 executor-index mismatch), int64_data field 7, Reshape -1/0 dims; onnxruntime real-model golden is user-gated. (3) SKEY typed schedule key + evidence rows: BLOCKED on the other agent's derive-eq and LAYOUT-BUFFER steps - check .dots/habu-checker-capability-derive-23788e95.md and -typed-a480c423.md S-status before dispatching. (4) Zed device backlog when the Orin returns ~2026-07-14: .dots/habu-infra-zed-unreachable-c3d8c991.md checklist + pending-zed queue (broadcast/gather/Rx1/fold-staged goldens, PTX-optimizer flip, MMA rungs, cad-6-tune, roofline, latency vs torch.compile, packaged device-stack reverify). (5) Paper device rows + TFAM ablation row once adoption lands. macOS gotcha: cp over an existing bin/hb SIGKILLs at exec - always rm first.
