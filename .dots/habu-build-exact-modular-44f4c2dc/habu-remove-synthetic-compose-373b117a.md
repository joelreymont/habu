---
title: Remove synthetic compose separators
status: open
priority: 1
issue-type: task
created-at: "2026-07-15T23:48:26.539815+02:00"
---

Full context: tools/source-compose.f LOAD-ACTION inserts generated LFs around include/require and maps them onto the loader token, so diagnostics can invent user coordinates. Prove loader blanking already preserves token boundaries and remove the separator bytes; if any boundary truly needs generated content, encode an explicit synthetic row whose origin lookup rejects instead of fabricating coordinates. Treat same-length blanked loader bytes as real positional transforms only where every output byte has a corresponding original byte. Acceptance: exact direct-vs-composed behavior for adjacent tokens, start/end loaders, CRLF/no-final-LF, empty include, repeated include/require; diagnostics on any generated byte fail closed; source/map digests and hb-build tests updated. Files: tools/source-compose.f/test, source-map format/tests only if synthetic rows remain.
