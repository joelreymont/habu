---
title: "Build: exact modular AOT source"
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-15T14:23:10.016110+02:00\""
blocks:
  - habu-fs-checked-no-7b20610f
---

Full context: hb-build hashes a dependency closure but gives the native AOT maker only the entry file, so modular entries fail when loader words are absent. Flat concatenation is forbidden: no separator can preserve real child-frame EOF semantics for comments, parsing words, packages, immediates, or failed definitions. Extend the existing evaluator input frames with one authenticated frozen-source provider. The build transaction freezes the entry first; the real Habu loader/interpreter requests dependencies in execution order; the provider reads each path at most once with the checked no-follow reader, stores exact path bytes and content bytes in one immutable bundle, and serves every repeated include/require from that table. Checker, maker, diagnostics, and cache consume that same verified in-memory bundle; none reopens a source path. Preserve include multiplicity, require/provided registry transitions, package/compiler state, child EOF, source identity, line, column, byte offset, and include chain. Reject missing, extra, reordered, digest-mismatched, unused, cyclic, NUL-path, capacity, and post-freeze mutation cases fail-closed. The canonical cache key covers bundle format/provider version, ordered frame/open events, exact path/content identities, target/compiler/checker/lint identities, and the executable artifact digest; a hit verifies its evidence and executable bytes, never merely mode bits. Compile modular top-level, transitive, duplicate-require, repeated-include, provided-before-require, shared-package-private, arbitrary parsing immediates, comments/strings/CRLF/no-final-LF/empty/NUL, boundary capacity, dependency edit, bad dependency, cycle, and input-swap fixtures through real hb-build. Prove direct framed loading and built executable behavior match, cold/hit/miss decisions are truthful, single-file AOT remains unchanged, and the real bulk scanner builds. Remove the synthetic composer and source-map remap path once the frame provider owns diagnostics. Add native/recovery/AOT/fixpoint/snapshot parity plus docs/manifests/FILEMAP. This blocks habu-tools-bulk-diff-f36d0508 and habu-tools-frame-diff-e98f8a6a. No scanner or diff-parser edits. Claim: agent=modular-aot workspace=.jj-ws/habu-build-exact-modular-44f4c2dc.
