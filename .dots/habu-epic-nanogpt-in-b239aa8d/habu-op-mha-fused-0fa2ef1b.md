---
title: "OP-MHA: fused batched attention MODEL: node"
status: closed
priority: 1
issue-type: task
created-at: "2026-07-21T06:37:19.914101+02:00"
closed-at: "2026-07-23T00:38:54.366037+02:00"
close-reason: Landed as b9c97b130045 on verified master 1ef723720909; fresh destruction READY; focused MHA, full Maki, full lint-libs, exact diff lints, host/filemap/dot, and native port gate green.
---

Problem: the model parser cannot place the existing batched MHA reference in a MODEL: graph because its rank-four SPEC equations exceed the composable equation surface. Exact contract: add author token MHA with operand order X, Wqkv, bqkv, Wo, bo and result Y. Capture one host-only fused op with forward arity five. Its attribute stores T, H, and the causal flag; the checked constructor rejects negative or field-overflow T/H before encoding and never masks an invalid value into a valid one. One operand-geometry authority validates X=(B*T,C), Wqkv=(C,3C), bqkv=(1,3C), Wo=(C,C), and bo=(1,C) during initial MODEL: capture and BIND-SHAPES rebind, reporting the owning context error. The current host executor additionally rejects any B/T/C/H/hd or causal mode not supported by the fixed maki/mha.f oracle; the later GB10 attention dot owns dynamic device geometry. Backward emits one synthesized arity-six MHA-BWD node carrying the five forward inputs followed by dY, recomputes the private forward tape from that node's own inputs, and slices one combined dX/dWqkv/dbqkv/dWo/dbo buffer. Forward and backward both fail closed through their real device-lowering dispatch paths. MHA is not precision-tagged as a GEMM until its internal device policy exists. Files: maki/op-kind.f, op-registry.f, adjoint.f, backward.f, cad.f, executor.f, mha-op.f, mha-op-test.f, move-facts.f, plan-vocab.f, prec-attr.f, lower/ew.f, lower/mm.f, lower/move.f, lower/red.f, test.f, and test-core.f; maki/mha.f is excluded. Acceptance: production MODEL: capture rejects malformed known operand shapes; negative and one-over-field T/H reject before capture without aliasing; host forward equals MHA-FWD; all five input gradients pass; two stacked MHA nodes prove tape independence; run-twice is deterministic; both MHA and synthesized MHA-BWD reject device lowering; MHA:FP16 rejects; every exhaustive opkind consumer is enrolled. Mutations removing capture geometry, canonical attribute checks, per-node recomputation, rebind geometry, or either lowering rejection fail. No runtime-only shape guard, masked attribute fallback, compatibility alias, PRODUCT/SUMTYPE definition, or modification of mha.f is allowed. Depends: none. Ownership: the Maki fused MHA graph bridge only. Claim: agent=mha_op_fix workspace=.jj-ws/habu-mha-op.
