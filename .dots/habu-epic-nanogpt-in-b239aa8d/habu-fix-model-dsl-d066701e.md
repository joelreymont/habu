---
title: "Fix model DSL: multi-operand dataflow (Q@K^T) via SPEC:"
status: closed
priority: 1
issue-type: task
created-at: "2026-07-18T16:40:36.105397+02:00"
---

Inventory finding (docs/nanogpt-inventory.md): MODEL: is single-running-value and 2-D-only — provably cannot express an internal Q@K^T (adam-train.f:15); residual skips only work via >V/named-ref workaround. THE FIX IS SPEC: (the load-bearing surface): multi-operand contractions are native to the spec grammar (A[ix[m] k] B[n k] form), so model compositions (MHA: habu-multi-head-self-a1e0692f, block: habu-gpt-2-block-a9039501) are AUTHORED as SPEC: lines, not MODEL: extensions — do NOT grow MODEL:'s running-value design. Scope here: (1) SPEC: grammar must cover the inventory's requirement list (contractions incl. transposed operands, gather indexing, reductions, broadcasts — docs/nanogpt-inventory.md SPEC-grammar section); (2) rank: (B,T,C) batch dimension enters via extent-typed tensors (DECIDED in docs/batch-sequence-design.md: batch as an extent role over 2D B*T-row memory, B outermost — see habu-extent-roles-b-df9d232f and habu-extent-role-product-8e364885); (3) MODEL: stays for what it already does until SPEC: parity, then compositions migrate; deprecation decision AFTER nanoGPT trains. Acceptance: attention fwd expressed as SPEC: lines type-checks and matches maki/attention.f golden.

Claim: agent=specdsl-opus workspace=.jj-ws/habu-fix-model-dsl-d066701e

CLOSED 2026-07-19: acceptance met with NO grammar change needed. The worker proved the shipped SPEC: grammar already expresses both attention contractions, including the transposed operand - the transpose is just which slot the shared contraction index takes in each factor (S[am an] = Q[am ak] K[an ak] * +SUM ak IS Q.K^T). Landed maki/spec-attention-test.f: golden numeric parity with maki/attention.f ATTN-FWD, dataflow-record and shape assertions, named-throw negatives, and a checker reject of the extent-swapped operand. Scope item disposition: transposed operands PROVEN COVERED; gather indexing and +SUM reductions were already proven by maki/spec-test.f; broadcasts are NOT expressible in the shipped grammar (contraction-only: OUT[free] = factors * +SUM ct) - follow-up dot habu-spec-broadcast-forms-ad851424 minted for the row/scalar/elementwise broadcast classes; the (B,T,C) batch capability remains owned by habu-extent-role-product-8e364885 (checked: its BTC-7 contract covers the product/factorization need - no new dot). Composition decision (orchestrator): model compositions are plain checked colon words composing SPEC:-generated words with named row ops (the escape hatch), exactly as the test's SPAT-FWD does; no multi-statement SPEC block surface unless MHA authoring proves a concrete need - recorded in habu-multi-head-self-a1e0692f.
