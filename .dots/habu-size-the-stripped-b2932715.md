---
title: Size the stripped-image closure tables from the program
status: active
priority: 2
issue-type: task
created-at: "2026-09-21T13:34:06.652632+03:00"
---

Problem: src/habu/aot-closure.f:281 '1024 constant MAX-CLO' sizes the five closure tables (CLO, CLO-LEN, CLO-REC there; NEWOFF, BLEN in aot-lib.f:35) and ADD-CLO fails closed at it, so every Tender entry point is refused: 'aot: closure exceeds MAX-CLO reachable_count=1024 max_closure=1024 root_word=MAIN last_added_word=RES-LIVE!' (aspen 2026-09-21; reproducer ~/.cache/tender/habu-gaps/stripped-closure-limit/gen.sh: chain-1100.f refused at CW76, chain-900.f builds and runs). Tender has 3,513 colon words under server/ and 4,950 under src/ before any lib, so a larger constant only moves the wall. Acceptance: the five tables are allocated when the closure walk starts, sized from the program being linked (an upper bound the linker already knows: the records the image names plus the stripped span table, or a doubling growth on the checker.f ARENA-BYTES-GROW/TV-GROW idiom), through checked storage with no TRUST seam; ADD-CLO keeps failing closed at the table's capacity as an invariant, CLO-LIMIT! keeps lowering the limit for the negative fixture (test/gate-aot-negative-lib.f CLOSURE-LIMIT, E-AOT-CLOSURE-LIMIT with reachable_count/max_closure) and refuses a limit above the capacity; the diagnostic and its suggestion no longer name a constant; chain-1100.f, chain-2000.f and chain-5000.f from gen.sh build stripped and run (link wall time reported for each; IN-CLO? is a linear scan per member - replace it only if the measurement shows it dominating); HBT-SIZE-AOT still accounts exactly; three generations gen2 == gen3 if the files prove baked; tools/hb-build-test.f, test/gate-aot-image.f, the stripped suites, test/run.f. Files: src/habu/aot-closure.f, src/habu/aot-lib.f, test/gate-aot-negative-lib.f, tools/hb-build-test.f (a fixture over the capacity refusal), docs/forth.md or docs/bootstrap.md where MAX-CLO is documented (rg first). Verify: the fixtures above. Depends: none. Ownership: hazel (src/habu). Claim: agent=hazel workspace=.jj-ws/hazel-closure-tables.
