---
title: REND-SIG blanked by certify epilogue breaks round-trip
status: closed
priority: 1
issue-type: task
closed-at: "2026-07-06T00:00:00+02:00"
close-reason: CERT-REPOINT-ROWS re-points BROW/DCUR (and RBROW/RCUR) at USIG-ADD's re-parsed SGIN/SGOUT after the certified-path CHECKER-USIG-CERT-ADD; probe renders i64 -- i64, engine-suite COK-REND-SIG fixture added, serial prop-test reports round-trip runs with 0 inconsistencies.
created-at: "2026-07-06T14:43:07.474136+02:00"
---

Route via item-12 (fix is in src/core/checker.f). CONTRACT: render.f:195-197 REND-SIG renders 'the just-checked word's effect' from BROW/DCUR; test/prop-test-core.f ROUNDTRIP (and any post-CHECK! caller) depends on it. BUG: CHECK's success epilogue records the verified declared effect (checker.f:5761 'SGIN @ BROW ! SGOUT @ DCUR !') but then persists the certified usig via CHECKER-USIG-CERT-ADD (checker.f:5782) -> USIG-ADD (checker.f:3337) -> NEW (checker.f:3338), and NEW re-initializes 'FRESH MK-ROW dup BROW ! DCUR !' (checker.f:1137), wiping the recorded rows. Every certified definition with a stored name exits CHECK! with BROW=DCUR=one fresh empty row, so REND-SIG renders bare '--'. FIXTURE (minimal, current head 57f0ef20): [ s" G ( i64 -- i64 ) 1- " CHECK! . REND-SIG type ] prints -1 then '--' (expect 'i64 -- i64'); node evidence: after CHECK! BROW=DCUR=3 (NEW's fresh pair) while SGIN=4/SGOUT=12 hold the re-parsed sig from USIG-ADD's PARSE-SIG-RAW. CLASSIFICATION: render-state lifetime bug in the certify epilogue (checker semantics side), NOT a re-parse or certify-verdict miss; prop-test's 3 'metamorphic round-trip inconsistency' reports (seed 1, iters 0/2/4) are three manifestations of this one root cause - every ROUNDTRIP re-declares R with sig '( -- )' and the checker correctly rejects. BORN BROKEN: the pre-usig-store code (E-PARSE-ADD at ed478c4d, checker.f:2725-2730 then) already called NEW, so NRT has been 0 since ROUNDTRIP's introduction; advisory (non-fatal) inconsistency counting plus SWEEP-QUIET/muted-stderr shards masked it. SUGGESTED FIX (item-12's call): after USIG-ADD's PARSE-SIG-RAW succeeds, re-point BROW/DCUR at the re-parsed rows (SGIN/SGOUT) restoring the contract; or snapshot the rendered sig into RSBUF at the checker.f:5761 record point so REND-SIG stops depending on post-epilogue live rows. REGRESSION: (1) fixture above renders 'i64 -- i64'; (2) serial prop run 'bin/hb 1 5 < test/prop-test.f' reports NRT>0 and 0 inconsistencies. BLOCKS: the prop-test fail-closed harness commit (metamorphic inconsistencies fatal) is committed on maki-type-families but must not merge until this lands - with the contract broken every shard dies red.
