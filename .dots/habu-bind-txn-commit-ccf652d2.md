---
title: Bind transaction COMMIT-MAPPED
status: active
priority: 2
issue-type: task
created-at: "2026-07-26T13:44:33.310377+02:00"
---

S6b3 of the frozen intake redesign (rev-4 Correction 3). Depends: S6b1 landed (master 5756381d13f3: GPT2TX PREPARE, prep capability, WSTORE:TABLE-DISPOSE). Interface: COMMIT-MAPPED ( GPT2TX:prep -- gpt2-model ) in package GPT2TX, maki/infer/gpt2-bind.f. Uses the prep's source-domain table directly; SAFET:DETACH-MAPPING is the atomic terminal consumption of the census's mapping authority, and NOTHING FALLIBLE may execute after it (destruction-review forward risk, the FIX-2 precedent: order every fallible step before the last recoverable point; prove the ordering by a fixture that injects a throw at each step boundary and shows either full rejection cleanup before detach or completed store after). store = mapped(mapping-owner, table) via the landed WSTORE mapped constructor; the model value = (store, nlayer, cfgkey) - define the gpt2-model type here (STRUCTURE with private proof per the MDLCFG/GPT2BIND pattern, sole constructor package-private, minted only by the commit leaves). FOREIGN-MODEL REJECTION IS THIS LEAF'S OBLIGATION (moved from PREPARE by the landed contract correction): commit compares the prep's captured cfgkey against the consuming configuration's CFGKEY= and refuses a foreign prep with a named code BEFORE detach; the twin fixture from the PREPARE suite is the input (two preps, different cfgkeys, same census shape - the wrong one must refuse). Plan values (count, prefix sum) read ONLY from the prep block readers (PREP-COUNT/PREP-SUM), never process statics (reviewer finding 3). GPT2TX:LIVE decrements on successful commit (the prep is consumed) and the mapping/table ownership transfers into the store - leak legs prove all counters balanced after commit AND after a refused foreign commit (prep still live, disposable by ABORT). Red-first fixtures through the real path; mutation kills: skip the cfgkey compare (foreign leg reds), reorder detach before the compare (the throw-injection fixture reds), leak the prep on refusal (LIVE leg reds). Real-artifact leg: commit the 160-row prep mapped, probe one weight span byte-identical to the file bytes at its offset, dispose the store. Acceptance: focused suite + maki/test.f + both diff lints + refine-lint (any new TRUSTED row seeded) + error-code-lint. STOP on any checker miss or unplanned interface. Claim: agent=s6b3 workspace=.jj-ws/habu-s6b3-mapped
