---
title: Bind transaction COMMIT-MAPPED
status: active
priority: 2
issue-type: task
created-at: "2026-07-26T13:44:33.310377+02:00"
---

S6b3 of the frozen intake redesign (rev-4 Correction 3). Depends: S6b1 landed (master 5756381d13f3: GPT2TX PREPARE, prep capability, WSTORE:TABLE-DISPOSE). Interface: COMMIT-MAPPED ( GPT2TX:prep -- gpt2-model ) in package GPT2TX, maki/infer/gpt2-bind.f. Uses the prep's source-domain table directly; SAFET:DETACH-MAPPING is the atomic terminal consumption of the census's mapping authority, and NOTHING FALLIBLE may execute after it (destruction-review forward risk, the FIX-2 precedent: order every fallible step before the last recoverable point; prove the ordering by a fixture that injects a throw at each step boundary and shows either full rejection cleanup before detach or completed store after). store = mapped(mapping-owner, table) via the landed WSTORE mapped constructor; the model value = (store, nlayer, cfgkey) - define the gpt2-model type here (STRUCTURE with private proof per the MDLCFG/GPT2TENSOR pattern, sole constructor package-private, minted only by the commit leaves). FOREIGN-MODEL REJECTION IS THIS LEAF'S OBLIGATION (moved from PREPARE by the landed contract correction): commit compares the prep's captured cfgkey against the consuming configuration's CFGKEY= and refuses a foreign prep with a named code BEFORE detach; the twin fixture from the PREPARE suite is the input (two preps, different cfgkeys, same census shape - the wrong one must refuse). Plan values (count, prefix sum) read ONLY from the prep block readers (PREP-COUNT/PREP-SUM), never process statics (reviewer finding 3). GPT2TX:LIVE decrements on successful commit (the prep is consumed) and the mapping/table ownership transfers into the store - leak legs prove all counters balanced after commit AND after a refused foreign commit (prep still live, disposable by ABORT). Red-first fixtures through the real path; mutation kills: skip the cfgkey compare (foreign leg reds), reorder detach before the compare (the throw-injection fixture reds), leak the prep on refusal (LIVE leg reds). Real-artifact leg: commit the 160-row prep mapped, probe one weight span byte-identical to the file bytes at its offset, dispose the store. Acceptance: focused suite + maki/test.f + both diff lints + refine-lint (any new TRUSTED row seeded) + error-code-lint. STOP on any checker miss or unplanned interface. Claim: agent=s6b3 workspace=.jj-ws/habu-s6b3-mapped

Redesign 2026-07-26 after the S6b3 checkpoint stop (supersedes the interface lines above where they conflict). Blocked by habu-add-wstore-resident-2ada0262 (first commit of this same lane). Corrected interface: gpt2-model is a fully checked public STRUCTURE - FIELD res WSTORE:resident, FIELD nl n, FIELD key MDLCFG:cfgkey, FIELD tok mdl-proof (private arity-0 TYPEFAMILY, TRUSTED mint, refine-seeded, MDLCFG pattern) - zero new erasures in GPT2TX; generated MAKE/UNMAKE are package-internal use only, the known sealed-destructure caveat applies and is named in the header. COMMIT-MAPPED ( GPT2TX:prep MDLCFG:mcfg -- GPT2TX:commit-result ) - the consuming configuration is an explicit argument (self-comparison is vacuous, measured); public payload ENUM commit-result 0 = committed(FIELD m gpt2-model) | refused(FIELD p GPT2TX:prep, FIELD code n) so a foreign refusal returns the prep live and ABORTable instead of stranding it by throw (the safetensors.f:707 hazard). E-GX-FOREIGN -5670 (already implemented in the checkpoint hunks). MODEL-DISPOSE ( gpt2-model -- result<n,n> ) in GPT2TX (unwraps in-package, delegates to WSTORE:RESIDENT-DISPOSE) - the model owner exit the original contract omitted. All other obligations stand: nothing fallible after DETACH-MAPPING with the throw-injection fixture, plan values from prep readers only, counters balanced after success AND refusal, real-artifact byte-identical probe, mutation kills.

Redesign 2 (2026-07-26, after the commit-2 checkpoint measured that gpt2-model cannot be an ENUM payload either - same transitive-linear guard, throw 7109 at the commit-result declaration): adopt the checked-prep shape, which is strictly stronger. Public DEFLINEAR GPT2TX:checked-prep; CHECK ( GPT2TX:prep MDLCFG:mcfg -- GPT2TX:check-result ) with payload ENUM check-result 0 = matched(FIELD c checked-prep) | foreign(FIELD p prep, FIELD code n) - the sole place the captured cfgkey is compared (E-GX-FOREIGN), refusal returns the prep live and ABORTable; ABORT-CHECKED ( checked-prep -- ) for a holder that declines to commit. COMMIT-MAPPED ( GPT2TX:checked-prep -- gpt2-model ) is then TOTAL: no union ever carries the model, the identity compare is a type-level precondition (the commit is unreachable without a value only CHECK mints, and the checked prep IS the prep after comparison so no witness mismatch is possible), and every fallible step precedes DETACH-MAPPING by construction (HOLD is total per the landed resident holder). gpt2-model STRUCTURE as in redesign 1 (accepted by measurement); MODEL-DISPOSE unchanged. The throw-injection ordering fixture and all counter/probe obligations stand, recast onto CHECK (refusal legs) and COMMIT-MAPPED (totality: no catch anywhere in it, asserted by review).
