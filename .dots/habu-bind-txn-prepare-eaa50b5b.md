---
title: Bind transaction PREPARE with prepared-bind capability
status: active
priority: 2
issue-type: task
created-at: "2026-07-26T10:41:27.683140+02:00"
blocks:
  - habu-add-wstore-public-db6c70fe
---

S6b1 of the frozen intake redesign (rev-4 message 20260724-191041.846-claude-7d24 Correction 3, plus rev-5 amendments in 20260726-081203.898-claude-2bfe). Dependencies: the tensor vocabulary introduced at 1746455e467b, whose current package is GPT2TENSOR; WSTORE (S3d), SAFET seam (S4: MAP-OFFSET? at maki/infer/safetensors.f:862, DETACH-MAPPING at :720, RETAIN/RELEASE). NOT dependent on MODELPROV. Owned result: new file maki/infer/gpt2-bind.f opening new package GPT2TX, plus maki/infer/gpt2-bind-test.f, with an error-code Owns block (freeze: -5660..-5674; verify numerically free through error-code-lint before commit), suite registration in maki/test.f plus exactly one slice. Interface: PREPARE ( SAFET:tensor-census MDLCFG:mcfg -- prep-result ) with payload ENUM prep-result = prepared(prep) | rejected(census, code); prep is an opaque DEFLINEAR owning the RETAINED census, the sealed source-domain slot table of (mapping-offset, extent) rows, the captured cfgkey, and nlayer; public ABORT ( prep -- ) totally disposes a prep (releases table and census) so a binder can decline to commit and tests can consume the linear value - the same disposal the later commit leaves reuse on resource-exhaustion throws. ALL validation lives in PREPARE and every rejection returns the census untouched and usable: census count equals GPT2TENSOR:COUNT; for each tensor id, its name is rendered through GPT2TENSOR:COPY-NAME? (rev-5: the span-returning name API no longer exists) and found in the census, dtype F32 (blanket rule, no mask exemption - settled 2026-07-26 with measured evidence: the pinned checkpoint 248dfc3911869ec493c76e65bf2fcf7f615828b0254c12b473182f0f81d3a707 stores every tensor F32 including every h.N.attn.bias), rank and dims exactly GPT2TENSOR:SHAPE, mapping offset via MAP-OFFSET?, GPT2TENSOR:SLOT with its result additionally checked against GPT2TENSOR:COUNT before any table indexing (rev-5); GPT2TENSOR:E-CONFIG surfaces from GPT2TENSOR:SLOT itself; checked extent arithmetic INCLUDING the packed-arena prefix-sum overflow pre-clear so both later commits are arithmetically pre-cleared; recorded ordering fact: ALL (slot, offset, extent) rows are collected and validated BEFORE the table is created, so the populate-and-SEAL step is infallible by construction. Package-private projections only; no public accessor exposes the census, a span, or the table. Tests, red-first through the real production entry point: hermetic synthetic-checkpoint fixtures (the safetensors-test SYNTH-PATH pattern) for a tiny nlayer geometry - full happy path yields prepared with every row populated (prove row count and one probed row through a package-internal test seam, not a public accessor); reject legs for census-count mismatch, missing name, wrong dtype, wrong rank, wrong shape, and a foreign mcfg (same nlayer twin - must reject on identity, not on any bound), each with its named GPT2TX code or the surfaced GPT2TENSOR code, each proving the returned census is still usable by running a subsequent successful PREPARE on it; ABORT leg proves total disposal (WSTORE-style leak counter or SAFET release evidence); checker negatives - prep cannot be forged from a raw cell, prep-result payloads cannot cross roles, ambient PREPARE without a census rejects. Real-artifact leg: if gpt2-model/model.safetensors is present (link from /home/joel/Work/m0bench-ext/models/gpt2/model.safetensors; do not commit the artifact), PREPARE over the real census with a directly-constructed GPT-2 124M mcfg must yield prepared with 160 rows; leg self-skips when absent, but the lane must run it locally before handoff. STOP conditions: any checker miss (Checker-Miss RCA), any need to expose a public census/table accessor, any interface not named here. Acceptance: focused suite and maki/test.f green; typed-local and package diff lints on the jj diff --git artifact; error-code-lint 0 findings. Claim: agent=s6b1 workspace=.jj-ws/habu-s6b1-prepare

Redesign 2026-07-26 after the checkpoint stop (the contract above stands except as corrected here). (1) ABORT is unimplementable without a public table exit; blocked by habu-add-wstore-public-db6c70fe (WSTORE:TABLE-DISPOSE), which lands as the first commit of this same lane; ABORT then erases the prep and disposes via TABLE-DISPOSE plus SAFET:RELEASE on the census. (2) Wording correction: SAFET has no RETAIN - the census is itself linear, so the RETAINED census means the prep owns the moved census token; no new SAFET surface. (3) Authorized shape: prep is a block-backed DEFLINEAR in the WSTORE token-is-its-block style, because an ENUM payload field cannot reference a STRUCTURE transitively containing a linear field (measured; tracked by habu-checker-enum-payload-9e1ae6cc); the package-private audited erasures (census and table cells in and out of the prep block, mint/read/consume of the prep token) carry TRUSTED.md rows citing the linear-scope and pointer-lifetime capability dots - roughly seven rows, accepted as the price of the checker gap, to be removed when those capabilities land. Test-plan correction from implementation (2026-07-26): the foreign-mcfg reject leg cannot exist as written, because PREPARE mints every layer identity from the configuration it validates (GPT2TENSOR:LAYER-ID is the sole constructor), so GPT2TENSOR:SLOT always compares a configuration with itself and GPT2TENSOR:E-CONFIG is unreachable from PREPARE by construction; the twin leg instead pins what PREPARE actually owns - two configurations differing only in a non-tensor field bind the same census while the preps capture DIFFERENT cfgkeys, which is the fact that lets a later commit refuse a foreign model. Foreign-model rejection is therefore a commit-leaf obligation (S6b2/S6b3 acceptance must include it).
