---
title: "Constructor: pass payload provider"
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-24T23:05:33.083774+02:00\""
blocks:
  - habu-enum-expose-provisional-bcb7c765
---

Why: the one shared constructor and DERIVE renderer in src/core/sumtype.f reads committed SUMV-PAY-* metadata directly, so the unified ENUM front end cannot supply its exact live declaration-token payload view before publication. Exact result: refactor that existing renderer call graph to accept an explicit provider context, family id, and three typed quotation capabilities with effects `[ n n n -- n ]` for payload count, `[ n n n n -- n ]` for declaration-order schema root, and `[ n n n -- n ]` for payload cell width. Every payload read used by constructor effects, zero padding, TAG drops, EQ binds/comparisons, and HASH binds/folds must go through those capabilities. The existing SUMTYPE/PRODUCT/legacy ENUM adapter supplies quotations over committed SUMV-PAY-N, SUMV-PAY-ROOT, and SUMV-PAYCELLS@ and preserves generated text, plan order, effects, protected packages, and failure behavior byte-for-byte. Keep family, provider context, and quotations explicit through the internal calls; no ambient mutable provider state, deferred global selector, copied schema rows, alternate generator, new public surface, ENUM-DECL wiring, grammar change, or publication change. Owner: the existing engine-private shared constructor renderer in src/core/sumtype.f; add no unowned global definition. Production proof: focused constructor/type-declaration tests drive the generalized existing render/evaluate path with the committed provider and compare its exact plans and callable constructors; a private hostile provider supplies deliberately distinct count/schema/cell values so direct SUMV reads in constructor, derived EQ, or derived HASH paths are detected. Mutations restoring any direct payload read in the renderer, swapping family/context, or dropping a provider call must fail. Files: src/core/sumtype.f and focused existing constructor/type-declaration tests only. Verify: constructor, type declaration, generated transaction, candidate validation, snapshot, AOT, exact fixpoint twice, typed-local, package, trust, host, and file-map gates. Depends: habu-enum-expose-provisional-bcb7c765. Claim: agent=ctor_payload_provider workspace=.jj-ws/habu-constructor-pass-payload-78c7069d.
