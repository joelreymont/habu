---
title: Stop double-loading the debugger trio where source is concatenated
status: open
priority: 2
issue-type: task
created-at: "2026-09-17T01:55:59.545788+03:00"
---

Problem: the manifest commit (88b97b26) took src/habu/debug-watch.f, stepper.f and debug.f out of the engine and gave debug.f and stepper.f `require` lines for their dependencies, and three consumers that CONCATENATE or `include` those files now load them twice and die `duplicate definition: BPW-MAX`: test/aot-wid-build.f CAPTURE-REPL-LINES (five `include` lines; every aot-wid-*, aot-data-*, aot-wide-format, aot-prefix-literal, aot-named-cells-image, aot-chain-capture and build-fixpoint-fixtures suite is red on the head, gate O and the prefix-fit lane's bisect 2026-09-17), src/habu/stdin.f READ-REPL (slurp + EVAL-HOST; build-fixpoint's refresh child), and test/bootstrap-engine-stack.fs BES-DEBUGGER (slurps debug.f; red on the pristine head, and since bootstrap_wide_gate is the first step of tools/bootstrap.sh the recovery chain cannot start). The prefix-fit lane proved it independent of the prefix strip: it reproduces with the strip disabled, and converting the fixture's include lines to require, or deleting the three require lines, turns all of it green. Acceptance: each consumer loads the debugger the way the manifest intends, once: a consumer that runs on an engine with a working `require` says `require src/habu/debug.f` and nothing else; a consumer that must concatenate text for an engine class that cannot require (say which, with evidence) concatenates the trio in dependency order and the trio's own require lines are no-ops there because src/core/include.f records a concatenated or included file in the require registry when the loader can see the path, or the consumer omits the trio if it never needed it; no debugger file is read twice on any path. Then the AOT family, build-fixpoint-fixtures and test/bootstrap-engine-stack.fs are green on an engine built from the tree (two generations) and the full recovery chain (no CHECK_ONLY) reaches `bootstrap OK`. Second commit in the same lane: a regression fixture for the separator defect the prefix strip exposed (0eaf921f): test/native-fixture-write.f emits an unseeded host and two runs of it, `--load drv.f` and `--load drv.f -- art extra`, both must run the driver and the second must not include `--`; registered in test/gate-stdlib-cases.f. Files: test/aot-wid-build.f, src/habu/stdin.f, test/bootstrap-engine-stack.fs, src/core/include.f (only if the registry rule is the fix), test/. Verify: test/aot-wid-suite.f, test/aot-data-span-forge.f, test/bootstrap-engine-stack.fs (gforth), tools/bootstrap.sh full; test/run.f is hazel's. Depends: none (0eaf921f landed). Ownership: the manifest lane's consumers. Claim: unassigned.
