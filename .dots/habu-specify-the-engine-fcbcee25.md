---
title: Specify the engine primitives in one table
status: active
priority: 2
issue-type: task
created-at: "2026-09-17T17:32:42.537487+03:00"
---

Problem: docs/x86-64.md (campaign habu-campaign-c6-targets-86bb56bb): the engine's primitives are an ARM64 assembly body in src/habu/habu1.f and a checker PRIM: row for the same name, so a second backend would fork the list. Acceptance: one machine-independent table src/habu/prims.f listing every primitive with its checker row and a reference implementation in checked Habu where one exists; the arm64 engine draws its rows from that table (no second copy of the effects); a parity gate (test/engine-suite.f is already the engine's behaviour suite, so the gate is its own file, test/prim-parity.f, driven by the table) that runs the same cases against each backend's primitive bodies and the reference, green on arm64 first; the table is the contract the x86_64 and Cortex-M backends implement. Files: src/habu/prims.f, src/habu/habu1.f, src/core/checker.f (row source), test/prim-parity.f. Verify: bin/hb --load test/prim-parity.f; byte fixpoint; tools/bootstrap.sh check; test/run.f. Depends: none. Ownership: hazel runs it on the arm64 host in two sequential workers (the table and the arm64 row source first, the references and the parity gate second). Worker 1 landed ab6c8e41 (line fb4f2392, chain AL): src/habu/prims.f (package PRIM-SPEC) loads before src/core/checker.f as pure data, 222 rows (210 global, 3 FFI package rows, 7 ELAB, 2 UNROWED) in the row forms 'EPRIM: name atoms [REF word] EPRIM;', 'EPPRIM: pkg name atoms ECLOSE-PRIVATE', 'ETRUSTED-ONLY!' flagging the row before it, 'ELAB: name', 'UNROWED: name'; atoms are the checker's PE-* spellings encoding codes A-IN 1 A-OUT 2 A-VAR-A..E 3..7 A-NUM 8 A-BOOL 9 A-REAL 10 A-U8 11 A-PTR 12 A-RAW 13 A-QUOT 14 A-QUOT-END 15 A-FINALLY 16 (composites are sequences: PE-PTR-A = A-VAR-A A-PTR); checker.f replays them (PE-SPEC-REPLAY right after PTABLE-START) so the effects exist only in prims.f, proven by a byte-identical textual round trip and a PES differential (482 rows identical); habu1.f FP-ARGS refuses a body with no row, habu2.f ENGINE-EMIT:PRIM-TABLE-COMPLETE refuses a KEEP?-kept row with no body; readers with PPRIM: axiom rows: COUNT, KIND@, NAME$, PKG$, REF$, TRUSTED-ONLY?, CODE-LEN@, CODE@, FIND ( ptr u8 n -- n ), K-PRIM/K-PKG-PRIVATE/K-ELAB/K-UNROWED; docs/porting.md 'Engine Primitives'. Worker 2 (next): the checked-Habu reference implementations attached by 'REF <word>' (a name, read back with REF$; today no row carries one) in a post-hook file, and test/prim-parity.f walking 0..COUNT-1, skipping K-ELAB and K-UNROWED, running the same cases against the backend body and the reference; delete the REF slot if the design diverges rather than leave it unused; the A-* atom constants have no checker rows (only the pre-hook replay reads them), and the capacity refusals and the treeshaken branch of PTC-ROW are untested. Worker 2 handed back dd88fd16 (line 383a18c7, chain AS): test/prim-parity.f (64 case sets over 81 effect rows, 416 assertions, name-keyed dispatch arms as the one place a primitive is spelled, coverage report naming the 132 rows with neither case nor reference), src/habu/prim-ref.f (46 references that never use the primitive they stand for), 46 REF clauses in prims.f; gen1==gen2==gen3; 432/432. Residuals: coverage by name not by row (habu-make-parity-coverage dot), FIELD reserved name in prims.f (habu-rename-prim-spec dot), the three engine facts (639af5fa, 50dc15ef, 074e1c48). Claim: agent=hazel-prim-parity workspace=.jj-ws/hazel-prim-parity (worker 2 done; closes when AS integrates).
