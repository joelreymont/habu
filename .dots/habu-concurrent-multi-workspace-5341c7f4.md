---
title: Concurrent multi-workspace gate runs flake E-PROC-TIMEOUT
status: open
priority: 2
issue-type: task
created-at: "2026-07-06T15:14:32.150721+02:00"
---

Two real incidents caught by the armed WHY-THREW trap during merge-gate cold-cache gating (2026-07-06): lib/process-test.f died with throw -2502 = E-PROC-TIMEOUT (lib/errors.f:47) under cold-gate load, on two DIFFERENT trees (once with the wait-rc migration commit, once without), immediate re-runs green both times. WHY-THREW dumps show all shared builders (SB/CK/CK-ROW) far from caps - pure timeout, not capacity; distinct from habu-event-closure-test-462fefdf (E-STR-CAPACITY -2201, different code path). Capture roots: hb-gate-1029715356506750-7 and hb-gate-1029974897391500-7 (gate pool out/err logs under HB_TMP). CAUSE HYPOTHESIS: multi-workspace gate concurrency - merge-gate cold runs racing worker-workspace gates saturate the box, and the process-spawning tests' fixed WAIT budgets (lib/process-test.f:41 PT-HB-TIMEOUT-MS=5000, :42 PT-CMD-TIMEOUT-MS=1000, :43 PT-SHORT-TIMEOUT-MS=100 feeding RUN-ARGV-CAPTURE / POLL-IN-OR-TIMEOUT which throw E-PROC-TIMEOUT at lib/process.f:261/:351/:137) expire while children starve for CPU. CANDIDATE FIXES (pick after measuring): (1) load-aware WAIT timeout scaling - scale PT-*-TIMEOUT-MS by the gate's measured calibration factor (the perf-profile cal-ms/cal-factor already quantifies box load) so a saturated box gets proportionally longer budgets; (2) gate-run mutex across workspaces - a repo-level lockfile so merge-gate and worker gates serialize (loses parallelism, simplest); (3) per-run slot budget - cap total concurrent gate pool slots machine-wide (shared semaphore keyed on the box, keeps partial parallelism). ACCEPTANCE: reproduce by running two cold gates concurrently in two workspaces (loop until a -2502 lands), land the chosen fix, then the same concurrent-loop runs N times green; the WHY-THREW line for any residual timeout must name the scaled/serialized budget so the next flake self-identifies.
