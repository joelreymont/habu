---
title: Own product-field lifecycle API
status: active
priority: 2
issue-type: task
created-at: "2026-07-23T00:08:08.366362+02:00"
blocks:
  - habu-bound-declaration-participant-9967c968
---

Problem: product-field transactions are mutated through raw global PF-BEGIN, PF-ADD, PF-PUBLISH, PF-FINALIZE, PF-COMMIT, and PF-ROLLBACK words. The event release leaf cannot adopt a qualified owner without either leaving duplicate authority or changing legacy product and test callers outside its scope. Exact contract: make package TYPE-FIELD-OWNER the sole lifecycle authority. It publicly owns OPEN, ADD, PUBLISH, and ROLLBACK with the current token, nesting, publication, rollback, error, and high-water behavior. PUBLISH validates the exact live open token before any watermark changes, publishes the outer watermark only at depth one, and releases exactly that frame. Remove every raw global PF lifecycle entry point; do not leave forwarding aliases. Migrate src/core/sumtype.f and the existing DECL-EVENT seam directly to the qualified API. In type-family-suite.f and type-family-rollback-suite.f, move only the changed transaction shims into short test packages and qualify their callers; do not package unrelated legacy helpers in this leaf. Update existing TRUSTED rows without adding a boundary. Preserve nested inner success followed by outer rollback, stale-token rejection before mutation, capacity failure atomicity, row reuse, snapshot behavior, and all declaration paths. Package owner: TYPE-FIELD-OWNER plus the two focused test packages. Acceptance: the production product declaration, declaration event, type-family, rollback, generated-declaration, structure, and enum suites pass; checked source probes reject every old PF lifecycle name; a hostile package-reopen attempt cannot reach private lifecycle helpers; package-diff lint reports zero without exemptions. Files: src/core/type-family.f, src/core/sumtype.f, src/core/decl-event.f, test/type-family-suite.f, test/type-family-rollback-suite.f, TRUSTED.md, focused absence/package fixtures only if required. Verify: exact production suites, shared candidate validation, typed-local/package/trust/refine gates. Depends: habu-bound-declaration-participant-9967c968. Ownership: product-field lifecycle names and direct caller migration only; excludes event PREPARE/COMMIT/FINALIZE semantics. Claim: agent=release_field_owner workspace=.jj-ws/habu-own-product-field-86660116.
