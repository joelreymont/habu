---
title: "Ship an FFI FUNCTION: call in a stripped image"
status: open
priority: 1
issue-type: task
created-at: "2026-09-18T21:25:26.164757+03:00"
---

Problem: a stripped AOT image whose closure reaches a word declared with the FFI declarer (lib/ffi-abi.f FUNCTION: ... ;FUNCTION) does not work. Reduction, measured on the integrated engine 0acb0a5f and tree 77fe4a87 (pristine): 'require lib/ffi-abi.f  PROCESS-SYMBOLS  FUNCTION: GETPID-CALL getpid ( -- n ) ;FUNCTION  : MAIN ( -- ) GETPID-CALL 0 > if 1 else 0 then . cr ;' built with tools/hb-build.f -o died rc 74 'aot: PC-relative target removed or outside closure' while ': MAIN ( -- ) ;' built; after the closure fix (Follow a branch out of a member into the closure, dc0ed9ee on the line) the branch out of the generated word is followed, and the stripped-closure lane measured a stripped COPY-FILE-STREAM program carrying the alias fix's FSTAT-CALL build and then SIGSEGV inside COPY-FILE-STREAM at run time - so re-measure the getpid reduction on the integrated engine first. The generated word (ffi-abi.f EMIT: FFI:RESET, STAGE, SLOT, CALL-WORD) reaches the FFI call helper the engine keeps outside any record and, at run time, process-owned state (symbol table, resolved addresses, the bounded-call trampoline) that a stripped image does not carry; the same class habu2.f EMIT-REPL-ROUTE fixed for 'throw' by ENGINE-HELPER:REGISTER of (LREPLROUTE), plus whatever data the call needs restored. Surfaced by alder's stream-copy alias fix (lib/fs-identity.f FSTAT-CALL, lib/fs-mutate.f TRUNCATE-CALL are FUNCTION: words), taken off the line until this lands (dot habu-preserve-the-src-2d3e2067 waits on this one); fs-identity's older DLSYM/ffi-call-bounded path also refuses at tier 1 (ncomp: cannot compile). Acceptance: name the exact unresolved target and the run-time dependency (make aot-lib.f MAP-TARGET!'s refusal print the site's record and target address so the next such case names itself); register or record what the FFI call reaches and carry the state it needs; the getpid reduction builds stripped and runs (prints 1); a tools/hb-build-test.f case for a stripped image with one FUNCTION: call; then the alias fix lands and HBT-STRIPPED-LIB-STATE stays green with it. Files: src/habu/aot-closure.f, src/habu/aot-lib.f, src/habu/aot-capture.f or aot-owned-cells.f, src/habu/habu2.f or lib/ffi-abi.f (whichever owns the helper), tools/hb-build-test.f. Verify: the reduction; tools/hb-build-test.f; three generations with cmp; test/run.f. Depends: none (the closure fix is on the line). Ownership: stripped-image closure and capture. Claim: unassigned.
