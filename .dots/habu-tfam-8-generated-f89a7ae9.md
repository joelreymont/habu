---
title: "TFAM 8: generated constructors without trust"
status: open
priority: 2
issue-type: task
created-at: "2026-07-03T23:36:48.942711+02:00"
---

PLAN.md item 8. Checked constructor effects+runtime words from SUMV metadata (payload cells, zero padding, tag); no TRUST/TRUSTED:/set-check emitted; package name via pinned Package Shape escape/hash derivation, reserved + non-reopenable, collision-checked; private families export nothing (metadata only until TFAM 9 construct form); bodies lower through existing checked paths (Gforth parity proven at TFAM 10); linear payload constructors reject until TFAM 11; undefine of generated entries rejects. Gate 17i. Depends: TFAM 7, TFAM 12.

SLICE 1 LANDED (commit "TFAM 8: pin constructor package-name derivation"): TF-CTOR-PKG$ derivation in src/core/type-family.f (escaped form uppercases + '--'-escapes EVERY joined segment INCLUDING the family tail — tail escaping is required for injectivity, package `a`+`b-c` vs top-level `a-b-c` collided under package-only escaping; docs/type-families.md + PLAN.md Package Shape wording updated to pin this); SHA-256 fallback (T + 16 lowercase hex over length-prefixed unescaped segment list + '-' + raw uppercase tail) reaches the hash through TF-SHA16-XT, installed by NEW prefix file src/core/type-family-sha.f (bin/hb re-parses its boot prefix from disk each run — habu2.f PFX tables — so the install must live in a prefix file after sha256.f; registered in habu2.f PFX load/path/provide tables + labels, bootstrap/cg/forth.fs mirrors, build-fixpoint COMMON+SNAP-KEEP, srclist.f, bootstrap.sh, diagnose-hb-core.f (+ COMMON-N 21 in diagnose-hb-test.f), shadow-lint.f, hb-build-lib key list, test/run-files.f, FILEMAP.md). SUMV-CTOR-PKG!/SUMV-CTOR-PKG$ accessors; TDECL-CTOR-PUBLISH at ;SUMTYPE interns the derived name into every variant of a PUBLIC family; private families stay zero. Escaped form <=16 bytes, hash form >=18 bytes, so the two forms can never collide; exact SHA goldens (92a8624462e75ea4 / e3b0c44298fc1c14) pinned in test/type-family-suite.f 12b; decl-level fixtures in test/type-decl-suite.f.

SLICE 2 DESIGN (investigated, not started): certification is the crux — a generated body pushes raw n literals, and hidden-field outputs cannot unify with raw cells (PARAM-HID-OK? one-hidden reject IS the anti-forgery rule), so a plain `: RESULT:OK ( a -- result<a,b> ) 0 ;` cannot certify through the normal hook. Sound path per census-tfam-8 §3b: register the constructor effect from SUMV metadata via E-ADD-EFFECT structured rows (axiom class, like PRIM: rows), runtime body emitted/generated from the SAME SUMV row; add a checker-owned pending-generated-constructor registration so the CHECK hook publishes the metadata effect for that definition instead of inferring (this is Package Shape's "constructor publish hook"); classify the new axioms in the prop census (test/prop-test-core.f GEN difftested vs NOEXEC-C). VALUE-RECORD is NOT a precedent (CHECKER-DEFRECORD registers metadata only, no generated words). Word creation: qualified definition `: RESULT:OK ... ;` auto-creates the RESULT wordlist (C-QUALIFY-DEF); generation may evaluate generated text from sumtype.f (unchecked prefix file — no new trust row; generated text must stay TRUST-free) or use a native friend hook. Caller package/checker state save+restore around generation (CHECKER-PACKAGE + PKG-PUB/PRI cells). SLICE 3: PROT-WID reservation (PROT-WID-ADD producer), non-reopenable + case-insensitive collision checks vs existing packages/qualified wordlists (hash-form names contain lowercase hex, users could alias case-insensitively), undefine-reject for generated entries. SLICE 4: retire TLP-MK2/UN2/MK4/UN4 (habu-retire-tlp-mk2-ac7760d2) once constructors express the seeds; linear-payload constructors reject until TFAM 11.

SLICE 2 LANDED (commit "TFAM 8: generate arity-0 sum constructors"): checker-owned pending-constructor certification (src/core/checker.f CTOR-PEND!/CTOR-PEND-MATCH?/CTOR-EXPECTED-ROW + the CHECK branch replacing SGOUT SUNI-COERCE with the SUMV-derived raw row SGIN+n-pads+n-tag for the single armed definition; publication rides the normal certified declared-sig path so callers get the hidden-field effect); sumtype.f TDGEN-* renders `: <CTOR-PKG>:<VARIANT> ( payload -- family ) 0 .. 0 tag ;` from interned SUMV metadata and evaluates it through the audited INCLUDE-EVALUATE boundary via TDECL-EVAL-XT (installed by include.f, boot-prefix-only; stage engines keep 0 = fail-closed); generation runs ONLY in the engine SUMTYPE word path — direct CHECKER-DEFSUM (verify-source/check tools) stays metadata-only so replay never mutates tool dictionaries. test/type-ctor-suite.f + gate wiring (GE-TYPE-CTOR-SUITE).

BLOCKER FOUND (parametric constructors): v1 params are cell-kinded vars and the linear kind discipline lets a linear con flow THROUGH a var (checker.f LIN-EFF-PASS/LIN-TAINT), so every arity>0 family is a possibly-linear layout: PUSH-LOGICAL keeps var-arg layout values ONE conservative logical cell, and a generated `( a -- zres<a,b> )` constructor's 1-cell output can never meet a caller's wide concrete expansion (probe: zmk-ok "expected zres<n,n> actual zres<n,a>"). Publication for parametric families therefore waits on TFAM 11 whole-bundle linear counting (or instantiation-time width resolution); TDECL-CTOR-WORDS gates on TFAM-ARITY@ 0 > with the RCA comment. Arity-0 families (concrete payloads, incl. multi-cell + padding + ptr payloads + enum-shaped) publish and certify wide today. SV.CTOR-SYM fill + undefine-reject + PROT-WID reservation + collision checks = slice 3; TLP seed retirement blocked by the same parametric gap (tlp-res/tlp-mix are arity-2) — retire dot habu-retire-tlp-mk2-ac7760d2 stays open pending TFAM 11. Static-tool constructor effects (tools/check preverify knowing RESULT:OK) = coordinated follow-up with the verify-source lane.

SLICE 3a LANDED (commit "TFAM 8: protect generated constructor entries"): SV.CTOR-SYM filled via CHECKER-RECORD-SYM post-publication; checker-owned protection guards (checker.f E-CTOR-PROTECTED 7111 + CTOR-PKG?/WORD?/EXTEND?-XT friend cells; predicates in type-family.f matching recorded SV.CTOR-PKG spellings case-insensitively): `package <derived-name>` rejects any case variant (CHECKER-PACKAGE), `undefine PKG:VARIANT` rejects BEFORE xref retirement (CHECKER-UNDEFINE-GUARD called first in xref.f UNDEFINE-NAME/UNDEFINE-FOUND + PRIM row; word remains callable after a caught reject), and a new tail cannot certify into a constructor package (CHECKER-USIG-CERT-ADD guard; exact ctor respellings fall through to duplicate-definition). Fixtures in test/type-ctor-suite.f slice-3 block. REMAINING for full slice 3 (NOT landed): the PROT-WID-ADD production producer (engine-level friend write at generated-package WID creation — habu2.f C-PACKAGE-ALLOC-WIDS/C-QUALIFY-DEF emitted-code surgery + AOT registry persistence; the checker-level guards above cover the CHECKED source paths only, raw engine sinks stay 2b-iii scope) and declaration-time collision vs pre-existing engine wordlists/packages (needs a name->wid lookup callable from prefix code). 2b-v(f)/2b-iii status for the coordinator: slice 3a does NOT yet unblock them — they need the PROT-WID producer, which remains the precise gap both dots already name; the generated-ctor package creation site is now concrete (TDECL-CTOR-WORD's first qualified definition creates the WID) so the producer hook has a landing point.