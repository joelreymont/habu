---
title: Cut the fixed per-word cost of tier 1 compilation
status: open
priority: 2
issue-type: task
created-at: "2026-09-16T16:41:00.724414+03:00"
---

Problem: tier 1 compiles a word in 2.51 ms against 0.105 ms at tier 0 (23.8x), and a TRIVIAL word costs 723 us against 28 us (docs/compiler-measurements.md, 2026-09-16), so most of the multiplier is a fixed per-word cost, not proportional to the code: arena setup, schema and attribute machinery, the regalloc verifier (src/compiler/native/regalloc-verify.f, 2,350 lines, run on every word) and whatever else runs per definition regardless of size. This is what makes an application AOT build take minutes (Tender: 159 s) and the engine self-build spend its compilation share. Acceptance: (1) a profile of tier 1 compiling a corpus (tools/tier-bench.f's compile-floor case and lib/string.f), taken with the internal profiler (habu-profile-the-build-c26f424c's tool or prof-on directly), attributing the 723 us floor to words with callers, committed as a section of docs/compiler-measurements.md; (2) the top fixed costs removed or made proportional: a verifier that runs once per build or under a flag rather than per word if it is the floor, arena reuse instead of per-word allocation, tables sized once, whatever the profile names, each with the measured floor before and after; (3) target: trivial word under 150 us, corpus under 0.8 ms per word, no loss of verification strength (the verifier still runs in the fixpoint build and the gate); byte fixpoint; full gate green. Files: src/compiler/ir/*.f, src/compiler/native/*.f, tools/tier-bench.f, docs/compiler-measurements.md. Verify: tools/tier-bench.f compile-floor and corpus before and after; tools/native-build.f fixpoint with timing; test/run.f. Depends: profiler slices 1-2 landed (3450a9cd). Ownership: tier-1 driver. Claim: unassigned.
