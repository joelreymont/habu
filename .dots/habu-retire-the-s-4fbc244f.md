---
title: Retire the S-PWID buffer readers left by the bitmap capture
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-13T09:13:20.984822+03:00\""
---

Problem: the artifact format's S-PWID section has no buffer since the protected-WID capture became a live bitmap (3e29a730), and the section is already SKIP-SECTION on merge (src/habu/aot-file.f:1099), yet src/habu/aot-file.f:244 (SEC-PTR) and tools/aot-chain-capture.f:335 still call AOT-PWID-BUF@, which no longer exists, and tools/aot-chain-capture.f also dies earlier on E-UNDEFINED: AOT-ARM:SIG-CLOSE; measured 2026-09-12 by the protected-WID fixture lane. Neither path is in test/run.f, which is why the residue survived.  Measured 2026-09-13 by the capture-window lane: AOT-ARM:SIG-CLOSE is referenced in four places (tools/aot-chain-capture.f:86 and comments at src/habu/aot-capture.f:831,846, src/core/type-family.f:2962) and defined nowhere, so the tool dies at AOT-CHAIN:CLOSE before it loads aot-capture.f; its body is determined by those comments (disarm the signature collection and latch the type registry, what ACAP-SIG-END does: CHECKER-ASIG-ARMED? 0= if exit then CHECKER-REG-AOT-CLOSE). With the capture-window landing (Keep only the window's declared address cells), ?XTOFF's literal XTOFF-N = 1 is wrong by the below-window cells whose target is in their kind's span (the three NFEED:INSTALL cells: 4), so the assertion must be the predicate (rows = window cells + below-window cells with in-span targets), and ?TRAPPED is unaffected (TRAPPED-BELOW walks the live XTCELL table). Acceptance adds: SIG-CLOSE exists as a public word of aot-arm.f with the effect the comments specify, owned by the tier stack lane if it lands first. Acceptance: the format owner's decision recorded (drop S-PWID from the format with a version note, or give it a live producer) and implemented; every reader resolves; tools/aot-chain-capture.f runs end to end the way its header says on a cold-built engine and its assertions state the current capture (the capture-window lane changes which below-window rows travel); a check that loads aot-file.f and aot-chain-capture.f through the real load path so an undefined word there fails a suite. Files: src/habu/aot-file.f, tools/aot-chain-capture.f, docs/. Verify: the tool end to end, test/run.f. Depends: habu-keep-declared-addr-dbd7d8d9. Ownership: hazel. Claim: agent=hazel-worker workspace=.jj-ws/habu-retire-the-s-4fbc244f.
