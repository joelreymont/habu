---
title: Test the guarded-extent clauses the fixtures name
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-16T14:18:00.818355+03:00\""
---

Problem: after d1192d32, test/engine-stack-wide.f:45-49,56-60,96,129 and test/engine-stack-jit.f:11-14 hand a plain create/allot buffer to run-in-stack, which GUARDED-EXTENT? (src/habu/habu1.f:2558) refuses before the callback runs, so about ten cases labelled tuck, count, s", pipe, 513-cell typed fetch, 128-cell copy and two-cell flush all assert the same fact (rc 67, -3802) and their bodies are dead text; the per-transfer capacity boundary they used to cover is untested. test/stack-guard.f:57-77 and test/engine-stack-lifecycle.f:170-176 attribute their refusals to the "outside DATA region" clause, but an allot address trips the base-alignment clause first; the DATA-region and wrap clauses are exercised nowhere (measured: `['] EMPTY data-base STACK-ABI:PAGE-BYTES run-in-stack` throws -3802). In BRUNSTACK (habu1.f:2579-2584) the first STACK-GUARD:CHECK-CURSOR runs with cursor = base right after GUARDED-EXTENT? passed and cannot fail. Comment and doc drift: test/engine-stack-jit.f:36-40 names `i` as the faulting read when the inner do frame WRITE at base-16 faults first; docs/debugging.md:470 says "two" inside the section retitled "three" (:426); docs/debugging.md:467 describes the refusal as provenance-based (MEM-ALLOC-GUARDED) when it is structural (page-aligned, page-multiple, outside DATA); test/process-pty-tty-smoke.f:154-161 says a leaked pool descriptor exits 102 when TTY-POOL is now a 64 KiB mapping and the discriminator is `depth .` = 0; test/bootstrap-wide-memory-src.f:239-243 cites the deleted BWM-REQUEST-GOLD and BWM-FETCH2 (:58) has no caller, and the only execution of a wide transfer while a switched stack allocation is active was lost with BWM-TEST-EXACT. Acceptance: one representative unguarded-extent refusal per file; the other cases become real overflow cases on a guarded stack filled to capacity (expect REFUSED-DATA) or are deleted; explicit cases for the DATA-region clause and the wrap clause (page-aligned $FFFFFFFFFFFF0000 capacity on a real guarded base); comments name the clause that fires; the vacuous CHECK-CURSOR is removed with the reasoning in the commit, or shown reachable; every listed comment and doc sentence is true; BWM-FETCH2 is called or deleted; a wide transfer runs while a switched guarded stack is active. Files: test/engine-stack-wide.f, test/engine-stack-jit.f, test/stack-guard.f, test/engine-stack-lifecycle.f, docs/debugging.md, test/process-pty-tty-smoke.f, test/bootstrap-wide-memory-src.f, src/habu/habu1.f. Verify: rebuild the engine with tools/native-build.f when habu1.f changes; run the listed suites and test/engine-stack-debugger.f against it. Depends: none. Ownership: the listed files. Claim: agent=hazel-audit-fixtures workspace=.jj-ws/hazel-audit-fixtures. Source: Opus audit of d1192d32 (2026-09-16), findings M4 M5 and minors.
