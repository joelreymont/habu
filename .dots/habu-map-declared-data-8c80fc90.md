---
title: Map declared DATA cells holding carried or below-window addresses
status: active
priority: 2
issue-type: task
created-at: "2026-09-21T13:52:58.157418+03:00"
---

Problem (audit of 63315bd8, CONFIRMED): a carried engine address that reaches a stripped image through a declared pointer cell is neither rewritten nor refused. src/habu/aot-closure.f:634 XTC-ROW skips DATA-kind address-cell rows (raw XTCELL-DATA-TAG and 0<> IF exit) and AOT-DATA-TEXTPTR-CHECK refuses only code/dict pointers, so DATA-TARGET/CARRIED-TARGET (reached only from SCAN-ADDRESS aot-closure.f:678 and COPY-ADDRESS aot-lib.f:604, both movz/movk decoders) never sees the cell. Reproducer: require lib/string.f / PTR-VARIABLE CACHED / STR-MAX-I64$ CACHED ! (build time, inside the window) / : MAIN ( -- ) CACHED @ STR-I64-DIGITS type cr STR-MAX-I64$ STR-I64-DIGITS type cr ; -> hb-build OK, the image prints 19 NUL bytes then 9223372036854775807: the cell shipped the engine address, which lands in the zero-filled DATA mapping. The hole predates the carried run but the carried constants invite it (caching a pointer to an engine table is an ordinary idiom). Also from the audit: OWNED-AT? answers false for a CARRIED claim so DATA-ADDRESS! (aot-closure.f:533-535 vs 570-575) would refuse an address DATA-TARGET maps - safe today only because its one caller is the fall-through in DATA-TARGET; CARRY-CELL's DATA-ADDRESS? guard admits a claim whose range lies inside the window (copied, never consulted); docs/native-applications.md says the carried run 'costs the image nothing but address space' while every stripped image ships its 326 non-zero carried bytes (data 66 -> 392 on a hello-world); the carried budget (624 of CARRY-BYTES 1024 used by the four claims) is undocumented. Acceptance: DATA-kind address-cell rows whose value is a DATA address go through DATA-TARGET at link (an in-window value maps as today, a carried value is rewritten to the copy plus its interior offset, an unowned below-window value is refused with the existing undeclared-cell refusal naming the cell); the reproducer prints 9223372036854775807 twice as a tools/hb-build-test.f fixture and a below-window uncarried cached pointer is a pinned refusal; one predicate answers 'owned or carried' for both DATA-ADDRESS! and DATA-TARGET; CARRY-CELL refuses a claim inside the window; the docs sentence states the measured cost and the budget. Files: src/habu/aot-closure.f, src/habu/aot-lib.f, src/habu/aot-owned-cells.f, docs/native-applications.md, tools/hb-build-test.f. Verify: tools/hb-build-test.f, test/gate-aot-image.f, the stripped suites, three generations, test/run.f. Depends: habu-size-the-stripped-b2932715 (same files). Ownership: hazel (src/habu). Claim: agent=hazel workspace=.jj-ws/hazel-data-cells.
