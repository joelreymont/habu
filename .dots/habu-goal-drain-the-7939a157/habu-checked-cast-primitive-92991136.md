---
title: Checked cast primitive to end per-declaration trust
status: open
priority: 1
issue-type: task
created-at: "2026-07-18T23:21:11.265434+02:00"
---

Root-cause fix for TRUSTED growing linearly with declarations: every EXTENT: mints a TRUSTED injector (>#M), every NOMINAL: mints two trusted identity casts, because the checker cannot express a retype into a nominal family. Design target: one checker-known cast form so declaration-generated converters are CHECKED, not trusted. Sketch: a core declarer CAST: <name> ( n -- <family-tail> ) [guard-body] ; where the checker itself certifies the body under the rule 'input cell flows to output unchanged; the only allowed effect is a guard that throws' - i.e. the checker types the identity data-flow and the guard, and the retype is sanctioned by the declaration form rather than by trust. Extent injectors then declare their range guard in the open (n 0 < over #M >= or if E-EXT-RANGE throw then) and stop being trust rows; NOMINAL: converters become guardless checked casts; the roles.f built-in pairs (>IDX/IDX>N etc.) migrate too. This retires the largest class of TRUSTED rows and stops the per-declaration growth. Phase-0 priority: design the checker rule first (what exactly the certifier must prove: single-cell identity flow, no other writes, guard throws only), write the negative suite (a cast that mutates, drops, or swaps must be rejected), then implement. Depends on nothing in flight; touches checker.f, so serialize with any other checker.f lane.

DESIGN SETTLED 2026-07-19 (orchestrator). The certifier rule, v1:

  CAST: <name> ( in -- out ) <body> ;

(1) The body is certified by the ORDINARY checked-word certifier under the row ( in -- in ) - the declared input type on both sides. So an empty body certifies trivially; a guard body (dup 0 < over 128 >= or if E-EXT-RANGE throw then) certifies because dup/comparison/throw are ordinary checked operations; any net stack change, unbalanced branch, or type misuse is an ordinary reject. No purity rule beyond checkedness in v1: the body may do anything a checked word may do, because the cast then adds no unsoundness any checked word could not already contain. (2) At publication, the declarer swaps the certified row's output for the DECLARED out and records ( in -- out ) through the one publish choke (checker.f:456 E-ADD-EFFECT). (3) The retype legality check at that swap: exactly one input and one output term, and both in and out must be single-cell of the same machine class - a plain cell, an arity-0 family scalar, or a parametric family cell (type var allowed, so IX>N's generic ( ix<e> -- n ) projection works). Width or class reinterpretation (n -> ptr a, rc -> ptr u8) is REFUSED - those stay TRUSTED; CAST: covers exactly the nominal/family retype class. 

COVERS (retires these trust sites): extent injectors >#NAME with their range guards (maki/extent.f generated), IX>N, NOMINAL: converter pairs (lib/type/value-nominal.f generated), the report handle converters after stage A0, roles.f built-in pairs >IDX/IDX>N/>LEN/LEN>N/>FD/FD>N. DOES NOT COVER (stays TRUSTED): reinterpret casts (ARENA-RC>PTR, TOKBUF-RC>PTR, HIDX-RC>PTR...), evaluate boundaries (XG-EVAL/NG-EVAL/DTC-EVAL), machine-code emitters.

NEGATIVE SUITE (must reject): out arity != 1 or in arity != 1; out family undeclared; in/out different machine class (n -> ptr a); body with net stack effect; body consuming the input (drop swap games caught by ( in -- in ) certification); declarer used inside a checked body (top-level-interpret-only like the other definers).

IMPLEMENTATION SHAPE: a core declarer beside the existing definers (sumtype.f neighborhood), parsing name + signature + body source, running the standard certify with the substituted row, then publishing the swapped row. FEASIBILITY FIRST: probe how a definer can drive certify-then-publish-swapped through the existing choke; if the publication path cannot take an overridden row cleanly, STOP and report the actual mechanics plus the minimal seam - do not force it with a private checker fork. MIGRATION is separate follow-up changes per consumer (extent.f, value-nominal.f, roles.f), not part of the primitive's landing change.

SEAM DECIDED 2026-07-19 (orchestrator, from the feasibility lane's report; zero edits made by that lane, baseline gate green). Certify-with-substituted-row is already reachable (CANDIDATE-SCOPE prims + CHECK, verdict -1, rollback proven). Publish-different-row is not: the one publisher of arbitrary rows (CHECKER-USIG-CERT-ADD -> USIG-ADD -> E-ADD-EFFECT at checker.f:4137) is internal-marked, and the exposed prims structurally enforce body-output == declared-output. The split DOES exist internally: the CTOR-PEND one-shot window (checker.f:8824-8843) makes CHECK certify against CTOR-EXPECTED-ROW while publishing the declared row - sealed behind ;SUMTYPE and xref-erased.

ADOPTED: option A, mirroring CTOR-PEND. (1) E-CAST-ARITY / E-CAST-CLASS / E-CAST-FAM error constants; (2) CAST-LEGAL? legality gate above CHECK: 1-in/1-out, no return clause, same single-cell machine class (plain cell | arity-0 family scalar | parametric family cell, type var allowed), width/class reinterpret refused; (3) new allowlist prim CAST-PEND! ( ptr u8 n -- ) arming a one-shot {name} window with CAST-PEND-MATCH?/CAST-PEND-CLEAR; (4) in CHECK beside the CTOR-PEND branch: on match, clear, throw the named reject if illegal, else certify body-output against SGIN (the identity-preserving retype) while the existing tail publishes the declared ( in -- out ) SGA/SGU row; (5) engine-side CAST: declarer in roles.f (the nominal-cast home) parsing name + signature + body, arming the window, driving one ordinary checked colon compile - no TRUSTED word anywhere. CAST-PEND! is deliberately NOT xref-erased: the window only admits an ( in -- in )-certified body retyped within the same single-cell class, which adds no unsoundness a plain checked word could not contain (the v1 rationale). Option B (an exposed publish prim) rejected: it would let any caller mint arbitrary certified rows.

Claim 2026-07-19: agent=spark-cast-impl workspace=.jj-ws/cast-impl (implementing the adopted option-A seam: checker.f CAST-PEND window + CAST-LEGAL? + roles.f CAST: declarer + suites). Any other checker.f lane (e.g. habu-flip-rscatch-opaque-5da02bd5) should serialize behind this landing to avoid CHECK-neighborhood merge conflicts.

PRIMITIVE LANDED 2026-07-19 as bd55f97f95cb (claim released). CAST-PEND window + CAST-LEGAL gate in checker.f (E-CAST-ARITY/CLASS/FAM 7129-7131), CAST: declarer in roles.f, positive + negative suites routed through candidate-validation, cast: in UNSAFE-TOK?/UNSAFE-SET-SEAL, byte fixpoint held, gate green composed with the RSEXEC/RSCATCH opaque-execute flips. Zero TRUSTED words added (one prim-axiom inventory row for CAST-PEND!, the honest bookkeeping). REMAINING on this dot: migrate the existing trusted converter sites onto CAST: - extent injectors >#NAME (maki/extent.f generated text), IX>N, NOMINAL: converter pairs (lib/type/value-nominal.f - serialize behind the Mac codegen-buffer lane), roles.f built-in pairs (>IDX/IDX>N, >LEN/LEN>N, >FD/FD>N), each migration deleting its TRUSTED.md rows. v2 ownership rule is dotted separately (habu-cast-v2-family-741e7bae).

AUDIT CENSUS 2026-07-19 at f7ed6085: exactly 69 empty single-cell `TRUSTED:` converters in Maki are mechanically eligible for the landed `CAST:` surface. Sites: tensor.f (8), async-dag.f (6), model-ir.f (10), typestate.f (10), numpolicy.f (2), target/target.f (2), evidence/promote.f (1), onnx/import.f (2), schema.f (2), producer.f (2), rev.f (2), artifact.f (2), config.f (2), extent.f (1), fusion-plan.f (2), tensor-value.f (2), journal.f (2), db/capability.f (2), db/action.f (2), db/diff-suite-id.f (2), db/obligation.f (2), db/evidence.f (2), and experiment/run.f (2). Scope is only declarations with an empty body and one same-machine-cell input/output; keep validation, visibility, and owner boundaries unchanged. Acceptance: an exact post-migration census finds zero eligible empty `TRUSTED:` converters, trusted inventory falls by 69 with no replacement trust, existing range/refinement and round-trip tests remain green, and the CAST negative suite still rejects arity/class/body violations.
