---
title: Create absolute package prefix nodes
status: active
priority: 1
issue-type: task
created-at: "2026-07-30T21:36:54.696414+02:00"
---

Current package A:B exits 75; E1 must make absolute create/reopen real without deep lookup. package A:B:C creates or reuses package records for A, A:B, and A:B:C. DREC stays 48 bytes: the full case-preserving absolute path is identity under folded comparison; -1 remains the namespace marker; the existing min-input byte is kind (package=0, future type=1); package rows own distinct, nonzero public/private WIDs at or above FIRST-DYNAMIC-WID; future type rows own one nonzero public WID at or above FIRST-DYNAMIC-WID and an exactly zero private role. One folded absolute path may occur only once across all namespace kinds. Parent is derived by chopping the path; no stored parent, side table, new counter or log, format or version change, compatibility state, or lazy repair of an invalid zero private role. Exact token length is retained: names through 16 bytes stay inline, longer names use existing CP-backed EXT storage, and insufficient CP rejects with rc 76 before NDICT publication; no path is truncated. Long-name storage keeps the rounded CP end separate from the source cursor. The separate generated-name hash/cap removal remains habu-reject-over-limit-84b133ca. Rollback remains CP/NDICT and WIDN stays monotonic.

Snapshot restore translates every EXT namespace pointer from the persisted RBASE-VA sentinel before bounding its span. It rejects malformed paths, duplicate folded namespace identities, zero or reserved package roles, a zero or reserved type public role, any nonzero type private role, namespace-role reuse across package and type rows, and non-type roles that overlap protected WIDs. Every type public role must appear exactly once in the protected-WID registry; every package role must appear zero times. The exact package row named by an owner entry is the only permitted overlap with that owner pair. All namespace roles remain disjoint from owner roles except the exact named owner package pair. Restore raises WIDN above every accepted package role and type public role. WIDN remains at or below OWNER-WID-LIMIT after every successful allocation: a package pair requires old WIDN plus two at or below the limit, and the shared one-WID wordlist primitive requires old WIDN plus one at or below the limit; each fails with rc 77 before stack, dictionary, name, or WIDN publication. Owner validators accept well-formed colon paths and reject leading, trailing, and double separators. Compact AOT EXT support is excluded because its proven capture range has no packages.

E1 owns the one shared read seam in src/habu/habu1.f: leaf label LNSFIND and emitter EMIT-NS-FIND take x9=absolute-path pointer and x10=exact length; return x5=matching namespace DREC or zero and x11=kind only on a hit; preserve x2, x9, and x10, use x30 only as the leaf return, and clobber x3-x8 and x12-x17. It matches only -1 namespace rows by exact folded inline or EXT name. Prefix ensure calls it; E2a later reuses it unchanged for LFIND and qualified definitions, which E1 does not wire.

Owner and write set: src/habu/layout.f, src/habu/habu1.f, src/habu/habu2.f, src/habu/aot-capture.f, test/gate-dictionary-lib.f, test/owner-wid-doctor.f, test/owner-wid-child.f, and only necessary current trust-ledger rows. The owner-WID doctor adds synthetic non-owner snapshot cases for zero and reserved roles, duplicate identities, role aliases, a valid protected public-only type row, a type row with a nonzero private role, a type public-role alias, and a type row missing protection; the child drives each through the real restore path. Exclude checker, bootstrap, declarers, nested blocks, deep or ancestor lookup, using, generic AOT work, and new lint. Package sealing is deleted by the preceding M13c engine leaf; E1 integration depends on that deletion and must not add a temporary first-component seal guard.

Acceptance at M17: a private word defined in A:B:C is called bare by a case-varied reopen; reopening prefixes allocates nothing; malformed paths publish nothing; a caught failure restores CP/NDICT; the existing long owner package survives snapshot restore; every hostile namespace snapshot exits with the corruption result; restored WIDs cannot collide. Valid snapshots forged at the allocation ceiling drive qualified-prefix creation, package creation, and wordlist through their real source paths and each exits 77 before publication. Pre-M17 checkpoint: authored production probes, the stdin AOT capture census, exact source censuses, and independent source review only; no load or engine refresh occurs before M17. Claim: agent=e1_impl workspace=.jj-ws/habu-create-absolute-pkg-2856a6d0.
