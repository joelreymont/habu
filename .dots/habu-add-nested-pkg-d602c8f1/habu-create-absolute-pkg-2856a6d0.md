---
title: Create absolute package prefix nodes
status: active
priority: 1
issue-type: task
created-at: "2026-07-30T21:36:54.696414+02:00"
---

Current package A:B exits 75; E1 must make absolute create/reopen real without deep lookup. package A:B:C creates or reuses package records for A, A:B, and A:B:C. DREC stays 48 bytes: the full case-preserving absolute path is identity under folded comparison; -1 remains the namespace marker; the existing min-input byte is kind (package=0, future type=1); package rows own nonzero public/private WIDs. Parent is derived by chopping the path; no stored parent, side table, new counter or log, format or version change, or compatibility state. Exact token length is retained: names through 16 bytes stay inline, longer names use existing CP-backed EXT storage, and insufficient CP rejects with rc 76 before NDICT publication; no path is truncated. The separate generated-name hash/cap removal remains habu-reject-over-limit-84b133ca. Rollback remains CP/NDICT and WIDN stays monotonic. Restore raises WIDN above namespace roles; owner validators accept well-formed colon paths and reject leading, trailing, and double separators. Compact AOT EXT support is excluded because its proven capture range has no packages. E1 owns the one shared read seam in src/habu/habu1.f: leaf label LNSFIND and emitter EMIT-NS-FIND take x9=absolute-path pointer and x10=exact length; return x5=matching namespace DREC or zero and x11=kind only on a hit; preserve x2, x9, and x10, use x30 only as the leaf return, and clobber x3-x8 and x12-x17. It matches only -1 namespace rows by exact folded inline or EXT name. Prefix ensure calls it; E2a later reuses it unchanged for LFIND and qualified definitions, which E1 does not wire. Owner and write set: src/habu/layout.f, src/habu/habu1.f, src/habu/habu2.f, src/habu/aot-capture.f, test/gate-dictionary-lib.f, and only necessary current trust-ledger rows. Exclude checker, bootstrap, declarers, nested blocks, deep or ancestor lookup, using, generic AOT work, and new lint. Package sealing is deleted by the preceding M13c engine leaf; E1 integration depends on that deletion and must not add a temporary first-component seal guard. Acceptance at M17: a private word defined in A:B:C is called bare by a case-varied reopen; reopening prefixes allocates nothing; malformed paths publish nothing; a caught failure restores CP/NDICT; snapshot owner identity and namespace WIDs survive; restored WIDs cannot collide. Pre-M17 checkpoint: a failing real probe, the stdin AOT capture census, and representative typed, package, and trust diff gates. Claim: agent=e1_impl workspace=.jj-ws/habu-create-absolute-pkg-2856a6d0.
