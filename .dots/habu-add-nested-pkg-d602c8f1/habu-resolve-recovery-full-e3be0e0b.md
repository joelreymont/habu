---
title: Resolve recovery full namespace paths
status: open
priority: 1
issue-type: task
created-at: "2026-07-31T06:35:01.144786+02:00"
---

Source dependency: exact reviewed recovery namespace-row candidate plus the frozen native last-separator contract; exact code ancestry enforces ordering inside the stop-the-world branch. Owner: Gforth recovery engine under the existing HB-EMIT vocabulary. Mirror native E2a with one private last-separator classifier and one private exact-WID lookup leaf. The exact-WID leaf takes x9/x10 name bytes and x2 WID, preserves the name and WID inputs, and returns the same x11/x12/x13 address, length, and encoded-found result as LFIND. LFIND owns only resolution order: malformed refusal; exact full-prefix namespace lookup; package-or-type public lookup; and bare private, public, then global lookup. The existing first-colon and inline exact-WID scans are deleted. C-REJECT-DUP-DEF calls the same exact-WID leaf with the pending tail and DEF-WL, then emits native's duplicate-definition diagnostic and recoverable code 78 before publication. This closes the pre-existing recovery defect where a recovered engine accepts a duplicate definition that native rejects without adding another scanner or dot.

Emit one full-prefix ensure walker that reuses recovery E1 LNSFIND, the package-row creator, and the shared classifier. Qualified definition creates missing package prefixes, accepts package rows, rejects type rows, then uses the shared duplicate wall. Package lookup accepts package or type rows. Preserve recovery E1 row shape, rollback, WID monotonicity, hard snapshot/protected-publication failures, and the single LCOMPILEDIE tail. No second semantics, parent link, side table, compatibility, version, ancestor lookup, using change, nested package blocks, native edit, new lint, or source-census pseudo-test. Write set: bootstrap/cg/forth.fs and only necessary removals or current manifest-spelling updates in tools/bootstrap-codegen-test.f. Pre-M17 proof is exact ancestry, every-hunk review, and source census only. M17 acceptance runs the real no-binary recovered compiler and proves native parity through deep lookup and definition, malformed refusal without publication, package/type walls, bare and qualified duplicate refusal, exact diagnostic/code 78, and unchanged CP/NDICT with monotonic WIDN after caught failures.
