---
title: Own engine emitter package
status: active
priority: 1
issue-type: task
created-at: "2026-07-30T22:13:55.502748+02:00"
---

E1 representative code cannot pass the mandatory package gate because the native emitter entry and its altered transitive callers are legacy globals. Make the existing emitter boundary a real package before namespace behavior changes. In src/habu/habu2.f, package the callback block and transitive emission chain under ENGINE-EMIT; keep all internals private and expose only FORTH ( ptr u8 n -- ). Hard-cut global EMIT-FORTH with no alias. ENGINE-BUILD:BUILD calls ENGINE-EMIT:FORTH. In src/habu/build.f, package only the changed GO and BLD-RUN driver boundary as BUILD-DRIVER with public RUN and invoke BUILD-DRIVER:RUN; unchanged legacy helpers remain untouched. In src/habu/stdin.f, replace the changed GO boundary with package STDIN-DRIVER public RUN and invoke STDIN-DRIVER:RUN. Update the generated driver call in package AOT-WID-BUILD to ENGINE-EMIT:FORTH. Hard-rename the one audited trust identity from EMIT-FORTH to ENGINE-EMIT:FORTH in TRUSTED.md with no alias or duplicate row. Bootstrap cg/forth.fs owns a distinct Gforth-local EMIT-FORTH and is excluded. Owner and files: src/habu/habu2.f, src/habu/build.f, src/habu/stdin.f, test/aot-wid-build.f, TRUSTED.md. Pre-change owning proof: a representative namespace emitter call forces edits to unowned EMIT-LABEL-CORE and EMIT-PRIMITIVE-SECTIONS and package-diff rejects. Acceptance before M17: exact package, typed-local, and trust source gates pass; old global EMIT-FORTH has zero native callers/definition or trust row outside excluded bootstrap; all production callers use ENGINE-EMIT:FORTH; focused exact load paths pass. No behavior change, namespace code, forwarding shim, compatibility alias, new lint, whole-driver packaging, bootstrap edit, or unrelated emitter refactor. E1 must rebase onto this commit before resuming. Claim: agent=engine_emitter_impl workspace=.jj-ws/habu-own-engine-emitter-42db38aa.
