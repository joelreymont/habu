---
title: Resolve checked full namespace paths
status: active
priority: 1
issue-type: task
created-at: "2026-07-31T06:35:00.353571+02:00"
---

Source dependencies: exact reviewed native E2a on E1, the HB package hard cut, and reviewed recovery E1; dispatch only from their combined exact source ancestry. Owner QNAME lives in one new src/core/qname.f loaded immediately after src/core/util.f. It exports BARE, QUALIFIED, MALFORMED, 7107 E-SYNTAX, and SPLIT ( ptr u8 n -- ptr u8 n ptr u8 n n ). For input a/u, SPLIT scans once: BARE returns a/0 and a/u; QUALIFIED returns a/split and a+split+1/u-split-1 at the last colon; MALFORMED returns a/0 and a/0 when any component is empty (leading, trailing, or doubled colon). Plain named constants are intentional because QNAME loads before declaration types exist; do not add a result family. Register exact checker effects for the constants and SPLIT before PTABLE-END. Replace only CHECKER-QUALIFIED?, type-family TF-CW-SPLIT?, type-family TFQ-SPLIT?, and xref qualification scanning with QNAME:SPLIT; delete their scanner state. Checker LCOLON remains the separate first-colon local name:type grammar.

Package XREF owns FOUND, NONE, MALFORMED and RESOLVE ( ptr u8 n n -- n n ): token plus the caller's bare WID to record index plus status. RESOLVE uses one private backward dictionary-index scan. BARE searches the supplied WID; QUALIFIED resolves the complete prefix through an exact namespace row of KIND-PACKAGE or KIND-TYPE, then searches its public WID; NONE and MALFORMED return index -1, and MALFORMED never scans. XREF-FIND-WL projects the same private index scan to its existing record-pointer interface. XREF-FIND keeps its existing pointer interface, delegates with bare WID 0, maps FOUND to XREF-REC and NONE to XREF-NULL, and propagates MALFORMED as catchable QNAME:E-SYNTAX; it never maps malformed input to absence. XREF, mandatory undefine, optional undefine, hide, and forget call RESOLVE directly with their existing bare-WID semantics (XREF/global, undefine/current, optional/global, hide/global, forget/global). NONE alone is absence; MALFORMED fails before checker, dictionary, CP, or NDICT mutation. Retirement consumes the resolved index without rescanning.

Add src/core/qname.f immediately after src/core/util.f under the frozen LPQNAME row in all three native prefix tables and label initialization. Add the same ordered source to tools/hb-build-lib.f, tools/boot-pin.f, tools/build-fixpoint.f, tools/build-fixpoint-test.f, tools/diagnose-hb-core.f, test/run-files.f, and tools/lint/shadow-lint.f. Update only the owning counts: test/boot-pin-test.f BPT-PFX-ROW# becomes 47 and tools/diagnose-hb-test.f COMMON-N becomes 32. The recovery LPQNAME rows, bootstrap concatenation, and shared native/recovery codegen expectations belong only to the dependent recovery-E2a leaf. Renderer prose, repair guidance, and the existing gate JSON suggestion mapper describe nonempty colon-separated components, not a one-colon grammar; do not add another parser. Tests use real deep sibling package definitions with the same tail and distinct effects, a real deep type-family signature, direct QNAME span/class cases, and production XREF/lifecycle entry points. The existing PPRIM property test receives the exact live QNAME rows. The existing test/load-reject-diag-test.f JSON parser must assert diagnostic fields and exact full tokens structurally; no substring-only JSON assertion. A well-formed missing deep name must remain undefined, while each malformed shape is malformed and optional undefine cannot swallow it. No compatibility spelling, alias, forwarding shim, second scanner, engine-error ABI expansion, namespace kind table, parent/path side table, version, ancestor lookup, using change, bootstrap mirror edit, new lint, or synthetic TRUST row.

Write set: src/core/qname.f, src/core/checker.f, src/core/type-family.f, src/core/render.f, src/habu/xref.f, src/habu/habu2.f, tools/hb-build-lib.f, tools/boot-pin.f, test/boot-pin-test.f, tools/build-fixpoint.f, tools/build-fixpoint-test.f, tools/diagnose-hb-core.f, tools/diagnose-hb-test.f, test/run-files.f, tools/lint/shadow-lint.f, test/engine-suite.f, test/type-decl-suite.f, test/gate-dictionary-lib.f, tools/xref-test.f, test/load-reject-diag-test.f, test/prop-test-core.f, tools/gate-json-assert-core.f, and only necessary current TRUSTED.md removals/rows. Pre-M17 proof is exact ancestry, source census, independent hunk/destruction review, and whitespace/conflict checks only. M17 acceptance: deep sibling identities remain distinct; valid deep word and type names resolve through production checker, type-family, and xref paths; malformed names remain distinct from missing names across XREF, optional and mandatory undefine, hide, and forget without mutation; structural JSON and prose retain the exact full token; and all old copied qualification scanners are absent. Claim: agent=e2a_checked_impl workspace=.jj-ws/habu-resolve-checked-full-9e0d9ac2.
