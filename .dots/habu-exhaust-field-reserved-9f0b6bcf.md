---
title: Exhaust field reserved names
status: open
priority: 2
issue-type: task
created-at: "2026-07-17T12:12:34.974497+02:00"
blocks:
  - habu-isolate-field-valid-16a9e2ac
---

Problem: the production `PF-RESERVED?` policy has sixteen canonical field tails whose real declaration coverage proves only `field` and `make`, plus four semicolon-prefixed comparisons that cannot reach reserved-name policy. Owned result: `src/core/type-family.f` and `test/type-decl-suite.f` only. Delete the unreachable `;variant`, `;sumtype`, `;enum`, and `;product` comparisons. Add one normal `PRODUCT ... FIELD ... ;PRODUCT` negative per exact reachable tail, mapping `frsv00` through `frsv15` respectively to `variant`, `typefamily`, `sumtype`, `enum`, `product`, `field`, `policy`, `derive`, `make`, `unmake`, `tag`, `eq`, `hash`, `order`, `encode`, and `decode`. Every row executes through the normal declaration transaction and rejects `E-PF-NAME`; `TDT-NEG` proves complete registry rollback. Separately use `frsv16` through `frsv19` to prove `;variant`, `;sumtype`, and `;enum` in field-name position reject at canonical spelling with `E-TFAM-CASE`, while `;product` is consumed as the live product closer and the incomplete field clause rejects `E-TDECL-SYNTAX`. Prove uppercase `Make` and mixed-case `deCode` reject `E-TFAM-CASE`. Put all new declarations and test words in a dedicated `FIELD-RESERVED-TEST` package; do not add globals. Add public product `frsvok` with adjacent lowercase nonreserved fields `variantx` and `decoded`; compile its generated `MAKE`/`UNMAKE` through the checker and execute one scalar round trip proving both fields retain declaration order. Add a Habu-native structural source-census assertion that the policy contains exactly the sixteen reachable canonical tails and no semicolon-prefixed comparison; it must inspect parsed definition structure, not raw substrings. Do not copy or call `PF-RESERVED?`, add a toy validator, expose the policy, use substring checks, or test the raw registry seam. Acceptance: mutating any one of the sixteen reachable production comparisons to false makes its exact declaration row fail; restoring a semicolon comparison fails the structural census; changing canonical validation or closer recognition changes the named error. Verify the exact `test/type-decl-suite.f` production load, type-family and generated-declaration transaction suites, typed-local and package diff lints, and host lint. Package owners: the existing type-family policy and the new test-only package. This leaf remains open until blocker `habu-isolate-field-valid-16a9e2ac` lands.
