---
title: Structured kernel-ABI record as single source of truth
status: active
priority: 2
issue-type: task
created-at: "\"2026-07-16T16:03:11.356311+02:00\""
---

Capability dot minted from the kexport BLOCKED report (habu-kernel-artifact-export-0e98d259): the kernel param ABI is hand-encoded THREE times with no shared structure - (1) checker KERNEL: parse is baked into the engine and unexposed (REND-SIG is not a live word in bin/hb, probe-proven E-UNDEFINED; bootstrap/cg/forth.fs:2262), (2) lib/ptx/cg.f:57-69 CG-ENTRY/CG-PARAMS hardcode the .visible .entry string and every ld.param literal, (3) tools/ptx/cuda-launch.f:72-76 hand-codes total size 24 and offsets 0/8/16/20 plus block 256,1,1. This is docs/ptx-sketch.md Resolved Decision item 3 implemented ad-hoc instead of as a structure. Work: add a checked, package-scoped kernel-ABI record populated by the ptx codegen machinery as it runs - per kernel: name; ordered logical params (kind span|matrix|uniform, elem type, extent token(s), optional align); derived flat .param layout (offset, size, .param PTX type, role, source param|launch-derived|static|dense-derived) with equal-token dedup (SAXPY's two extent-n spans -> one p_n); block + grid derivation. Data-drive CG-ENTRY/CG-PARAMS from the record (kernel name parameterized, cg.f:58) so PTX text and record share one structure; record the GRID: derivation token instead of discarding it (lib/ptx/header.f:42-46 PTX-SKIP-ONE parse-name 2drop -> a PTX-GRID$ accessor); expose sm target / ptx version accessors instead of literals (src/arch/ptx/emit.f:34-37); generate cuda-launch.f's offsets/size from the record, killing the triplication. Acceptance: PTX output byte-identical for ALL existing kernels (saxpy, fold/collective, mma modes, fusion) proven by capture+compare before/after; cuda-launch offsets generated == old literals (pinned test); GRID: token recorded and queryable; all ptx suites + tools/ptx tests green; typed-local/host/filemap lints clean; no new TRUSTED sites. Files: lib/ptx/cg.f, lib/ptx/header.f, src/arch/ptx/emit.f, tools/ptx/cuda-launch.f, new lib/ptx/kernel-abi.f + kernel-abi-test.f, FILEMAP.md. Ownership: ptx codegen metadata. Unblocks habu-kernel-artifact-export-0e98d259 (manifest derives from this record).

Claim: agent=kexport workspace=.jj-ws/fable-kexport (lane continues from the export dot BLOCKED report; capability lands first, export re-opens behind it)
