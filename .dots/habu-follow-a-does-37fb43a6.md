---
title: "Follow a does> child's tail branch into the stripped closure"
status: active
priority: 2
issue-type: task
created-at: "2026-09-18T20:28:01.447723+03:00"
---

Problem: a does>-created word reachable from MAIN cannot be shaken into a stripped image: hb-build refuses every such program with 'aot: PC-relative target removed or outside closure' (rc 74). Reduction: ': CONST: ( n -- ) create , does> ( -- n ) @ ; 42 CONST: FOO : MAIN ( -- ) FOO . cr ;' built stripped dies; the same source without the does> word (create FOO 42 , : MAIN FOO @ . cr ;) builds and prints 42, and the same does> source built as a REPL image runs. Cause: src/habu/habu2.f DOESPATCH:EMIT patches the created body's RET into a plain 'b D' ($14000000) to the parent's ';does' companion record (elaborate.f DOES-NAME), and src/habu/aot-closure.f SCAN-DIRECT (line 670) follows only CALL? (BL, $94000000), so the companion record never joins the closure; aot-lib.f MAP-TARGET! then refuses the B it cannot rewrite. DIRECT? (line 93) already decodes B and BL together. Acceptance: SCAN-DIRECT follows a DIRECT? word whose target lies outside the member being scanned (i CLO-AT .. i CLO-AT + i CLO-BYTES, the span SCAN-MEMBER walks) exactly as it follows a BL - record via FINDADDR-PTR/SCAN-CALLEE, else the span table via SPAN-AT-ENTRY/ADD-SPAN-CLO - while a B whose target is inside the member stays untouched (intra-record control flow); the comment above SCAN-DIRECT states the rule. The reduction above builds stripped and prints 42; tools/hb-build-test.f gains BUILD-AOT-SPAN, the stripped twin of BUILD-REPL-SPAN (same source, same expected output '64\n\nspan\n16\n\n', built through the stripped route the BUILD-AOT-* cases use), and BUILD-REPL-SPAN's 'THE IMAGE IS NOT STRIPPED' paragraph is replaced by one sentence naming the stripped twin; a stripped does> case whose clause is empty (elided, no patch) still builds. src/habu/** is baked: rebuild through tools/native-build.f from a tree copy with a private host engine (docs/bootstrap.md), never build-fixpoint install --force; gen2==gen3. Files: src/habu/aot-closure.f, tools/hb-build-test.f. Verify: bin/hb --load tools/hb-build-test.f; bin/hb --load test/run.f (full suite green). Depends: none. Ownership: src/habu/aot-closure.f, tools/hb-build-test.f BUILD-AOT-SPAN/BUILD-REPL-SPAN. Claim: agent=hazel workspace=.jj-ws/hazel-aot-does
