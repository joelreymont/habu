---
title: Owner-wid live leg hangs after a dev snapshot run
status: open
priority: 2
issue-type: task
created-at: "2026-07-30T15:58:07.017781+02:00"
---

REPRODUCIBLE, measured 2026-07-30 on .jj-ws/habu-declare-persisted-cb-b150b5d5 at commit 'Declare persisted callback table cells'. Recipe, all four steps in ONE HB_TMP root: bin/hb --load tools/build-fixpoint-refresh.f -- install --force; then the same with -- snap; then run $HB_TMP/hb-new once on any input; then HABU_OWNER_WID_HARNESS=1 bin/hb --load test/owner-wid-child.f. The child's live-image leg -- $TMPDIR/habu-owner-wid-image-*/hb-snap-owner-live --load test/owner-wid-state.f -- produces zero bytes of stdout and stderr and is killed by the harness's own 120000 ms timeout, so asserts 44 and 45 fail and test/owner-wid-internal.f reports three failures with 14 bytes on stderr. CONTROLS, each measured: the same child run in the same root immediately BEFORE the hb-new step passes; three runs in freshly created empty roots pass; a root with only install --force passes; a root with only -- snap passes; a root with install --force and -- snap but no hb-new run passes; and the three runs that follow the failing one in the same root all pass. So the trigger is executing the dev snapshot engine from that root, and it costs exactly the first child run afterwards. WHAT IS RULED OUT: running hb-new changes nothing in HB_TMP (directory listing and per-file MD5 identical before and after) and does not touch the global fixpoint stamp at ~/.cache/habu-fixpoint/stamp (same MD5, same mtime). test/owner-wid-image.f BUILD makes its own private root with TMPDIR-MKDIR and points the build's HB_TMP at it through BF-TMP!, so on the face of it the shared root should not reach the child's build at all -- which is the part that needs the debugger rather than more guessing. Leftover partial $TMPDIR/habu-owner-wid-image-* roots exist on this machine holding hb-stdin/hb-stage/hb-snap-src but no hb-snap-owner-* image, which is what a failed run leaves behind and may also be what a later run finds. WHY IT SURFACES NOW: before dot habu-declare-persisted-cb-b150b5d5 the live leg died rc 134 immediately on every run, so the path could not hang; it now runs, and this is a previously unreachable behaviour rather than a regression from working to broken. No gate creates the trigger -- the suites use private HB_TMP roots and none of them runs the dev snapshot image -- and test/owner-wid-internal.f is green in every private root it was given. Start with the timeout: attach to hb-snap-owner-live under the harness (docs/debugging.md) and find out what it is blocked on, since zero bytes on both streams points at a block before any output rather than a slow run. Acceptance: the recipe above no longer hangs, the mechanism is named in the dot with debugger evidence, and a regression covers it.
