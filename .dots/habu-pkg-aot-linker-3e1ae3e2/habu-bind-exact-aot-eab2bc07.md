---
title: Bind exact AOT entry
status: open
priority: 1
issue-type: task
created-at: "2026-07-23T22:00:07.249230+02:00"
blocks:
  - habu-pkg-aot-closure-e8852de1
---

Why: AOT entry selection scans dictionary tails from the first record. It can select a package-private MAIN ahead of the global program entry, select a retired MAIN after undefine and replacement, and accept a program whose only MAIN is private. The linker must use the language resolver and the requested identity, not a name scan. Dependency: AOT-LINK must own the linker, maker, and closure; the complete positive and negative AOT test packages must already be landed. Owner and interface: private AOT-LINK entry-selection code replaces FINDMAIN and MAIN? with FIND-ENTRY. FIND-ENTRY obtains ENTRY-NAME$ and calls XREF-FIND, returning that exact live dictionary record or XREF-NULL. No public API changes. Unqualified MAIN therefore resolves only the latest live global wordlist-0 MAIN. A qualified package entry resolves only that package public wordlist. Unqualified package public, package private, namespace, retired, and missing records never satisfy a global entry request. Delete dead MAIN?; retain REC-NAME= only for unsupported-word classification. Update generated-source observations to use the production XREF-FIND path. Update the existing preseed fetch entry from HLP to AOT-LAYOUT-FETCH-BAD:HLP because it intentionally selects a package-public helper. Preserve zero net maker data-space allotment before READ-PROG and prove byte-identical output for a data-bearing program. Files: src/habu/aot-closure.f, test/gate-aot-positive-lib.f, test/gate-aot-negative-lib.f, and exact comments or inventories only if required. Checkpoint: record the current real tools/hb-build.f failures before editing: private MAIN before global MAIN runs the private body; MAIN then undefine MAIN then replacement MAIN runs the retired body; private-only MAIN builds and runs. Run a representative FIND-ENTRY correction through the package gate and baked-reader maker smoke before the full fixture matrix; stop on any unplanned object-format or caller change. Acceptance through the real tools/hb-build.f path: private MAIN before global MAIN runs the global body; a retired MAIN followed by a live replacement runs the replacement; private-only MAIN rejects with rc 74; retired-only and missing entries reject with rc 74 and name the requested identity; a global selected HLP still works; a qualified public entry works through build, object store, object hit, and relink; a qualified private entry rejects rc 74; case folding matches XREF semantics; colon-qualified entry metadata round-trips. The data-bearing golden remains byte-identical and the maker smoke proves top-level qualified calls through the baked reader. Rebase the active owner-WID package change containing private ENGINE-BUILD:MAIN and prove tools/hb-build-test.f, its owner-WID AOT build, positive/negative AOT, package, typed-local, trust, fixpoint, and required master gates. Mutation proof: restore the first-tail scan and the private-shadow or retired-record fixture runs the wrong body; force wordlist 0 and the qualified public entry fails; use current lexical lookup and the private-entry rejection or shadow fixture fails. Forbidden: renaming ENGINE-BUILD:MAIN, tail scans, first/latest match heuristics, numeric wordlist persistence, aliases, compatibility globals, broad public test seams, object-format changes, process-isolation work, publication hardening, or claims that package privacy secures untrusted source execution.
