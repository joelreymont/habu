---
title: Prepare checker and dictionary release
status: active
priority: 1
issue-type: task
created-at: "2026-07-22T23:20:42.484606+02:00"
blocks:
  - habu-bound-declaration-participant-9967c968
  - habu-make-event-snapshot-0b239a3a
---

Problem: src/core/generated-declaration.f CHECKER-PREPARE and src/core/generated-declaration-dictionary.f PREPARE are empty, while checker rollback-frame and dictionary depth validation still run in FINALIZE after every participant has committed. A late rejection can therefore occur after publication. Exact contract: the checker rollback-frame owner adds one release-readiness query covering nonzero core depth plus equal core, type-family, and schema rollback-frame depths through a typed extension hook; the generated-declaration checker PREPARE compares that state with the current coordinator depth and throws E-PARTICIPANT-DEPTH before commit on disagreement. The dictionary PREPARE validates its frame depth against the coordinator depth. Checker, type-family, schema, and dictionary final callbacks then only decrement the already-proved top frame and contain no throw, validation, allocation, lookup, or externally visible publication. Preserve nested transactions and reverse release order. Package or namespace owner: the existing checker rollback-frame core API and GENERATED-DECL-DICTIONARY package; do not add a global forwarding shim or test-only public setter. Acceptance: a focused production GENERATED-DECL:RUN regression corrupts each saved-depth relation through a narrow private test seam, proves PREPARE rejects before any commit/publication and rollback restores byte-identical state, then proves nested clean success releases exactly one frame per owner. Mutations removing each readiness check or moving it back to final release must fail. Files: src/core/checker.f, src/core/type-family.f, src/core/type-schema.f, src/core/generated-declaration.f, src/core/generated-declaration-dictionary.f, focused generated-declaration transaction tests, candidate-validation enrollment only if needed. Verify: the generated-declaration transaction production suite, shared candidate validation, and exact typed-local/package/trust checks. Depends: habu-bound-declaration-participant-9967c968 and habu-make-event-snapshot-0b239a3a. Ownership: checker and dictionary release readiness only. Claim: agent=release_checker workspace=.jj-ws/habu-release-checker.
