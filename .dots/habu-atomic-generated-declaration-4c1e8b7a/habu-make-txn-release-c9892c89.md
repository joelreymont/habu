---
title: Make transaction release total
status: closed
priority: 1
issue-type: task
created-at: "\"2026-07-22T23:21:11.378680+02:00\""
closed-at: "2026-07-26T08:59:33.243402+02:00"
close-reason: "Implemented, reviewed, merged: landed as 80dac3c072d2 (Make declaration transaction release total), an ancestor of master@origin. Participant FINALIZE became RELEASE: total, uncaught, run in reverse order only after every reversible commit succeeded. The structural release inventory that closes over everything the sealed release words reach and fails on throw, catch, allocation, lookup, validation, or publication landed with it and reported ten reachable throw sites on the parent tree, zero after (LESSONS records the method)."
blocks:
  - habu-prepare-checker-and-79a74da0
  - habu-prepare-event-and-3473ec0b
  - habu-use-coordinator-depth-dc1c963a
---

Problem: src/core/declaration-transaction.f models FINALIZE as another fallible participant phase, catches late throws, poisons the coordinator, reports cleanup failure, and can return an error after every reversible COMMIT succeeded. This makes successful publication observably non-atomic. Exact contract: replace the participant FINALIZE phase and callback with RELEASE. REGISTER accepts a typed zero-input, zero-output release quotation. After all reversible commits succeed, RELEASE runs every sealed production participant in reverse order only to discard an already-prepared savepoint. RELEASE is not caught, diagnosed, poisoned, or treated as a recovery phase; each production release callback is total after the checker, event, field, and dictionary prerequisite leaves, and the production participant set remains sealed with registration authority undefined. Rollback failure handling, primary-error preservation, coordinator poison semantics for failed rollback, participant order, nested runs, and telemetry for real failures remain unchanged. Remove obsolete finalization-failure injection and phase vocabulary instead of keeping compatibility aliases. Package owner: DECLARATION-TRANSACTION and GENERATED-DECL; no public raw callback cast or second transaction protocol. Acceptance: the real generated-declaration path proves prepare then forward commit then reverse release order, one release per active participant at every nesting depth, no public visibility before all prepares pass, and no fallible operation in any production release callback. A structural regression inventories the sealed production release definitions and fails if throw, catch, allocation, lookup, validation, or publication re-enters them; mutations restoring fallible finalization or release diagnostics must fail. Files: src/core/declaration-transaction.f, generated-declaration participant adapters, focused transaction tests, public phase users and documentation only where the rename requires them. Verify: generated-declaration transaction, declaration-event, dictionary rollback, structure/enum declaration, snapshot/AOT/fixpoint focused suites, shared candidate validation, and exact typed-local/package/trust/error-code checks. Depends: habu-prepare-checker-and-79a74da0, habu-prepare-event-and-3473ec0b, and habu-use-coordinator-depth-dc1c963a. Ownership: generic release protocol and production adapters only.

Claim: agent=claude-solo workspace=.jj-ws/habu-txn-release-r2. Recorded retroactively at closure; implemented during the solo-orchestrator shift against this then-unclaimed dot.
