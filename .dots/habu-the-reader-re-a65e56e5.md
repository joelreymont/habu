---
title: The reader re-entry refusals
status: open
priority: 2
issue-type: task
created-at: "2026-08-12T20:50:32.016807+02:00"
---

Census 2026-08-12: 9 first-refusals E-NFEED-SCAN - defining words that re-enter the reader (BUFFER:, ENUM+, BEGIN-STRUCTURE shapes). No dot owned the class. Establish whether the chain models defining-word re-entry or refuses it by design; if by design, the cut needs the answer for every tree use. Files: src/compiler/native/feed.f. Depends: none.

RULED 2026-08-14 (design probe report, all measured; probe corpus
/tmp/scan-probe/): the title names a mechanism the tree does not
need. E-NFEED-SCAN's invariant (one unit, one reader scan - a tape
from two scans binds nothing) is CORRECT and stays. The nine census
rows (still exactly 9 after eleven landings; deeper population 12 -
task.f's TASK/+USER/FACILITY hidden behind prot-wid-add) are does>
definitions: TWO ROUTINES WEARING ONE COLON, which the engine
already treats as two (DOESB split, two CHECK-SCANs, pass-2 fails
closed on split bodies). Their ~440 use sites are top-level data
declarations that never reach colon compilation - the cut's
obligation is the 12 definitions, not the sites. Every does> body
already compiles; the definer halves refuse ONLY on the create
axiom defect (below). RULING: option (b-prime) - CONVERT THE 12 to
the LAYOUT-BUFFER shape (parse-name + allot + evaluate-generated
ordinary colon accessors; 388 sites of precedent by the same author
for the same job; evaluate-generated accessors in the three
pre-hook surfaces accepted on layout-buffer.f's existing terms;
load position must not move). DELETE-FIRST: ENUM+/ENUM4+/CFIELD:
have zero call sites tree-wide - the lane deletes them first and
proves it through the full battery before converting the rest.
task.f's three ARE in scope on the structural count. NOT
recommended and rejected: (a) model re-entry (3-4 lanes ending in
the compiler patching code pointers - no consumer found), (c) a
named boundary (the checker certifies all 12 today - nothing here
is inexpressible). feed.f does not change - confirmed as the
intended outcome. SEQUENCING: after habu-lift-the-recorder-d7480352
(the 512-byte recorder cap refuses the layout-buffer shape's own
file today - the converted forms must be PROVED compilable, not
assumed). The census -8401 taxonomy row folds into 477f96d7.
