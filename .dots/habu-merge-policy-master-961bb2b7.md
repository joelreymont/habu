---
title: "MERGE POLICY: master R3 nominal kinds x fable enum families (maki reconciliation)"
status: active
priority: 2
issue-type: task
created-at: "\"2026-07-12T15:00:44.362099+02:00\""
---

Master 441b834e landed Model-CAD V2 R3 (MODEL-CAD-V2-PLAN.md; epic habu-epic-model-cad-70b629a9): package CAD-KIND arity-0 TYPEFAMILY nominal kinds (design/rev/obj/node/analysis/plan/artifact/evidence/target/toolchain/pass/schema ids + dim/shape/rows/cols/dtype/layout/address-space/stage/effect/region) applied across ~50 maki files - on a maki tree that NEVER merged fable, so it lacks fable's family-typed surface (descriptor trio ENUMs, opkind, SKEY, LAYOUT-BUFFER slots, ONNX importer growth, autograd, eval, checkpoint). 50 files changed on BOTH sides since merge base 80a13843. RECONCILIATION POLICY for the master->fable merge (maki is the maki-lane's surface; this dot is the coordination record for tfam so their later fable-merge adopts, not re-derives): (1) Textual merge is hopeless for the semantic core; per file take FABLE content as base and RE-APPLY master's R3 typing as a semantic patch. (2) Identity/index roles fable never typed (all the id kinds + dim/rows/cols/shape/address-space/stage/effect/region): adopt master's CAD-KIND kinds and their call-site typing verbatim. (3) dtype + layout: fable's ENUM families (dtype w/ df-variants + DERIVE eq + MATCH render boundaries; layout likewise) are STRICTLY STRONGER than CAD-KIND:dtype/layout opaque nominals (out-of-range unrepresentable; swaps reject; no raw-n public API - master's TRUSTED: DTYPE-REFINE/DTYPE-RAW universal converter pair violates the V2 plan's own R3 design decision 'do not publish universal n conversion words'). Port master's R3 call sites onto the enums; drop dtype/layout rows from maki/cad-kinds.f with a comment pointing at the enum families. CAVEAT for the recon worker: read master's actual CAD-KIND:dtype usage sites first - if a site is a genuinely different role (registry/schema id rather than element dtype), keep both under distinct names and document. (4) R3 acceptance criteria (cross-role swap rejects, storage preserves family, no raw n public handles, render preserves names, rollback/snapshot/replay retain kinds) must hold on the MERGED tree - keep master's negative fixtures, re-pointed at whichever family owns the role. (5) tfam merge protocol after fable pushes: fable will contain master entirely; merging fable into master is then conflict-free adoption. Blocked by: crash-rca + evaltok merge windows (in flight).
