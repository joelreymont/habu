---
title: Recalibrate cold gate budget
status: closed
priority: 2
issue-type: task
created-at: "2026-07-26T09:01:14.718775+02:00"
---

Problem: the cold-cache budget in test/run-lib.f lines 84-87 (SPARK-COLD-MS and MACOS-COLD-MS both 25000) is wrong for this box, so every cold run of test/run.f reds spuriously while correctness stays green. Four measurements: 30840, 31612 on the unmodified parent, 33571, and roughly 33000 recorded in LESSONS - all against the 25000 cold budget, while warm runs pass consistently against 35000. Required result: recalibrate the cold budget base for the affected profiles or derive the cold budget from the warm one with a measured cold factor, keeping the provenance lines (base, pct, cold flag) intact so a halved budget stays visible. Acceptance: a fresh-HB_TMP cold run of bin/hb --load test/run.f passes the performance verdict on this box on an unmodified tree; the budget calibration test pins the new values; warm budgets unchanged. Files: test/run-lib.f, test/run-budget-cal-test.f. Verify: cold and warm runs of test/run.f plus the budget calibration test. Depends: none. Ownership: cold budget constants and their derivation only. Claim: unassigned.

Amended 2026-07-26 (MODELPROV lane evidence): the maki core slice grew from 14.8 s at calibration time to 21.9-22.6 s today, measured at 22590 ms on the unmodified base 4383174718d4, so the 25000 stop-line cannot fit for ANY tree (forced cold: base 38066, candidate 39945, both hard-fail, correctness green). Record the derivation (measured value, margin, date, slice inventory) beside the constant, add the rule that any change adding a suite to the measured slice re-derives it in the same commit, and coordinate with habu-refuse-perf-measurement-150e80b6 so the recalibration measurement happens in a provably quiet window.
Amended 2026-07-26 evening - THE MECHANISM, measured: the perf verdict counter pre= grew 86 to 88 over one day of landings while e= grew 30-32k to 33-35k on a byte-identical benchmark and engine - the timed phase includes work that scales with the tree (the harness file set grew by 2 units today), but the budget is a fixed constant calibrated against the smaller tree. Every landed suite permanently eats margin; the gate now sits on its own ceiling (idle-box e=35157 vs ceiling 35350) and marginal-fails are coin-flips on the pct wobble. Required fix, in addition to the original scope: the budget derivation must model the scaling input (budget = recorded base + per-unit cost x measured unit count, derivation recorded beside the constants and re-derived by any change that moves the unit count), or the timed phase must exclude tree-scaled work and time only the sha-pinned benchmark. Recalibrate warm AND cold from a quiet-window measurement with the derivation, per-unit table, and date recorded. This dot is now the LANDING-THROUGHPUT BLOCKER. Priority: 1. Claim: agent=perfcal workspace=.jj-ws/habu-perf-recal

RE-FROZEN 2026-07-26 evening (supersedes the recalibration framing - the perfcal checkpoint falsified the unit-count mechanism: pre=/post= are a fixed-iteration calibration spin, and the budget rose slightly with it; the true defect stands proven differently - e= wraps the entire 41-phase gate whose dominant suite grew 14.8s to 25.4s through legitimate landings, and the gate is now DETERMINISTICALLY red on any current tree, stop-the-line for every landing). New contract per the timing ruling: RETIRE the aggregate perf verdict entirely. Decisions: (D1) no total-runtime verdict survives - per-phase timeouts remain as hang guards; a generous whole-gate wall cap may remain ONLY as an infrastructure-hang guard whose failure is clearly not a perf verdict; the budget/attempt/2-of-3/cold-subprocess machinery, its argv, TR-BUDGET-RC 65, and the aggregate emitter all retire as dead code with the verdict. (D2) The perf coverage gap is ACCEPTED for this wave - gate perf coverage becomes the six confined JSON benchmarks; a follow-up dot adds pinned engine/checker/inference benchmarks each with its own budget (minted separately, open). (D3) measurement-impossible (contention-retry exhaustion in a confined phase) exits with a DISTINCT non-zero code, loud, never silently green and never conflated with a genuine perf fail; landing policy is rerun-in-quiet-window. (D4) Order: the contention-refuse commit lands first in the confined JSON phase (its correct owner), the retirement second. NO interim cold-budget bump - explicitly rejected as the margin-eating ratchet the ruling killed. Cleanup scope from the checkpoint survey: test/perf-verdict.f + test, test/run-verdict.f + test, test/run-budget-cal-test.f, run-lib.f lines ~1938-2195, RUN-BUDGET constants, --budget-ms/--wall-budget-ms/--gate-attempt argv, docs/bootstrap.md perf section, FILEMAP rows; every retired suite file removal updates WALKED-FILES/FILEMAP/registration per convention; correctness semantics of the gate untouched (a slow TREE no longer reds the gate - a slow BENCHMARK reds its own phase, proven by the slowed-benchmark mutation).

Closed 2026-07-26: superseded per the RE-FROZEN contract and landed as commit 2265ec9e "Confine perf verdict to pinned benchmarks". The aggregate performance verdict is retired entirely - test/perf-verdict.f, test/run-verdict.f, their tests, run-budget-cal-test.f, the budget/attempt/2-of-3/cold-subprocess machinery, its argv, and exit 65 are deleted; DRIFT-OK? moved to cal-spin ownership; whole-gate elapsed-ms is informational only. Decisions D1-D4 executed as frozen: per-phase timeouts remain as hang guards, the six confined JSON benchmarks are the only performance gates, measurement-impossible exits distinct (CONTENDED-RC 68), the contention-refuse commit landed first. Proof the class is gone: warm rc=0 at elapsed 35429 (red under the old 35000) and forced-cold rc=0 at 38272 (hard-fail under the old 25000), while the slowed-benchmark mutation still reds its own phase rc=1. The accepted coverage gap (engine/checker/inference benchmarks, each sha-pinned with its own budget) is tracked by habu-add-pinned-engine-90090800.
