---
title: Add the span type and its bounded words
status: open
priority: 1
issue-type: task
created-at: "2026-09-18T11:33:02.841208+03:00"
---

Problem: band 0 of habu-bound-pointers (habu-bound-pointers-with-719ba3f4): the type and every word the bands need, with nothing in the checker changed. Acceptance: lib/span.f, package SPAN: the generic family (STRUCTURE span 1 FIELD base ptr a FIELD len n ;STRUCTURE, verified that a ptr a field is admitted in a generic; if not, the two-cell layout family the checker does admit, recorded), SPAN:MAKE ( ptr t n -- span t ) as the one audited crossing with its header stating who may call it, SPAN:LEN, SPAN:$, SPAN:BYTES, SPAN:SKIP, SPAN:TAKE, SPAN:SUB (E-SPAN-RANGE past the reach, no wider span ever), SPAN:AT (bounds-checked element address), SPAN:C@ SPAN:C! SPAN:@ SPAN:! by index, SPAN:COPY ( ptr u8 n span u8 -- ) (E-SPAN-CAPACITY, no partial copy), SPAN:FILL; the producers SPAN-BUFFER: NAME n ( -- span u8 ), n SPAN-CELLS: NAME ( -- span n ), MEM:ALLOC-SPAN ( n -- span u8 ) and MEM:FREE-SPAN in lib/memory.f, and TYPED-BUFFER's span form if the definer family admits it; an E-SPAN block in lib/errors.f; tools/lint/bare-copy-lint.f refusing BYTE-COPY and SPAN:MAKE in checked source outside lib/ and src/ with the site and the replacement named, its fixture registered, and its report over the habu tree recorded here (not gated yet); lib/span-test.f with a red-first refusal for every E-SPAN case, exact-fit and zero-length copies, every narrowing at and past the reach, a hostile length, and the generic instantiation over u8, n and a family element; docs/type-system.md section 'Bounded pointers' (the design in the campaign dot, in the doc's voice, with the measured probes) and one paragraph in docs/forth-card.md; a cost measurement of SPAN:C@ against c@ in a compiled loop so band 3's optimisation has a baseline. Files: lib/span.f, lib/span-test.f, lib/errors.f, lib/memory.f, lib/memory-test.f, tools/lint/bare-copy-lint.f, tools/lint/bare-copy-lint-test.f, test/gate-stdlib-cases.f, docs/type-system.md, docs/forth-card.md, docs/stdlib.md. Verify: the suites; three generations with cmp only if lib/errors.f or lib/memory.f (engine-provided, baked) change — they do, so the fixpoint is required; tools/bootstrap.sh check; test/run.f. Depends: none. Ownership: lib. Parent: habu-campaign-c2-mem-c3d7662b. Claim: unassigned.
