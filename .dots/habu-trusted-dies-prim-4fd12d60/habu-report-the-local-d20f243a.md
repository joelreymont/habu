---
title: Report the local-name length limit accurately
status: active
priority: 1
issue-type: task
created-at: "2026-09-11T16:07:57.885292+03:00"
---

Owner: unassigned; reported by Rowan on BB20260911-125858.119-rowan-0ae4. `: T17 ( n -- n ) {: abcdefghijklmnopq:n :} abcdefghijklmnopq ;` reports E-UNDEFINED for abcdefghijklmnopq:n then ncomp cannot compile T17. Sixteen-byte spelling works; source LOC-NAME-W is16 and checker.f documents that internal limit, public docs/forth.md does not. Maki shortened its name; no Habu fix yet.

Acceptance: reject the17-byte local with a precise length-limit diagnostic before native emission, accepted16-byte control, consistent checked --load/JIT/AOT behavior and concise public limit documentation. Do not raise a fixed buffer without examining its representation.

Reconfirmed on current integrated binary SHA1b965ddf: both tier0 and tier1 accept16-byte typed local and return3; both reject17-byte name with misleading E-UNDEFINED (0.238-0.261s). Current source locations: checker.f LOC-NAME-W9861, overflow guard9987; native local capture habu2.f4270. No fix applied.

Claim: agent=hazel workspace=.jj-ws/habu-report-the-local-d20f243a

Result 2026-09-11 (hazel, lane .jj-ws/habu-report-the-local-d20f243a on 3098ae3e): two defects, both fixed. (1) src/habu/habu2.f C-LBRACE-STORE-ONE copied the whole bare name into the 16-byte LOC-REC name field with no bound: a second local's length cell overwrote byte 17 so the engine itself reported the first local as E-UNDEFINED, and a long name in record 63 wrote past LOCNAMES ($3600) toward REPLH-CELL ($3640). Now layout.f LOC-NAME-CAP (LOC-REC minus its length cell) bounds the store; a wider name branches to LLOCWIDE, prints `hb: local name over 16 bytes: <token>` and takes the LDIAGRET tail (rc 70, catchable inside evaluate); the 65th-local refusal in the same routine takes the same tail with `hb: more than 64 locals in one definition: <token>` (was a bare token and rc 75; layout.f LOC-RECS names the 64). (2) src/core/render.f had no text for LOCALBAD, so the pinned declaration token rendered under E-UNDEFINED (referenced) or bare `at '...'` (unreferenced). checker.f LOC-REJECT now records LOCALBAD-KIND (shape / count / width) and LOCALBAD-LEN, latched to the definition's first failure so the pinned token is never relabeled (independent review finding); render.f emits E-LOCAL-NAME-TOO-LONG `local 'x:n' has a 17-byte name; the limit is 16 bytes` (class shorten_local_name), E-TOO-MANY-LOCALS (reduce_local_count) and E-BAD-LOCAL-SHAPE, ahead of E-UNDEFINED. Tier 0: engine refusal by name; tier 1 (`1 set-tier`): the checker's diagnostic then `ncomp: cannot compile`; both rc 70, 16-byte control returns 3 on both. Docs: docs/forth.md limit bullet, docs/repair-diagnostics.md codes/classes. Tests: engine-suite (width, 64 ok / 65 reject, quotation shape, JSON packet), runtime-regression GE-LOCAL-NAME-WIDTH (top level, first-of-two, last record, eval-catch, control); negative control against the root binary fails at `hb local-width top-level`. Suites on the rebuilt engine: engine-suite only the pre-existing F303/F308, runtime-regression ok, load-reject-diag ok, repair-schema-doc ok. Untested: tools/check.f, test/gate-diagnostics.f and tools/hb-build.f all die on the root before reaching this code (E-TRUST-UNRESOLVED TYPE-RESERVED?, hb-build rc 83), identically with the root binary. Not raised: the limit; a wider field needs the LOCNAMES band moved (64 x 40 bytes would overrun REPLH-CELL) plus the checker's LOCNB buffers, a separate decision. Files checker.f, habu2.f, layout.f are rowan-owned: the commit goes to rowan as a candidate.
