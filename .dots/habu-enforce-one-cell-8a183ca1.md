---
title: Enforce one-cell carrier constraints
status: active
priority: 2
issue-type: task
created-at: "2026-07-31T04:29:57.464481+02:00"
---

Why: carrier NEWTYPE parameters must remain one cell after generic instantiation; width 1 is not proof because unresolved variables report one. Owner: CHECKER owns variable-kind constraints, unification, trail rollback, effect persistence, and signature hook calls; TYPE-FAMILY owns structural family/schema interpretation. Result: preserve TVK-ANY=0 and TVK-RAW=1, add TVK-CELL=2 with an explicit semantic meet ANY<CELL<RAW. Keep one-cell trail entries: tag 2 restores ANY and tag 3 restores CELL, so ANY to CELL to RAW unwinds exactly. CELL-OK? constrains unresolved variables, accepts con/atom/quotation/plain pointer without constraining a pointer pointee, delegates family terms, and rejects all other shapes. Replace RAW-BLOCK? with TVK-BLOCK? and use it after occurs checking in both U-TYPE variable-binding arms. Persist the sole kind in existing EN.B; E-INST restores 0/1/2 and rejects impossible values. Add TFAM-CARRIER-OK-XT and TFAM-CELL-OK-XT hooks. SIG-END-PARAM calls the carrier hook only after exact arity and no prior SGBAD; MK-PARAM remains a raw builder. Define TF-NO-SCHEMA=-1 because schema root zero is valid; initialize and reuse existing TF.SCHEMA-ROOT offset 15, leaving TF.DERIVE untouched. Carrier checking instantiates its stored schema with the existing TFC schema interpreter, then calls CELL-OK?. Structural family checking accepts boxed/niche, checks TK-CELL carrier or absent carrier, accepts a product only when exactly one instantiated field is one-cell, accepts sum/enum only when every variant is payloadless, and rejects evidence/other. Phantom binders and pointer pointees remain unconstrained. Files: src/core/checker.f, src/core/type-family.f, test/type-decl-suite.f. Dependencies: none. Forbidden: numeric max for the lattice, T-WIDTH=1 alone, TFC-CON-CLOSED?, PK-CELL, new type-family record cells, side tables, snapshot versions, duplicated schema interpreters, enforcement in MK-PARAM, grammar, generator, runtime word, compatibility, or new files. Checkpoint before edits: exact census of two current kinds, one arena, one EN.B field, one RAW freshening branch, both U-TYPE arms, the dormant schema-root field, and current failing e7id<e7wide> production source. Pre-M17 proof is source review and exact three-file census only; no bin/hb/load/refresh. M17 acceptance: id<n>, id<r>, and id<one<n>> accept; id<wide2> rejects; id<one<a>> constrains a; phantom<wide2> and ref<wide2> accept; both U-TYPE orientations reject bypass-built invalid carriers; CELL with ANY preserves CELL and CELL with RAW resolves RAW; trial rollback restores CELL after CELL to RAW; real E-BUILD-EFFECT to E-INST preserves CELL through EN.B; mutation controls cover both U-TYPE calls, carrier hook, EN.B restore, and trail tag 3. Claim: agent=cell_impl workspace=.jj-ws/habu-enforce-one-cell-8a183ca1.
