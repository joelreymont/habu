---
title: Qualified defs leak checker effects under bare tails
status: open
priority: 2
issue-type: task
created-at: "2026-07-09T16:39:26.230309+02:00"
---

Pre-existing checker/engine divergence found by TFAM 9 slice-3 fixtures (fable-tfam12). Evidence chain: (1) ': QQQ:WWW ( n -- n ) 1 + ;' at top level creates TWO checker USIG records - one under the qualified sym (pkg=qqq, PUBLIC, tail www) from the CHECK cert path, one under the BARE-GLOBAL sym ('', GLOBAL, www) - probe: CHECKER-FIND-ACTIVE-SYM + USIG-FIND-OFF-SYM show both records at distinct offsets (660528 qualified, 661040 global). (2) The checker then CERTIFIES bodies calling bare 'www' ('P1 ( n n -- n n ) www' -> -1) while the ENGINE rejects them (': RUN1 ( -- ) 5 www . ;' -> E-UNDEFINED: www) - checker-accepts/engine-rejects divergence, fail-closed at runtime but a checker-semantics lie. (3) WORSE: DO-TOK consults user sigs BEFORE prims, so the leaked bare record SHADOWS core prims for every later checked definition: after any PUBLIC 'SUMTYPE x ... VARIANT swap ...' (generated ctor X:SWAP is a qualified def), plain '( n n -- n n ) swap' REJECTS with 'actual: n x<>' - reproduced with dup/swap/frob variant names; hit by type-match-suite CASE-interleave fixtures until reordered. (4) Localization: 'TRUSTED: QQ2:VV2 ( n -- n ) ;' creates ONLY the bare-global record (TRUST's engine call receives the TAIL-ONLY token in GLOBAL scope) - so the extra record comes from the ENGINE-side record call that runs AFTER C-QUALIFY-DEF (habu2.f 1685+) rewrites TKA/TKL to the tail for C-STORE-NAME; the original qualified spelling is preserved in DEF-TKA-CELL/DEF-TKL-CELL (stored at 1689-1690). Suspected fix: the engine's post-qualify checker-record call(s) (TRUST path + the ':' publish-tail record call producing the duplicate) must use DEF-TKA/DEF-TKL (original spelling) not TKA/TKL, + stage0/Gforth mirror parity + negative regressions: bare tail must NOT certify after (a) plain qualified def, (b) TRUSTED qualified def, (c) generated constructor (bare variant name must stay unresolvable per item 8 acceptance 'not publishing bare OK'); qualified TRUSTED: words must resolve QUALIFIED (today their only record is the bare-global one - qualified lookup of a TRUSTED: PKG:TAIL word likely finds nothing, latent because the tree has few/no qualified TRUSTED defs). Fix needs fixpoint rebuild + full gate. Until fixed, suites declare word-named-variant families AFTER fixtures that use those names as bare prims (type-match-suite mwv block, type-ctor-suite zwv block carry dot-citing comments).
