---
title: seed build-mode source list omits bytes.f and include.f
status: closed
priority: 1
issue-type: task
created-at: "2026-08-22T23:04:09.644365+02:00"
closed-at: "2026-08-22T23:58:57.612143+02:00"
close-reason: implemented, reviewed, merged, gates green: src/core/bytes.f and src/core/include.f join SRC_COMMON at boot-pin's positions, the dead snap.f conditional is gone, and bootstrap-mirror-lint checks boot-prefix membership in the seed through the real lexer with three fixtures (4be7d815, integrated as 63c6e867, landed on master 85fac4cb); the lint names exactly the two rows on the pre-fix script and 0 after; full recovery 'bootstrap OK: bin/hb' rc 0 on the Linux host.
---

Problem: tools/bootstrap.sh emit_src concatenates the stage2 source from two lists (the SRC_COMMON array and the explicit 'cat src/core/...' rows); tools/boot-pin.f:148 lists src/core/bytes.f in the native prefix and the mirror's own cold-prefix rows (forth.fs:1979) carry it, but neither seed list does, while four seed sources call BYTE-COPY (src/habu/aot-ident.f PATH+ at :90, habu2.f twice, aot-file.f and src/core/include.f in the stdin/snap emissions; a 2026-08-22 reading blamed engine-size.f, which defines its own byte words). Measured 2026-08-22 on the integration of 9269e3a3 + 2c16821e: every Gforth-side gate passes, then hb-stage0 dies at tools/bootstrap.sh:427 (its boot; line 425 is the Gforth compile, which succeeds) with stderr 'BYTE-COPY' and rc 70 (a bare-token die, see habu-twelve-native-die-a7a5139d). The only other native prefix row absent from the seed is src/core/internal-mark.f, referenced by nothing in the seed (inert; recorded on habu-mirror-prefix-rows-49994263). Nothing checks the seed lists against boot-pin: bootstrap-mirror-lint reads SRC_COMMON only for directory membership. SECOND ROW (lane measurement 2026-08-22): with bytes.f added the same boot dies one row later, stderr 'REQUIRE-REG:COUNT' rc 70 - src/core/lower-cert-seal.f:97 (the last SRC_COMMON entry) probes package REQUIRE-REG, defined in src/core/include.f (boot-pin row 27, tools/boot-pin.f:153), which emit_src cats only inside 'if [[ "$driver" == "src/habu/snap.f" ]]' (tools/bootstrap.sh:265-268), a branch no call site reaches (the script's two emit_src calls pass stage2.f and stdin.f). Probe: splicing include.f after src/os/env-base.f into the emitted seed makes hb-stage0 boot and write stage2-got (2,195,648 bytes). Rulings: include.f joins SRC_COMMON after env-base.f in this leaf; the dead snap conditional is deleted; the lint counts a cat row only at if/fi depth 0 inside emit_src (a driver-conditional row is not in the seed) and must name exactly include.f and bytes.f on the pre-fix tree. Order is NOT checked (boot-pin order is the boot-reload order, the seed's is a compile order; they differ on include.f and script-argv.f by design, and the seed's own build refuses a wrong order loudly). Acceptance: src/core/bytes.f (after roles.f) and src/core/include.f (after env-base.f) in the SRC_COMMON array; the dead conditional gone; tools/bootstrap-mirror-lint.f gains a structural membership check, through the real lexer on tools/boot-pin.f and a real parse of the script's two lists (array + depth-0 cat rows, spliced at the for line), that every boot-pin src/ row appears in the seed source, with a named exception table carrying a reason per row (internal-mark.f today) and a fixture that removes a row and reds; hb-stage0 boots past the prefix; the full HABU_ALLOW_BOOTSTRAP=1 tools/bootstrap.sh run reaches 'bootstrap OK: bin/hb' or stops at a later, separately owned failure reported with its exact line. Files: tools/bootstrap.sh, tools/bootstrap-mirror-lint.f, tools/bootstrap-mirror-lint-test.f. Verify: the lint's test with the fixture; the full recovery run. Depends: 9269e3a3 and 2c16821e (base = their integration). Ownership: recovery chain. Claim: closed (landed on master 85fac4cb).
