---
title: Prove migration neutrality in ownership gate
status: active
priority: 2
issue-type: task
created-at: "\"2026-07-23T09:09:26.122384+02:00\""
---

Teach tools/package-diff-lint-core.f the proven-migration rule so incremental packaging is possible without exemptions. Why: two lanes (LEX packaging, MMADT slice) proved requalifying calls inside pre-existing unowned globals reds the gate, and the migrations are atomic, so the incremental packaging strategy is structurally blocked; exemptions and aliases are forbidden by standing rulings. Rule (frozen, cross-orchestrator concurrence): a pre-existing unowned definition may survive only when its header, stack effect, declaration role, and lexer token stream are unchanged except reference tokens replaced per an explicit old-global to new-qualified-public mapping proven by the SAME diff - each mapped old global removed by the diff, each new qualified word defined public under a real package in the post-image, every replacement preserving token order/count/role; prefix stripping only through the proven mapping (LEX-FOO to LEX:FOO covered). Stays red: comments/strings/forged text, unmapped qualification, qualification to a pre-existing unrelated public, provider/caller body edits beyond mapped replacements, new unowned definitions, mixed edits. Owner: package PACKAGE-DIFF in tools/package-diff-lint-core.f (changed-definition classification, unpackaged branch + replay twin). Write-set: package-diff-lint-core.f + package-diff-lint-test.f. Dependencies: none (self-contained synthetic fixtures on this base); LEX migration, held lint stack, and MMADT resume after it lands. Acceptance: red-first through the real gate command - an isolated LEX one-core sample and the preserved MMADT 12-caller diff red on base and pass with zero findings under the rule; direction-preservation fixtures stay red (generic body edit, unmapped/unrelated-public qualification, alias retention, private target, mixed edit, order/count/role change, comment/string fake); mutation liveness on the removal/public/exact-stream checks; both diff lints exit 0 on the leaf artifact. Smallest owning-path check observing the pre-change defect: the preserved MMADT diff run through bin/hb --load tools/package-diff-lint.f reds 12 E-PACKAGE-OWNERSHIP findings on base.
Claim: agent=lexer-worker workspace=.jj-ws/paren-lint-fix
