---
title: Cut colon compilation onto the checked chain
status: open
priority: 2
issue-type: task
created-at: "2026-08-05T10:36:19.611694+02:00"
---

CG-01, phase 4 of the hard cut. src/habu/habu2.f:7020 still calls COMPILE-EMIT:EM-COMPILE; tools/bootstrap.sh:69-103 loads no src/compiler modules; strings bin/hb contains none of NMIGRATE/NREACH/IR-CTX/A64SEL/NPUB. Make checked HIR plus the native pipeline the sole compiler for normal colon definitions: complete every language capability the current tree requires (see the open capability dots: spill support habu-spill-for-real-791367e4, multi-word calls habu-let-the-chain-f28711ba, typed-local rebinding habu-rebind-a-typed-b2a3e369, local address habu-take-the-addr-18a38b4f, trapping arithmetic habu-lower-trapping-arithmetic-5f514ffe, data-word addresses habu-resolve-a-data-a1c8067f), route bootstrap and fixpoint through it, prove self-hosting to a byte-identical fixpoint, and run every gate (language, AOT, image, REPL, debugger, profiler, maki, inference) on the sole path. An opt-in migration hook is not completion. Reconcile with habu-cut-over-staged-070d68c8 and habu-self-host-staged-520ab588 — extend or close those rather than duplicating. Blocked by the bulk-window publisher dot and the phase-2 safety dots.

ADDED REQUIREMENT (2026-08-05, from habu-split-call-crossed-6eda1613 generation 5): the sole-compiler pipeline must support a pressure retry — attempt normally; on E-A64RA-SPILL re-elaborate with the call-crossing split directive (the one-line CROSS-L suppression, proven: flag on compiles CALL-PRESSURE with bit-identical answers) and re-run the chain. The bridge could not host this (evaluation and publication fused in EV); the cut's pipeline must separate compile-attempt from publish so a refused attempt costs nothing. Five generations of design evidence live in the split leaf.

SCOUT RESULTS (2026-08-05, read-only recon; supersedes parts of the list above): (1) habu-let-the-chain-f28711ba is DONE and closed — drop it from the prerequisites. (2) The pressure-retry requirement is ALREADY SATISFIED BY DESIGN: E-A64RA-SPILL throws inside A64RA:ALLOCATE strictly before NPUB:REPUBLISH (migrate.f WORK :575-591, EMITTED :568-573), and the publisher's VALIDATE/COMMIT split means a refusal moves nothing — the retry is catch-set-CROSS-L-flag-re-enter ABOVE WORK; restate as confirm-and-test (verify arena/binding give-back on the refused attempt per migrate.f:592-600). (3) EM-COMPILE remains the sole production entry, habu2.f:7020, no drift; COMPILE-EMIT package opens :6216/:6988. (4) Seed closure: derive transitively from migrate.f (ir/* before native/*, migrate last); NREACH is NOT in the closure — the strings-check list is wider than the compile path; decide the seed by closure, not directory. (5) THE hard capability blocker: data-word addresses (habu-resolve-a-data-a1c8067f). (6) Two whole-tree probes before committing to the cut order: does any tree definition certify under CNUM-OVERFLOW:TRAP reaching hir.add/sub/mul (E-A64SEL-TRAP would refuse it — probe by migrating a trapping word), and does any tree definition use to/^ on typed locals (the rebind/addr dots' corpus-based 'not a blocker' verdicts do not cover the whole tree the cut compiles).

DISCOVERED CUT PREREQUISITE (2026-08-05, remat lane): A64SPILL:REWRITE is NOT on the production path — migrate.f EMITTED runs SELECTED -> ALLOCATE -> ACCEPT -> EMIT and never rewrites, so today the allocator fits or refuses, and refusal is survivable only because the old emitter is the fallback. Post-cut there is no fallback: the sole-compiler pipeline MUST wire the spill rewrite loop (allocate -> if spills planned, A64SPILL:REWRITE -> re-validate -> emit), which is what CG-28's 'complete spill support before cutover' concretely means. The pressure-retry (CROSS-L split) and the rewrite loop are the two halves of full-language pressure handling.
