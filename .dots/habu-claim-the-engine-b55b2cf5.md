---
title: "Claim the engine's path scratch for stripped images"
status: open
priority: 2
issue-type: task
created-at: "2026-09-21T14:58:35.301188+03:00"
---

Problem (aspen, Tender 2026-09-21, reproducer ~/.cache/tender/habu-gaps/stripped-engine-data/open-path.f: s" /dev/null" PATH0 0 0 open): src/core/util.f:91 'create PZB PATH-CAP 1 + allot' is the shared scratch PATH0 zero-terminates every path into; it is allotted while the engine is built, so it sits below the capture window and PATH0's code spells its address, and aot-closure.f DATA-ADDRESS! refuses it: 'aot: address refers to data outside the restored span caller=PATH0 region-off=3151320 value=13962159520 target=PZB'. A stripped image therefore cannot open a file by path - every Tender entry stops here (standalone) or at an earlier site the linker cannot name (server/scraper: 'caller=<unknown> region-off=3918416 value=13964713400 target=<unknown>', an interior pointer or a recordless member). Acceptance: PZB claimed FRESH in src/habu/aot-owned-cells.f (scratch: zeros are right, PATH-CAP+1 bytes, no per-cell facts beyond the length) with the reproducer as a tools/hb-build-test.f fixture that opens /dev/null from a stripped image; the server/scraper site identified from the notes' logs (~/.cache/tender/habu-gaps/stripped-engine-data/notes.md, read-only) and claimed by name the same way if it is scratch, or refused with a named cell if it is state; REFUSE-DATA-SPAN names a recordless or interior site by the nearest record below the caller's code address and its offset (caller=NAME+off) and the target by the nearest declared claim or dictionary record below the value, so the next wall can be read without a debugger; HBT-SIZE-AOT still accounts exactly. Files: src/habu/aot-owned-cells.f, src/habu/aot-closure.f (REFUSE-DATA-SPAN and its JSON twin), tools/hb-build-test.f, docs/native-applications.md (the claims table). Verify: tools/hb-build-test.f, the stripped suites, test/gate-aot-image.f, three generations, test/run.f. Depends: habu-map-declared-data-8c80fc90 (same files, in flight). Ownership: hazel (src/habu). Claim: unassigned.
