---
title: Parse structure destructure policy
status: open
priority: 2
issue-type: task
created-at: "2026-07-26T20:49:58.437906+02:00"
blocks:
  - habu-store-structure-destructure-8c20c92a
---

Problem: STRUCTURE declarations cannot select the registry destructure policy. Required syntax: an optional header clause DESTRUCT public|owner before the first FIELD; omission means public. The clause may appear once. owner requires a real non-global declaring package and a structure with at least one field; global, duplicate, missing, unknown, late, zero-field, and mixed-legacy forms reject at the offending token and roll the entire declaration back. Live parsing, DECL-EVENT, replay, verify-source, diagnostics, and docs/type-families.md grammar must carry the same code; substring scans or a replay-only side table are forbidden. No generation or call behavior changes in this leaf. Owner: STRUCTURE front end and declaration event/replay parity. Dependency: habu-store-structure-destructure-8c20c92a.

Exact insertion points, verified against the tree on 2026-07-28 so the implementer does not re-derive them. The template to copy is the existing POLICY clause, which is the same shape end to end. (1) Dispatcher: `CLAUSE` at `src/core/structure-decl.f:365-374` is a token ladder — `;structure`, `field`, `policy`, `derive`, then reject "unexpected token in structure declaration" at the offending token. A `destruct` arm goes beside `policy`; the existing final reject already gives the mixed-legacy and unknown-token behavior this leaf requires, so it must not be duplicated. (2) Clause body: copy `POLICY-CLAUSE` at `:285-289`, which is exactly `HEADER-ORDER` then `SD-NEXT` then a name-to-code word then the registry write then the event emit. `HEADER-ORDER` at `:276-278` already supplies the before-the-first-FIELD rule by rejecting on `SEEN-FIELD`, so the ordering requirement needs no new mechanism. (3) Name-to-code: copy `POLICY-CODE` at `:280-284`, which rejects a missing name and an unknown name with distinct messages; the codes are the landed `TD-PUBLIC`/`TD-OWNER` from the storage leaf, and the registry setter is `TFAM-DESTRUCT!`, which already enforces frame ownership and rejects invalid codes. (4) Duplicate rejection is NOT in the front end: it lives in `src/core/decl-event.f`, where `DEV-POLICY` at `:440-444` tests `DEV-CUR-HAS-KIND?` and throws `E-DEV-DUP-POLICY` (`7163`, declared `:105`). A `DEV-DESTRUCT` needs its own kind constant beside `DEV-K-POLICY 3`/`DEV-K-DERIVE 4`/`DEV-K-VARIANT 5` at `:84-86`, its own duplicate code from the error-code registry, and a public entry beside `POLICY` and `DERIVE` at `:762-763`. (5) Grammar docs: the clause list is `docs/type-families.md:133-139` with the ordering rule at `:163-164`; the `POLICY` reference section is §22.0 at `:2347` and `DERIVE` is §9.3.1/§9.3.2 at `:985`/`:1062`, so `DESTRUCT` needs its own section rather than an existing slot. (6) ENUM is out of scope for this leaf — `src/core/enum-decl.f` carries a byte-for-byte parallel `POLICY-CODE`/`POLICY-CLAUSE`/`HEADER-ORDER` at `:311-324` and its own dispatchers at `:390-395`, `:419-426`, `:499-508`, and `owner` is meaningless for a sum family; the reject path must therefore refuse `destruct` in an ENUM declaration rather than silently ignoring it. Acceptance: structure suite hostile fixtures cover comments, strings, duplicate clauses, reordering, wrong package role, replay parity, exact diagnostics, and byte-identical rollback through real bin/hb declaration paths.
