---
title: Check owner-only structure destructure
status: open
priority: 2
issue-type: task
created-at: "2026-07-26T20:50:10.078971+02:00"
blocks:
  - habu-seal-pkgs-owning-139e660f
---

Problem: owner-only structures need an in-package inverse without publishing a callable destructor. Required checker form: destructure family consumes one owner-policy product value and produces its declaration-order field row. family is an operand token resolved only while the original, not-yet-closed owning package block is active; qualified foreign spelling, later package reopen, using imports, globals, PUBLIC-policy products, ENUM families, unknown families, and missing operands reject with named diagnostics on the real token. The checker derives fields and generic substitution only from committed TFAM/TYPE-FIELD metadata and preserves whole-bundle linear accounting. This is a reserved form, not a dictionary word, xt, public raw cast, caller-name heuristic, or test friend. Owner: checker parsing/effect and negative fixtures only. Dependency: habu-seal-pkgs-owning-139e660f.

Template, verified against the tree on 2026-07-28: `construct family variant` is the landed precedent for exactly this shape and must be copied rather than reinvented. Its design note at `src/core/checker.f:8870-8879` states the ownership predicate in the same words this leaf needs — "the family in the ACTIVE package only (the ownership predicate — cross-package construction never resolves)". The token machine is `CONM`/`CONFAM` at `:8880-8881`, `CONSTRUCT-BEGIN` at `:8883-8884`, `CONSTRUCT-TOK` at `:8890-8900`, dispatched from the body scan at `:8954` and `:8956`. It is fail-closed on truncation: `CONM @ 0 <> IF MD-CON-TRUNC MDIAG! THEN` at `:9534` and `:10001`, with `CF-FAIL` at `:9556` and `:10006`, and named diagnostics `MD-CON-FAM`/`MD-CON-KIND`/`MD-CON-VAR`/`MD-CON-TRUNC` at `:8050-8053`. The registry side is `TFAM-CONSTRUCT-FAM` at `src/core/type-family.f:2405-2409`, whose first act is `TFAM-ACTIVE-PKG$ na nu TFAM-FIND-IN` — an exact-package lookup that is the ownership check itself, followed by a kind check that rejects the wrong family kind with its own diagnostic. Installed at `:2587-2588`; resolver hooks `CONSTRUCT-FAM-XT`/`CONSTRUCT-STEP-XT` at `checker.f:467-468` with fail-closed defaults at `:651`.

The destructure sibling therefore needs: an exact-package `TFAM-FIND-IN` lookup (not `TFAM-SIG-RESOLVE`, which is signature scope and resolves across packages — the two are already distinguished in the lowering surface at `type-family.f:2543-2553`), plus a policy check that the family reads `TD-OWNER` via `TFAM-DESTRUCT@`, plus a kind check that it is a product rather than a sum. The second precedent, for a reserved operation inside a sealed armed window, is `field-project` at `checker.f:8912-8944` with its registry side at `type-family.f:2519-2522` — that is the model for "a reserved form, not a callable word, xt, public raw cast, caller-name heuristic, or test friend".

Do NOT template this on the linear machinery. `CT-ADD-LINEAR` at `checker.f:2654-2656` and the `LIN-*` family are an obligation-count discipline; they can express that a value is consumed exactly once but cannot express "only inside this package", which is the entire property this leaf exists to enforce. Acceptance: in-owner concrete and generic cases certify; every bypass above rejects; a proof field can be recovered only inside its original declaration block; CHECK!, checker replay, type-linear, and structure suites pass.
