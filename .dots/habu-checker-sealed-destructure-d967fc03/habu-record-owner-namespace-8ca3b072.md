---
title: Record owner namespace
status: open
priority: 2
issue-type: task
created-at: "2026-07-30T05:52:17.762153+02:00"
blocks:
  - habu-delete-runtime-pkg-a21b0679
  - habu-reject-owner-pkg-938a7d15
---

Problem: XREF has no rollback-safe record that says a package owns construction. Result: package XREF-OWNER in `src/habu/xref.f` owns `REC? ( ptr a -- bool )`, true exactly for a live namespace record with WID -1, DNAME-INT set, and zero start and length; `NAME? ( ptr u8 n -- bool )`, which folds the name and finds that record; and `WID? ( n -- bool )`, which joins a public or private WID to the earlier canonical namespace and tests its name. Package OWNER-GUARD owns the native builder mirrors. This leaf adds `REC? ( -- )`, emitted with a dictionary-record pointer in x9 and boolean output in x13, and `NAME? ( -- )`, emitted with token byte pointer x9, token length x10, and boolean output x13; bootstrap uses the same builder path. Ordinary namespace lookup calls XREF-OWNER:REC? to skip markers and continue to the canonical record. Before native ;package clears checker state, query the existing type-family owner flag and preflight one record. Call CHECKER-END-PACKAGE; only after success, write WID -1, DNAME-INT, zero roles, and the canonical name representation: copy inline bytes or reuse the canonical DNAME-EXT pointer and length. Publish NDICT after the full write, then clear engine package state. Repeated same-name marker rejects before mutation; unowned close appends nothing. Recognition bit-tests DNAME-INT, so monotonic immediate or wide marking cannot erase it. Snapshot rebasing preserves shared external pointers. Compact AOT round-trips inline markers; long names remain outside its existing DNAME-EXT input domain and gain no fallback here. Owner: XREF-OWNER record identity, OWNER-GUARD record/name mirrors, native package close, and focused persistence tests. Production red: closing a directly flagged package leaves no marker or classifier. Acceptance: one canonical namespace is followed by one marker; the three XREF-OWNER predicates and two OWNER-GUARD mirrors agree on marked, unmarked, mixed-case, inline-name, external-name, public-WID, private-WID, unrelated-WID, and INT ordinary-word controls; qualified lookup still resolves through canonical WIDs; injected checker-close or capacity failure produces no marker; source and snapshot preserve inline and external names, while AOT and fixpoint preserve every in-domain marker. Forbidden: OWNER-WID state, PROT-WID enrollment, copied external name, duplicated WID roles, unowned native global, independent predicate, HIDX redesign, AOT DNAME-EXT work, WID rebinding, trailer, version, reader, capacity table, compatibility path, or lint. Smallest owning check: close one inline-name and one long-name directly flagged package, prove predicate/mirror agreement, resolve each public word, snapshot both, and AOT the inline case.
