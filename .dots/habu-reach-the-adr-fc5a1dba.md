---
title: Reach the ADR a stripped Tender image cannot
status: open
priority: 2
issue-type: task
created-at: "2026-09-22T07:15:31.649939+03:00"
---

Problem: the same measurement as habu-name-the-site-03a33e54 - the standalone tender image's closure is large enough that some ADR (reach +-1 MiB, src/arch/arm64/icode.f ADR-LO/ADR-HI) cannot reach its target: a captured body's literal or data reference, or an engine helper's, placed beyond the window by the stripped layout (src/habu/aot-lib.f). The AOT-section precedent (dot d1326596: 29 sites at chain scale, fixed by TADR, and kept by tools/aot-section-reach-lint.f) shows the family. Acceptance: with the site named (habu-name-the-site-03a33e54), decide whether the layout keeps the target in reach (bands adjacent to the code that addresses them) or the reference addresses long (ADRP+ADD / TADR,), weighing thin binaries and code size; implement it; the tender standalone build completes (aspen measures); a fixture builds a stripped image past the ADR reach and pins it, or the measured reason such a fixture is infeasible at test scale is recorded beside the code. Design decided (study 2026-09-22): only four ADR sites grow with the code band, all in the fixed startup, each emitted once per image - crash.f:274 LCRASHH (G-INSTALL-CRASH), aot-lib.f:416 LSIGH (EMIT-SIGNAL-PUBLISH), aot-lib.f:218 BLOB-LBL (EMIT-DATA-COPY), aot-lib.f:175 MLBL (entry-xt cell); copied application bodies never reach ?ADR (COPY-COMPACT-BLOB emits raw words; a member's ADR is re-encoded by RELOC-W32 with its own refusal) and DATA/string references use movz/movk chains, so only the code band enters any reach. Layout cannot fix it (MLBL points into the band, the other three past it). Fix: address those four through the TEXT-BASE claim RBASE-CELL (aot-owned-cells.f:121,:280; aot-lib.f:177-179) with LOFF, (icode.f:386-394) + ADD - the habu2.f:87-92 TADR, scheme, moved to a file the stripped-link path loads (habu2.f is not: tools/aot-build-core.f:9-13) - at most 48 bytes per image; keep ADR, for LTEXT (backward inside the startup) and the message labels (rt.f:145/147, crash.f:197/235/248); delete AOT-DATA-BLOB-MAX and its guard (aot-lib.f:53, 371-372: dead for any band >= $100000 because LCRASHH fires first); update test/gate-aot-image.f:144-166 to find the blob by the movz/movk pair; a lint on the tools/aot-section-reach-lint.f model over aot-lib.f and crash.f (a bare '<label> LABEL@ ADR,' is a finding unless the label is LTEXT or bound by LBL, earlier in the same definition); HBT-STRIPPED-CHAIN is the end-to-end proof of the converted sites, and a build fixture past 1 MiB is not affordable (the link is quadratic in NCLO: OLD>NEW aot-lib.f:513-517 and MEMBER-AT :454-458 scan per relocated instruction - also worth a dot if Tender's link time matters). Files: src/habu/aot-lib.f, src/habu/crash.f, tools/aot-section-reach-lint.f (or a sibling lint), test/gate-aot-image.f, src/arch/arm64/icode.f, src/compiler/native emitters as found, tools/hb-build-test.f. Verify: tools/hb-build-test.f; test/run.f; Tender's build with aspen. Depends: habu-name-the-site-03a33e54. Ownership: hazel. Claim: unassigned.
