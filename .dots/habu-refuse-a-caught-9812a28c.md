---
title: Refuse a caught quotation that reaches below the catch depth
status: open
priority: 2
issue-type: task
created-at: "2026-09-18T19:55:32.755712+03:00"
---

Problem (adversarial review of 534734bb..a6ce705f, HIGH, probes clob0/clob1/clob2): a throw pushes its code at the CURRENT data-stack depth (src/habu/habu1.f BDIV0? and every engine throw; the compiled divide's cold side src/compiler/native/emit.f PUT-SDIV 'str rd,[x19],#8' is a new site), and src/core/checker.f RSCATCH admits a depth-preserving quotation whose inputs are cells present at catch time and keeps their pre-catch types after the throw. So a throw from a word that consumed cells below the catch level overwrites a caller cell the checker still types: ': NOP ( -- ) ; 1 set-tier : D3 ( ptr u8 n n -- ptr u8 n ) {: p u d :} NOP u d / {: q :} p q ; 0 set-tier : T ( ptr u8 n n -- ptr u8 n n n ) [: D3 0 ;] catch ; s" hello" 0 T . drop type cr' prints -6400 and an empty line - the NOP call synced x19 to catch-level minus 24, the divide's cold side pushed -6400 there, and after catch the cell typed ptr u8 holds -6400 (type is a write(2) so it fails EFAULT silently; c@ would SIGSEGV). At tier 0 (no NOP) BDIV0? clobbers the same cell; at tier 1 with no call it prints hello because x19 lagged - which cell dies depends on the tier. Any throw has this root; the batch added the compiled divide to the sites. Acceptance: decide and land the sound shape - either RSCATCH refuses (by name, own reason) a caught quotation whose Q>DIN reaches below the catch depth, or the cells below the catch depth are retyped unknown after the catch, or the throw ABI carries the code in a register and pushes only at the catch's recorded level (engine + compiled sites + gforth mirror); state the decision and why in docs/effects.md's catch section; red-first fixtures for clob0/1/2 in the checker's catch suite (find it: rg -l RSCATCH test) and a runtime fixture that shows the caller cell intact after a caught throw at both tiers; the 'contract holds at every tier' bullet in docs/forth.md stays true. Files: src/core/checker.f (RSCATCH), or src/habu/habu1.f + emit.f + bootstrap/cg/forth.fs for an ABI change, docs/effects.md, test/. Verify: the probes; three generations with cmp; bootstrap check if the engine changes; test/run.f. Depends: none. Ownership: checker / engine throw ABI. Parent: habu-campaign-c2-mem-c3d7662b. Claim: unassigned.
