---
title: Factor nominal registry template copies
status: open
priority: 2
issue-type: task
created-at: "2026-07-19T20:55:07.005251+02:00"
---

An exact census finds ten Maki identity-registry modules carrying the same owner-private refinement pattern across 2,647 lines. producer.f, config.f, schema.f, rev.f, and db/evidence.f are near template copies: each declares a bespoke id-result sum, 256-entry/256-byte key arena, offset/length/content-key columns, private RAW>ID/ID>RAW casts, bounds checks, linear KEY-FIND/CK-FIND, SHA-256 storage, identical LE-PUT/LE-GET, 8-byte ID wire codec, 32-byte content-key codec, EQUAL?, RESET, and matching copy-shaped tests. artifact.f, journal.f, diff-suite-id.f, run.f, target.f and obligation identity tails repeat large subsets. Besides 2.6K source/JIT lines, each 256x256 key arena plus columns/hash table reserves roughly 70-76 KiB even when empty, multiplying resident DATA across a full Maki image. Build one package-owned nominal-registry substrate with shared search/hash/codec algorithms and storage descriptors, plus a concise checked declaration form that emits only owner-specific nominal mint/project wrappers, error mapping, and policy differences (interned-content versus occurrence sequence). Generated wrappers must call shared bodies, not reproduce loops. Back storage with bounded chunked/used-capacity arenas so independent registries retain quotas without preallocating every maximum; preserve package-private authority and distinct nominal families. Prove byte-identical wire/content-key forms, insertion/fresh-process identity, rollback/reset, capacity/failure atomicity, cross-family rejection, and current tests for every migrated registry; measure source lines, live JIT bytes, and DATA before/after. Coordinate the longer-term immutable nominal arena habu-add-immutable-nominal-9290a81f but remove present duplication independently. Files: new registry substrate/declaration module, listed owners/tests, TRUSTED. Depends: none. Ownership: repeated nominal registry mechanics/storage only; owner schemas and semantic fields stay separate.
