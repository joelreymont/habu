---
title: Check split K workspace size
status: open
priority: 1
issue-type: task
created-at: "2026-07-19T21:53:03.542829+02:00"
---

Current master correctness defect: lib/ptx/cuda-driver.f:273-280 exposes SPLIT-WS-ALLOC (m n s -- cuda-devptr) over three interchangeable raw integers and computes m*n*s*4 with unchecked signed-cell multiplication before comparing to free device memory. A huge positive request can overflow into a small positive byte count and pass the claimed E-CUDA-WS wall; zero and negative dimensions/split are not rejected locally. The production emitter has a second unchecked contract: lib/ptx/cg-mma.f:1357-1360 computes Kslice=K/S with truncating div.u32 and the split loop issues whole BK stages, but MMA-CHECK-SPLIT at 1436-1442 does not require S<=K, K%S=0, or (K/S)%BK=0. Those checks exist only in tools/ptx/mma-gemm-check.f:680-685; gemm-bench.f:110-154 can launch an accepted kernel that drops the K remainder or stages past its slice. Consume the shared typed gemm-shape and mma-config from habu-structure-mma-autotune-e02d3197, with refined positive dimensions and split count. Put workspace arithmetic and split-slice divisibility in one shared production legality boundary used by every emitter, allocation, benchmark, checker, and launch consumer. Compute workspace bytes through checked multiplication returning an explicit numeric result; reject S>K, K%S!=0, (K/S)%BK!=0, overflow, or invalid input before CU-MEM-ALLOC or launch. Represent memory-info scratch as STRUCTURE {free-bytes,total-bytes} with named FFI field addresses. Preserve exact valid allocation, PTX, and CUDA errors. Prove compile-negative dimension/split role swaps where roles differ; zero/negative; K around every S and BK boundary; every multiplication boundary around max cell and free bytes; every invalid shape makes zero CUDA allocation/launch calls; exact-fit and free+1; mocked memory-info/allocation failures; cleanup ownership; valid split device goldens. Measure JIT/DATA/CODELEN, allocation calls, and launch preparation. Correct the closed habu-split-k-for-8c3ee0fb fail-closed correctness claim; serialize with the shared config and CUDA resource-scope owners.

This is retained-feature correctness only. The measured-losing path has no production consumer, so habu-remove-losing-split-5639823d is the default owner and deletes the defect. Implement this dot only if a concrete retained product requirement with target measurements is established first.
