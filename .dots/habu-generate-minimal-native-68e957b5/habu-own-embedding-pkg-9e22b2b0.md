---
title: Own embedding package
status: open
priority: 1
issue-type: task
blocks:
  - habu-lowering-hash-unified-586f7881
created-at: "2026-07-19T23:27:41.113394+02:00"
---

The positional-embedding change extends broad package MAKI with EMB-*, WPE-*, ROW-ADD, WPE-FIT and public TOKPOS-EMBED, while E-WPE-EXTENT is a raw global defined before the package. maki/pos-embed-test.f reopens MAKI and creates 30+ TP-* globals, ambient trainer buffers/state, raw slot constants repeatedly converted through SC-SLOT/MIR-SLOT-ID despite existing MIR:input-slot, split initial/final loss cells, and two identical MODEL: TOK-POS definitions instead of owning a test module. Zero/negative TP-RUN iterations publish no new loss but leave old result cells readable. This violates package-first scope, lengthens dictionary names, and exports helpers/state. Exact identical-preload measurements put the total positional production+test delta at +8,052 JIT bytes, +1,416 DATA bytes and +50 definitions; production helpers account for +1,388 JIT/+6 definitions, while the new test payload accounts for +6,664 JIT/+1,416 DATA/+44 definitions. Create a real EMBED package with short private tails and only the measured production API; keep named errors inside it. Put the fixture in POS-EMBED-TEST with private state and one load/run effect, store actual MIR:input-slot values, reuse one model capture, and reject old MAKI-prefixed/test-global names. If a reusable runner remains, return STRUCTURE training-result and store payload-bearing ENUM run-state = uninitialized | complete(result); otherwise delete it after the focused numeric proof. Consume helper deletion from habu-remove-positional-buf-ceaf46d0 and heavyweight dependency removal from habu-expose-typed-gradient-3c4fedda rather than preserving aliases. After unified lowering, pass STRUCTURE embedding-table, sequence-shape and bounded typed spans/token ids instead of interchangeable pointers/counts. Preserve valid numerics and diagnostics. Prove standalone load, package visibility negatives, import-no-side-effect, zero/negative/one run, two isolated fixtures, exact public-name census, no duplicate MODEL capture, and attributed source/definitions/JIT/DATA/CODELEN reduction from the measured baseline. Files: maki/embedding.f, embedding-test.f, pos-embed-test.f and direct callers. Block STRUCTURE/payload-ENUM work on habu-lowering-hash-unified-586f7881; immediate memory safety remains habu-validate-embedding-access-2cf51e2c.
