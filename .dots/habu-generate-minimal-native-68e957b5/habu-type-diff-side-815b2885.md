---
title: Type diff side content state
status: open
priority: 1
issue-type: task
blocks:
  - habu-lowering-hash-unified-586f7881
created-at: "2026-07-19T21:48:49.160051+02:00"
---

Current master defect: tools/diff-side-content.f defines content-kind at line 16, but public SIDE at 324-333 still accepts independent present, content-kind, binary, path span, and body span. KIND-CHECK at 179-190 exists only to reject representable contradictions such as absent+present, file+absent, gitlink+body, and non-file+binary. The encoder lifecycle is split across sixteen ambient cells at 33-48 plus raw READY/ROW/SIDE integers at 55-57; transitions at 250-345 update phase and payload fields separately around fallible writes. tools/diff-side-content-read.f:69-100 recreates the split representation; selection uses -1 plus ambient offsets/counters at 162-187 and publishes ten independent OLD/NEW accessors at 248-294. Define payload ENUM side-content = absent | file(path,body,binary) | symlink(path,target) | gitlink(path,identity), STRUCTURE change-row with ordinal/old-side/new-side, payload ENUM builder-phase carrying only legal phase data, and a decoded STRUCTURE row/view returned by selection. Keep numeric tags, offsets, sizes, digests, and reserved bytes behind the explicit wire codec. Make construction and publication transactional. Preserve the wire layout, exact non-gitlink artifact bytes, and scanner/integration behavior; gitlink digest bytes must deliberately change from fabricated zeros to the real authenticated identity. Prove compile-negative side/span/state swaps; exhaustive variant round trips; mutation of every wire tag/bool/reserved/length/digest; injected failure at every encoder write leaves the prior or explicit aborted state; selected-row access requires validated selection; exact hostile-path corpus; capacity/canaries. Measure source/JIT/DATA/CODELEN, storage, and encode/decode throughput. Serialize with habu-tools-bulk-diff-f36d0508 and M5 framed-diff work; those dots retain protocol/scanner/integration ownership.

Current gitlink semantics are already lossy: diff-side-content.f:179-190 and 306-317 normalize every gitlink to an all-zero digest, and diff-side-content-read.f:69-91 reads that fabricated identity; current tests codify it. Two changes to different submodule commits therefore produce indistinguishable side content and can collide in every downstream authenticated diff decision. The gitlink payload must carry the exact validated object identity supplied by the scanner, bind it into the artifact digest, and round-trip it without a file body. Add real repository fixtures changing a gitlink between two distinct commits plus zero/short/long/nonhex/mismatched identity mutations; prove distinct commits yield distinct authenticated artifacts and selected rows expose the exact identity.

Remove semantic re-encoding bloat too. KIND-ABSENT?, KIND-FILE?, and KIND-GITLINK? at tools/diff-side-content.f:62-86 call the full four-arm KIND-BYTE wire encoder and compare ordinals 0/1/3. Derive equality for content-kind and compare typed constructors, or MATCH only where behavior differs; KIND-BYTE belongs exclusively in wire output. Require disassembly/CODELEN proof that predicates no longer duplicate the codec.
