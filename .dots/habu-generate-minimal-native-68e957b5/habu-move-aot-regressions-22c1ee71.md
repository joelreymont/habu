---
title: Move AOT regressions out of builds
status: open
priority: 2
issue-type: task
created-at: "2026-07-19T20:47:07.009803+02:00"
---

src/habu/aot-capture.f:603 and :627 define ACAP-WID-SELFTEST and ACAP-PWID-SELFTEST, executed at :618 and :638 during every production stdin metabuild (include chain proven: tools/srclist.f:89 emits aot-capture.f into the stdin source list between driver-io.f and stdin.f). They are regression fixtures, not capture prerequisites. REDESIGNED 2026-07-23 after a checkpoint stop with evidence: the ACAP words and their habu2.f buffers exist ONLY inside the fresh-stage metabuild host, so no standalone test can load them (loading the metabuild list on bin/hb dies rc 78 duplicate ENV-DATA; loading only engine files dies rc 70 E-UNDEFINED G-POP). SCOPE REDUCED 2026-07-24: the protected-WID band (ACAP-PWID-SELFTEST, ACAP-PWID-MAXWID and related deletions) is RELEASED from this dot — it is owned by the protected-WID retirement contract in the other orchestrator's pending declaration-family train (blackboard record 2026-07-23: four-item deletion with the baseline sha isolation method). This dot must not touch those words.

Required result, two parts, one change: (1) Delete the ACAP-WID-SELFTEST definition and its immediate execution from src/habu/aot-capture.f. (2) Move the record-WID compact round-trip proof (the ACAP-WID-SELFTEST body: ACAP-COMPACT-RECS/ACAP-PROVE-RECS over the [40] u32 wid, plus max-WID calculation and exact buffer reset) into the EXISTING build-and-spawn owning path: a proof driver alongside test/aot-wid-build.f's generated metabuild driver that runs the proof against the real ACAP words in the metabuild host after CAPTURE-REPL, dies on mismatch, and exits before image emission to skip maker cost; test/aot-wid-suite.f (or a sibling registered in test/gate-stdlib-cases.f and executed by test/gate-stdlib.f) asserts the driver exits 0. Red proof recorded once: a deliberately truncated codec makes the driver die. NOT DISPATCHABLE YET: the proof driver needs a metabuild private-test injection capability - a way to run a test word inside the metabuild host through an audited extension seam - and no published contract owns that capability today. It must NOT be built by splicing stdin source text: three independent reviews proved no handwritten splice scanner is fail-closed; the rejected splice commits are preserved unpublished in workspace .jj-ws/habu-move-aot-regressions-22c1ee71 as evidence. When a capability dot is published, add the exact dependency edge here before any claim.

Acceptance: byte-identical hb engine output after the single self-test removal (sha compare; method proven on the recorded pair-deletion experiment); the proof driver green on the real metabuild host and red under an injected truncating codec; test/gate-stdlib.f executes the registered suite; package, typed-local, and host gates green on the diff artifact. Files: src/habu/aot-capture.f, test/aot-wid-build.f or sibling driver, test/aot-wid-suite.f or sibling, and test/gate-stdlib-cases.f. Ownership: the record-WID self-test body and its relocated proof only; no serialization redesign, no capture behavior change, no protected-WID band edits.
