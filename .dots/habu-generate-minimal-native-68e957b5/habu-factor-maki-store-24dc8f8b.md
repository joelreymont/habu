---
title: Factor Maki store framing
status: open
priority: 2
issue-type: task
created-at: "2026-07-19T20:54:16.826033+02:00"
---

maki/store.f (422 lines), maki/competitive-store.f (438), and maki/competitive-evidence-store.f (415) independently implement the same mechanics: 1 KiB stable row/key/input/canonical buffers, root/env/path resolution, bounded byte append/copy, pipe-delimited field scanning, decimal/token parsing, duplicate/field-count checks, append/read plumbing, and canonical re-render integrity checks. The three files total 1,275 lines before schema-specific code; store.f and competitive-store also each reserve a separate 256 KiB whole-file read buffer when loaded. This is copy-shaped architecture: fixes to framing, overflow arithmetic, path handling, malformed rows, and error preservation must be repeated, while each copy uses different errors and subtly different persistence layout. Extract a package-owned bounded row codec and store transport with explicit injected schema callbacks/typed records: stable input ownership, checked field iterator, unique-label set, canonical encoder comparison, bounded append, and selectable append-log versus content-addressed path transport. Keep each schema’s typed field vocabulary and error mapping in its owner; do not collapse semantically different evidence/benchmark/general rows. Replace whole-file scans with streaming indexed lookup where measured, so factoring also removes redundant 256 KiB resident buffers rather than centralizing one global scratch buffer. Prove byte-identical valid encodings and exact existing reject classes with golden/mutation tests for all three stores, concurrent/error cases, and before/after source/JIT/DATA measurements. Coordinate canonical identity with habu-v2-r3-preserve-f081f2c9 but do not wait for its broader type migration. Files: new store framing/transport package and the three store modules/tests, FILEMAP.md. Depends: none. Ownership: shared storage mechanics only; no schema, promotion, or transaction semantics.
