---
title: Remove losing split K experiment
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-19T22:02:19.976216+02:00\""
---

Measured current-master bloat with no production consumer: every recorded S>1 split-K result loses materially (S=2 0.40x and S=4 0.34x versus the 0.67x committed path), every committed autotune winner keeps split=1, and an exact reference census finds no Maki consumer of MMA-SPLITK, SPLIT-WS-ALLOC, or the reduction kernel. All 59 references are confined to cg-mma.f, cuda-driver.f, autotune plumbing, benchmark/checker tools, and their tests. The experiment retains roughly +157 source lines in cg-mma.f, +19 in cuda-driver.f, +87 in gemm-bench.f, substantial mma-gemm-check code and 44 MiB checker-buffer capacity growth, plus config axes and follow-up correctness/factoring obligations. New autotune-sweep.f makes the retained surface actively misleading: AT-LEGAL? can accept S>1, but the sweep allocates/binds only A/B/C with the 36-byte single-pass MMM ABI and never provides the split workspace or launches the reduction, so a supposedly legal split candidate is checked/timed through the wrong contract. Delete the S>1 emitter, reduction kernel, workspace allocator, launch paths, sweep/config fields and constructors, checker buffers, benchmark cases, and tests; preserve the falsification measurements and conclusion in the performance history. Prove the remaining S=1/full-range PTX is byte-identical, the sweep has no split path/field, committed winners and normal GEMM behavior are unchanged, no split symbol or dormant branch remains, mapped checker bytes fall by the exact retired capacity, and source/JIT/DATA/CODELEN plus full PTX/Maki/native gates improve. If a concrete retained product requirement is established before implementation, document it with target measurements and keep habu-check-split-k-fda7652d plus habu-factor-split-k-362e08c0; otherwise this deletion supersedes polishing a measured-losing unreachable feature.

Claim: agent=splitkrm workspace=.jj-ws/fable-splitkrm machine=spark (owns lib/ptx/cg-mma.f cuda-driver.f + tools/ptx bench/check/autotune surfaces for the deletion; the session GPU lane for its verification runs)
