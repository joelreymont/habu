---
title: Structure precision policy
status: open
priority: 1
issue-type: task
blocks:
  - habu-lowering-hash-unified-586f7881
created-at: "2026-07-19T21:23:37.771362+02:00"
---

maki/prec-attr.f:41-69,117-133 and maki/precision.f:42-105 model compute precision and licensed tolerance policy with raw integers and flat arrays. CPREC-PACK accepts payload and precision as n, so a small equation slot and precision code can swap and still pass; CPREC-REQ/PREC-REQ are generic cells. PREC-ROWS is a hand-indexed four-cell row table where rtol-mantissa zero doubles as absence, and class/precision/table coordinates are interchangeable n values. Define a compute-precision ENUM and typed request storage. Reuse the operation-class ENUM from habu-structure-op-metadata-7fec08bf, define a STRUCTURE tolerance-row with named atol/rtol mantissa/exponent fields, and represent an unlicensed class/precision pair explicitly as option<tolerance-row> in typed storage. Keep attrs bits and numeric ordinals only in a private encode/decode boundary; CPREC-PACK/CPREC@ become typed codec operations and every public policy API accepts/returns enums. Replace range checks and case defaults with exhaustive MATCH. Preserve exact attr bytes, default TF32 behavior, licensing matrix, tolerances, kernel selection, reports, and device goldens. Add checker negatives for payload/precision/class swaps, raw n and foreign enums; exact codec round trips, full class-by-precision matrix, absent rows, defaults, and tolerance goldens. Measure JIT/DATA/CODELEN, table bytes, lookup latency, and emitted PTX before/after. Files: maki/prec-attr.f, maki/precision.f and direct tests/consumers. Verify precision/lowering/device suites, Maki, typed-local diff, type/package/host/filemap/dot lints, and full native gate. Coordinate source-grammar behavior dots; ownership here is representation and finite domains.

Currentness addendum after 2f508b27345b: CPREC-PACK validates only the precision tag, not that payload is nonnegative and confined to bits 31:0; a negative or high-bit payload can overwrite the precision field or reserved high bits. CPREC@ and CPREC-PAYLOAD@ then mask the corrupted cell into plausible values instead of rejecting noncanonical attrs. The new one-shot override is another split raw state: CPREC-OVR uses -1 as none, CPREC-REQ holds the fallback separately, and CPREC-NEXT@ consumes by mutating ambient state. plan-ops.f runs fallible PLAN-MM-DESC and equation operand checks before CPREC-NEXT@, so catching E-TV-SHAPE leaves the requested precision pending for the next otherwise untagged operation. cad.f:570-600 separately returns suffix parse as `(n bool)` with fabricated `0 false` and manually nips the unread payload. Those helpers remain in CAD's public section, exporting CPREC-SUFFIX?, EMIT-CPREC-SET, EMIT-GEMM-PREC, EMIT-EQ-PREC, and EMIT-COLON-TOKEN only so tests can bypass MODEL: error handling. Model parse and override as option<compute-precision>, pass the typed precision explicitly to the composer, validate exact canonical attr bits and operation-specific payload range on both encode and decode, and make lowering consume an immutable node precision rather than ambient next/default cells. Keep parser/emitter helpers private under their owning package and test public MODEL: behavior through a catchable isolated path. Add negative/high/reserved-bit mutations, noncanonical round trips, equation-slot bounds, suffix absence/presence, caught-failure-then-valid-operation, interrupted-override/reset cases, public-surface census, and exact failure identity. Prove no ambient precision state remains. End-to-end tag consumption is owned by habu-wire-per-op-d34a0b66.
