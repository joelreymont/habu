---
title: Make the pre-arm prefix record-free
status: open
priority: 2
issue-type: task
created-at: "2026-09-18T10:24:05.098447+03:00"
---

Problem: nine mixed records live in the pre-arm prefix (src/core/util.f through include.f), where no family facility exists (constructor generation dies before TDECL-EVAL-ARMED, src/core/sumtype.f:1676): the SYM row (checker.f:4370-4467, two pointer and three scalar columns, ptr-cell-mark per row), CWIN (checker.f:1309 and cell-effects.f:43 STATE), the declaration-owner record and cells 0x360/0x368 (checker.f:6-7,118-127; src/habu/layout.f:954-955 names the same offsets differently and nothing asserts the spellings agree), transaction state (declaration-transaction.f:80-105), the DYNAMIC-STORAGE registry (dynamic-storage.f:44-58), the DYNAMIC-BUFFER control record (dynamic-storage.f:1-11, generated by layout-buffer.f:517-535), the include frame (include.f:695-724), startup DATA (env-base.f:6-24), and 17 reserved engine-layout cells read as data-base <const> + 0 ptr-field (NULL-PTR among them). Acceptance (docs/type-system.md section 10.4, The engine prefix): each record split into one declared cell or table per field, SYM's two pointer columns as PERSISTED-PTR-U8-TABLE-VARIABLE marked once per table and its scalars as plain tables; the DYNAMIC-BUFFER control record dissolved into NAME#base/NAME#cap/NAME#slot with three registry columns, or dynamic-storage.f moved after include.f and the record declared (decide, record why in the file header); the reserved-cell declarator over the 17 sites, which makes the 0x360/0x368 spellings one declaration; ARENA-SNAP-BOOT's zero pass writing NULL-PTR through the table accessor; the growers ARENA-BYTES-GROW, TV-GROW-ONE, TVK-GROW-ONE on COPY ( ptr a ptr a n -- ); AOT-SPAN:BASE-CELL (src/habu/aot-closure.f:242 vs :243, read both as a pointer and as a number) given one type with an explicit crossing; no mixed record left in the prefix; byte fixpoint; the snapshot marks one cell per table instead of two per live SYM row. Split per record at dispatch; SYM alone is one lane. Files: src/core/checker.f, src/core/cell-effects.f, src/core/declaration-transaction.f, src/core/include.f, src/core/dynamic-storage.f, src/core/layout-buffer.f, src/core/pointer-storage.f, src/os/env-base.f, src/habu/stepper.f, src/habu/aot-arm.f, src/habu/aot-closure.f, src/habu/native-runtime.f, src/compiler/native/checker-owner.f, src/compiler/native/compiler.f, bootstrap/cg mirrors. Verify: three generations with cmp; tools/bootstrap.sh check; test/run.f. Depends: habu-replay-derive-addr-5fd9a813, habu-mark-a-persisted-5d0993aa. Ownership: engine prefix. Parent: habu-campaign-c2-mem-c3d7662b. Claim: unassigned.
