---
title: Include required module source in stage certification
status: closed
priority: 2
issue-type: task
created-at: "\\\"2026-09-14T02:03:28.878822+03:00\\\""
closed-at: "2026-09-16T14:34:49.480222+03:00"
close-reason: "superseded by habu-campaign-c1-finish-1f129a00: Stage certification still does not follow require, so a module's source can be missing from the certified input; keep it with the build source assembly."
---

Cedar owns BF source assembly dependencies. Product I certifies prefix-src then
rejects stage2-src at ENGINE-HELPER:REGISTER ADD because SOURCE-BUF does not
follow require. Exact input: /tmp/cedar-I-wid-maker-O129pcQz/stage2-src;
require/preload reductions: /tmp/cedar-I-wid-cert-{require,preload}.f.

The existing common assembler now emits primitive-registry before habu1 and
code-origin at its require boundary after ENGINE-HELPER. It emits fmt's actual
transitive closure in order: option, CAD-NUM types, CAD-NUM arithmetic, string,
float, fmt. Prelude/errors remain before these consumers. Every emitted module
has its provided fact, and the existing split helpers pin the complete input
before reading either half.

Registered build-fixpoint tests assert module bodies, dependency/fact order,
changed-between-halves rejection, and stage2/stdin certification of their actual
distinct generated source. The two obsolete RPD@ presence/order probes now use
the real habu1 FPRIM emitter, preserving their assertion.

Structural and split-pin controls pass on I. Old I then rejects stage2 at the
separately repaired replay import leak (7141; dot cc037817):
/tmp/cedar-I-maker-deps-focus.{out,err}. Through the actual changed checker and
native source handover, those controls also pass and stage2 reaches the next
boundary: 7102, duplicate option family already present in the warmed verifier
host (/tmp/cedar-I-maker-deps-source.{out,err}; window -2805). The family
declaration was previously absent from the generated bytes; no refusal has
been suppressed. Full phase certificates, real maker build and the frozen WID
collision executions remain pending that verifier ownership defect.
