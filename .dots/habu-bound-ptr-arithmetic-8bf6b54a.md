---
title: Bound pointer arithmetic from NULL-PTR and data-base
status: active
priority: 2
issue-type: task
created-at: "2026-09-17T17:29:19.248115+03:00"
---

Problem: ': PEEK3 ( n -- n ) NULL-PTR + @ ;' and ': POKE3 ( n n -- ) NULL-PTR + ! ;' certify and run (scout probe 2026-09-17 on d5e871c0, report ~/.cache/hazel/scout-pun/report.md: read 4660, wrote $C0DE), and 'data-base + @' does the same. NULL-PTR (src/core/cell-effects.f:9) is declared '-- ptr a' with an ordinary TVK-ANY pointee and + is pointee-polymorphic (src/core/checker.f:6900), so the caller also picks the pointee: 'NEWTYPE thing 0  : F ( n -- thing ) NULL-PTR + @ ;' certifies and forges a nominal value. The axiom 'ptr a n + -- ptr a' with an unconstrained n reaches any address from any base, so the raw-storage rule (habu-refuse-a-ptr-5ad2734e) does not close this; it needs extents or provenance on pointers. Measured 2026-09-17 by the raw-ptr lane: data-base itself is 'PRIM: data-base PE-PTR-A PE-OUT' (src/core/checker.f:7129), an ordinary TVK-ANY pointee, only here is PE-PTR-A-RAW (:6981), so ': F ( n -- n ) data-base ! data-base @ @ ;' still certifies under the raw-storage rule; declaring data-base PE-PTR-A-RAW is not the fix: the RAW kind propagates through + into declared type variables and NP-CHECK refuses definitions that never touch a pointer (first hit in the engine build: 'E-NONPARAMETRIC-EFFECT habu: in sb-len: declared type variable a is restricted by raw storage; its declared kind must stay unchanged'), and data-base + offset is the read path (DATA-CELL@) the raw-storage rule prescribes, so data-base stays PE-PTR-A and its @ @ case is this dot's, not 5ad2734e's. Acceptance: first the nominal forgery: a pointer derived from NULL-PTR or data-base by arithmetic never yields a nominal or pointer pointee (the F fixture above is rejected by name while 'ptr thing NULL-PTR =' and 'NULL-PTR -' keep certifying), with rejected-program fixtures; then the reach: a design under the campaign for bounded pointers (a span type carrying an extent, or provenance on +) with PEEK3 and POKE3 rejected and the cost on the 'BUF k +' idiom measured across src lib tools test and the five consumers before the rule lands. Files: src/core/cell-effects.f, src/core/checker.f, docs/effects.md, docs/type-system.md. Verify: the fixtures; test/run.f. Depends: habu-refuse-a-ptr-5ad2734e. Ownership: checker. Parent: habu-campaign-c2-mem-c3d7662b. Claim: agent=hazel-ptr-arith workspace=.jj-ws/hazel-ptr-arith. Measured 2026-09-18 by the lane (release e7b1e2dc): the forgery is already reachable through shipped words (`require lib/string.f  : F ( -- thing ) SB-LEN @ ;` certifies) because 25 checked cell accessors return a data-base-derived pointer under a declared variable pointee (repl.f DATAB; lib/fmt.f FMT-IX/FR/DV/NUM-U; lib/string.f SB-LEN; lib/genio.f OUT/IN/ACTIVE/BUSY-PTR, ENGINE-ROW; lib/ffi-abi.f FFI-BUF/STACK-BUF/KPARAM-PBUF/KPARAM-VBUF/DLBUF; lib/fs.f FS-IO-FD/LEN/RD/OFF/WR; DISPATCH-CELL x4; lib/json-read-test.f JRT-ZERO-PTR); the offset-0 forms `data-base @` and `NULL-PTR @` certify too, so the rule fences the pointee kind (TVK-BASE, fenced at top level, permissive inside a pointee), not the arithmetic rows. Nothing in the tree does arithmetic on NULL-PTR (0 of 140 uses); data-base has 411 uses, 87 `OFF + @` scalar reads, 22 ptr-field reads that stay the declared door. The reach half: a pointer stepped by an integer and dereferenced on the same line occurs at 4209 sites in 2552 definitions across 440 files (src 1668, lib 994, tools 610, test 937), 4115 of them a bare `+` by a computed offset, 27 cell+, 67 1+/1-, 64 inside TRUSTED:, 150 with data-base as the base; a span type carrying an extent would have to reach all 2552 definitions, so the reach half is a campaign design, not a rule, and stays open here after the forgery rule lands.
