---
title: Give the stage sources concrete effects under the strict-parametric rule
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-13T11:05:27.080398+03:00\""
---

Problem: src/habu/stdin.f:33 ': HLINE ( a u -- ) H+ HNL ;' declares single-letter type variables and its body specialises them, which the strict-parametric rule (E-NONPARAMETRIC-EFFECT, landed 2026-09-12) refuses; the stage/maker engines compile stdin.f only since the unseeded-runtime rule (d48aff2f) gave them a runtime from source, so the certify pass now rejects the generated stage source (E-BUILD-CERTIFY 'generated stage source rejected'), keeping aot-wide-format (assert 1, builder stderr), aot-wid-restore (assert 1) and build-fixpoint-fixtures (assert 12/13) red and stopping the no-binary recovery check at its first native stage ('hook: non-certified definition: hline at HNL', bootstrap rc 70). Measured 2026-09-13 on root c3b6b3de, engine a503a443. Acceptance: every definition the stage payload compiles (src/habu/stdin.f, src/habu/stage2.f and whatever tools/build-fixpoint.f BF-EMIT-STDIN-RUN-SOURCE and tools/bootstrap.sh assemble) carries the concrete effect its body has, type variables kept only where the word is genuinely parametric; found by running the certify pass over the assembled stage source until it is clean, not one failure at a time; the recovery check (HABU_ALLOW_BOOTSTRAP=1 HABU_BOOTSTRAP_CHECK_ONLY=1 tools/bootstrap.sh) reaches its end or stops on a defect named with evidence; aot-wide-format, aot-wid-restore and build-fixpoint-fixtures green on a seed cold build; the engine image unchanged (these files are not in the product prefix; say so with the sha). Files: src/habu/stdin.f, src/habu/stage2.f, others the certify names. Verify: the certify pass, the recovery check, the three suites, test/run.f. Depends: none. Ownership: hazel. Claim: unassigned

Parked 2026-09-13 (hazel, account limit): landed 604f2c17 (RD-1 concrete, dead HLINE/H+ deleted) and 833e9ff4 (recovery status: chain reaches hb-stdin, dies at boot 'hb: AOT call site unresolved'); certify clean on all four assemblies. Remaining, with rulings: wall 2 = wire PFX-LOAD-STDLIB-COLD/PFX-PROVIDE-STDLIB-FILES into EMIT-COLD-PREFIX (the gforth mirror already has its stdlib block), drop lib/errors.f and lib/prelude.f from bootstrap.sh SRC_COMMON, add the two-assembly differ test in tools/bootstrap-codegen-test.f; a WIP commit b728e382 with an uncommitted habu2.f edit sits in .jj-ws/habu-give-the-stage-2f64be7c. Wall 3 = the seed's 8 named pre-window callees (6 layout.f constants, NULL-PTR, NULL$): constants compile to literals at tier 0 as a narrow proven arm (general inliner stays parked, habu-decide-the-tier-374c95ff), EM-AOT-PATCH-SITES prints site and name, the capture refuses a callee the target cannot have at build time, then decide late-patch vs closure for any remaining colon word. This wall gates the seed mint, address-table stage 2 and Tender.
