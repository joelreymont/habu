---
title: "MMA: amortize the B-side fragment feed (wider register tiles)"
status: active
priority: 2
issue-type: task
created-at: "\"2026-07-17T09:53:01.379866+02:00\""
---

THE lever from the mmaparity attribution (2026-07-17, evidence in habu-close-mma-gemm-ae9a76fa): the swizzled TF32 mma.sync GEMM is FEED-BOUND on B-side scalar ld.shared.b32 fragment loads (~40% of iteration time; each 8x8 B fragment feeds one mma with zero reuse), NOT mma-issue-bound; measured ceiling with the B-feed amortized = 2270 GFLOP/s = 1.20x the Triton baseline 1890.5 at 918MHz. Implement B-feed amortization in lib/ptx/cg-mma.f, preferred mechanism first: WIDER M REGISTER TILE (e.g. 32-row warp tile / 128x64 block) reusing each B fragment across >=2 M-frags with the ALREADY-PROVEN A/B fragment layouts (lower correctness risk); alternative/second wave: B-side ldmatrix on a transposed/swizzled Bs (one ldmatrix replaces 8 scalar loads - higher payoff per instruction but REQUIRES a new element-exact fragment-layout proof in tools/ptx/mma-probe.f BEFORE any kernel use, per the fragment-layout discipline: it is the top cause of correct-in-NumPy-garbage-on-device). Constraints: new configs are NEW rows/knob points - the pinned legacy modes (SWZ, SWZ-BK64, lower-mm/cg-mma verbatim consumers) stay byte-identical (capture+cmp); element-exact device golden for EVERY measured config at the 918MHz pin; XOR swizzle only if the wider tile needs the 4KiB pad memory back (lever #4 as enabler); do NOT invest in cp.async stages until the tile grows (attribution-dead standalone). Measurement per the established procedure: as-found devfreq recorded/restored, 25W, never ncu, medians tagged orin-nx-25w-918mhz in tools/ptx/perf-rows.tsv. Also fold in: productize the MMA-ABLATE emit-knob + ablation bench (8 DCE-safe modes, built as a scratch tool by the attribution lane - keep it as a checked harness per build-the-tool) and update docs/kernel-principles.md step-3c + the stale LESSONS mma-issue hypothesis with the feed-bound finding. Acceptance: >=1890.5 GFLOP/s device-correct at the pin (parity) or the measured post-amortization ceiling with remaining-gap attribution; ablation harness landed; docs updated. Files: lib/ptx/cg-mma.f, tools/ptx/mma-probe.f, tools/ptx/mma-ablate*.f (new), tools/ptx/perf-rows.tsv, docs/kernel-principles.md, LESSONS.md. Ownership: kernel perf. Feeds habu-feed-mma-config-d783e33b (autotuner rows). Device lane - sole zed owner.

Claim: agent=bfeed workspace=.jj-ws/fable-bfeed (device lane - sole zed owner for measurement sessions)
