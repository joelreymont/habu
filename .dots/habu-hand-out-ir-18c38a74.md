---
title: Hand out IR arena descriptors as region offsets
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-16T21:42:34.716684+03:00\""
---

Problem: after the scratch region landed (30df3a77), the arena registry's generation seal and 64-slot registry still exist and an engine with every generation, state and owner test cut to its bound check runs 4,887,014 instructions per trivial tier-1 definition against 5,186,689 (corpus 43.5 G against 46.6 G): the remaining registry-and-generation machinery is about 5.8 percent per definition (scratch lane measurement, docs/compiler-measurements.md section 6, 2026-09-16). The dot habu-allocate-ir-tables-3f3da802 asked for descriptors in the region, handles as plain offsets and no generation; the lane stopped because two things were unspecified. Acceptance: (1) IR-CTX gains an offset interface: an allot that answers a region offset and a resolve from offset to address, with the region bound checked in RD@ before any table header check, because a descriptor read out of released region bytes has an arbitrary count and data offset; (2) cross-word loudness is carried by READER tokens, not arena handles: a reader is short-lived and can carry the region epoch, while IR-BUILD's PROTO-SYM and HIR-WORD's SESS-POOL/SESS-ROWS hold session-owned arenas across definitions, so section 6's epoch-per-region compared at the boundary cannot be carried by an arena handle (the lane's finding); (3) with those two in place, arena descriptors move into the region, handles become offsets, the registry and its generation go, and the measured per-definition cost drops toward the 4.89 M bound with census byte-identical and every ir-*/native-* suite green; two-generation byte fixpoint. Files: src/compiler/ir/context.f, arena.f, build.f (PROTO-SYM), src/compiler/native/frozen.f, docs/compiler-ir-design.md, docs/compiler-measurements.md, test/compiler/ir-*.f. Verify: perf stat instructions:u per trivial definition and corpus as section 6 states; suites; fixpoint. Depends: none (30df3a77 landed). Ownership: IR context and arena. Claim: agent=hazel-offsets workspace=.jj-ws/hazel-offsets
