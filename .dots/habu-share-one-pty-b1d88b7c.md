---
title: Share one pty harness between the three pty suites
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-17T14:01:37.054879+03:00\""
---

Problem: test/proc-pty.f, test/aot-data-span-forge.f and test/process-pty-tty-smoke.f each carry their own copy of the pty primitives (open the master, spawn on the slave, poll, read to a marker under a deadline, keep the buffer tail, reap); the same defect has now been fixed three times in three commits (the marker read afbaa314, the forge deadline 9dab2b92, the tty smoke), and the copies already diverge: proc-pty.f READ-STEP falls through to a read on a blocking master when POLL-IN returns an errno and can block past its own deadline, while the forge's READ-STEP counts an errno as a quiet poll (measured 2026-09-17, forge lane). The tracker rebuild lost the earlier record of this duplication (habu-one-pty-boot-c932c9c3, docs/tracker-rebuild.md), and 9dab2b92's header cites that lost id. Acceptance: one checked library module (lib/pty-harness.f or the existing lib/pty.f family) owns spawn-on-pty, READ-STEP (errno counted quiet, hang-up reported), KEEP-TAIL compaction, WAIT-FOR to an absolute deadline through PROC-DEADLINE-AT / PROC-LEFT-MS, and a tracked never-seen fact for negative assertions (a compaction may drop the bytes a buffer scan would need); the three suites require it and keep no private copy; every suite green on a rebuilt engine; docs/process-pty.md names the module. Files: lib/pty*.f, test/proc-pty.f, test/aot-data-span-forge.f, test/process-pty-tty-smoke.f, docs/process-pty.md. Verify: bin/hb --load lib/errors.f lib/process.f test/proc-pty.f; bin/hb --load test/aot-data-span-forge.f; the tty smoke; test/run.f. Depends: habu-give-reject-in-71cf8ab6, habu-put-barriers-in-11291413. Ownership: test pty harnesses. Claim: agent=hazel-pty-harness workspace=.jj-ws/hazel-pty-harness.
 Added 2026-09-17 (pty-barriers lane): both harnesses now also carry their own span search (proc-pty.f FIND-AT / MATCH-AT / CONTAINS?, smoke FIND / FIND-AFTER / HAS-AFTER?) and their own ordered-prompt and window-bounded absence machinery (BARRIER / REJECT?, PROMPT-AFTER!); the shared module owns one span search over a caller-supplied span and both barrier shapes.
