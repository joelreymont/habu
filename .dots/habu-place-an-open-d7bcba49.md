---
title: Place an open generic family row in the native chain
status: active
priority: 2
issue-type: task
created-at: "2026-09-18T13:45:20.732400+03:00"
---

Problem: the native compiler refuses every checked word whose declared row holds an OPEN generic family instance below another value: '( span<t> n -- span<t> ) drop' dies 'ncomp: cannot compile' -8519 E-NELAB-BUNDLE under 1 set-tier while '( span<u8> n -- span<u8> ) ...' and '( span<t> -- n )' compile (measured 2026-09-18 on engine b584306e with a two-field STRUCTURE span 1 FIELD base ptr a FIELD len n; probes in the lead's scratchpad p-p1..p8.f). Cause by reading: src/compiler/native/dict.f ROW-GLUE answers GLUE-UNKNOWN when the checker's row has fewer terms than cells and more than one term - the checker (CHECKER-OWNER:QUERY / EFF-COUNTS, src/core/checker.f PUSH-LOGICAL, TFAM-INST-WIDTH@) expands a CONCRETE family instance into its cells with slot numbers but leaves an open instance as one term whose width the glue cannot place, although a family whose type parameters occur only as pointees (span's t is under ptr) has a fixed declared width. Consequence: lib/span.f (SPAN:SKIP/TAKE/SUB, generic by design) cannot be required by any tier-1 or AOT program: Tender's images fail at ncomp (aspen 2026-09-18, /tmp/aspen-span-native-bundle.md), habu's own test/native-fixture-write.f dies once lib/fs.f requires span.f (band 1 group 1 handoff), and a related shape 'TBUF cap SPAN:TAKE {: w :}' fails with 'hb: lowering certificate replay drift' (scratchpad drift1.f, T4-T8.f in the span-lib1 lane). Acceptance: the responsible layer (the checker's row expansion for an open instance whose width is fixed, or ROW-GLUE reading per-term widths) places such rows; '1 set-tier require lib/span.f' loads; lib/span-test.f passes under 1 set-tier; the drift shape compiles or is refused by name; a native-build gate case for lib/span.f in tools/hb-build-test.f (a stripped image that requires lib/span.f and exercises SKIP/TAKE/SUB/COPY/ALLOC-SPAN, printing a value, modelled on BUILD-AOT-DIV-REFUSAL); red-first fixtures for the refused shape in test/compiler/ (the native suite that owns E-NELAB-BUNDLE); docs/forth.md's generics or native-compiler bullet states the rule. A family whose parameter IS a payload cell (width depends on the argument) must still be refused or handled exactly, never guessed. Files: src/core/checker.f (or src/compiler/native/dict.f by measurement), test/compiler/, tools/hb-build-test.f, docs/forth.md. Verify: the probes; lib/span-test.f at tier 1; three generations with cmp; test/run.f. Depends: none. Ownership: checker/native chain. Claim: agent=hazel workspace=.jj-ws/hazel-open-row.
