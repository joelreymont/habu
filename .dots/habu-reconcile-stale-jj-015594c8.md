---
title: Reconcile stale jj workspaces
status: open
priority: 2
issue-type: task
created-at: "2026-08-03T16:35:17.983816+02:00"
---

Claim: unassigned

Why: merged and abandoned lanes were not consistently cleaned. The 2026-08-03 snapshot found 479 `.jj-ws` entries and 491 registered workspaces; the 2026-08-04 snapshot finds 481 and 493. Result: perform one safe census pass over every registered or on-disk workspace. Before removing a deletion candidate, establish its tip, parent, owning dot, and commits not reachable from master; do not duplicate all retained registry rows in this ledger. Forget the workspace and trash its directory only when every change is already on master or explicitly abandoned or superseded. Retain unique valid work and record its exact commit under the owning dot; do not merge product work in this cleanup. Remove missing-directory registrations only after recording their tips.

Current holds: `.jj-ws/habu-speed-up-scalar-8f5563d2@a4cd46f805f6`; and SHA-lane artifacts `/tmp/habu-sha256-bench.f`, `/tmp/habu-sha256-bench64.f`, `/tmp/habu-sha256-profile.f`, and `/tmp/habu-sha64.bin`. Unicode, sampling, and capacity implementation, salvage, review, mutation, dissolution, and landing-gate holds are landed, absent from the workspace registry and `.jj-ws/`, and recoverably trashed. The live jj registry, not this ledger, is the single nonduplicated exact inventory of registered workspace roots and tips; this ledger records only exceptional current holds and cumulative cleanup evidence. At each successful landing or abandonment, immediately forget the workspace, trash its directory and lane-private temporary artifacts, and verify both the registry and default workspace are clean. Before each push or session handoff and after each feature wave, reconcile the registry, `.jj-ws/`, lane-private temporary artifacts, and this ledger. Record newly discovered cleanup obligations here instead of creating another cleanup tracker.

Audit evidence: every registered tip and every non-master commit in its ancestry was enumerated; zero-context `jj` Git patches were normalized by removing object ids and hunk offsets, then compared with every nonempty master patch. The removed tranche was also reconstructed from jj operation history and recoverable-trash metadata. Patch equality alone does not prove ownership or abandonment, so other ancestor-redundant candidates remain untouched pending exact proof. The earlier 2026-08-04 safe passes cumulatively forgot 81 registrations, trashed 89 workspace directories, and trashed 4,436 unowned top-level `/tmp/habu-*` entries (20,392,873,984 bytes). Subsequent landing cleanup removed the completed Unicode and sampling workspaces and their declared temporary roots. This pass recoverably trashed 31 residual Unicode or sampling review/mutation roots and three generated roots; the capacity worker recoverably trashed its three handoff roots. Final capacity cleanup forgot three obsolete workspace registrations and recoverably trashed their three directories plus 12 completed review or landing-gate roots: 15 paths and 195,332,614 bytes. The current census is 396 registered workspaces, 376 on-disk `.jj-ws` directories, and four top-level `/tmp/habu-*` entries totaling 67,109,695 bytes. Those four entries are exactly the SHA-lane artifacts named above. The registry remains the authority for all ordinary and pathless workspace rows; none are duplicated here. All removals are recoverable through trash.

Acceptance: remove a target only with exact landed, abandoned, or superseded evidence; lose no unique commit; remove every proved-completed lane from registry and disk; keep the repository and default workspace clean. This active dot makes no claim that every retained ancestor-redundant workspace has been fully resolved. Forbidden: bulk deletion, product code change, permanent cleanup script, report, framework, lint, manifest, doc, or suite.
