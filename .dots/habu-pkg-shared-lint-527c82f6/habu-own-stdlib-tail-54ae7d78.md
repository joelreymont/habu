---
title: Own stdlib process schedule
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-23T05:06:54.486290+02:00\""
blocks:
  - habu-pkg-stdlib-gate-fb28fe63
  - habu-pkg-match-factor-28b18558
---

Why: the verify-source PRIM/PPRIM regression spawns malformed-source child engines and mutates verifier state, so it belongs in the isolated standard-library process group rather than the in-process CHECK group. That group is still an unowned global routine, so changing its inventory would fail package ownership. Required result: wrap only the existing GSI-TAIL-PROCESS definition in package TAIL-PROCESS and publish it as RUN ( -- ). RUN preserves the current parallel group header, test setup, subject and tail-ratchet setup, pool reset, all 22 existing fork rows in their exact order, one final drain, copy-on-write isolation, timeouts, and failure propagation. Add `test/verify-prim-test.f` exactly once after `test/prim-link-test.f` and before the drain. Add one `verify-prim` suite containing only that file to test/gate-stdlib-cases.f. Update every production reference to TAIL-PROCESS:RUN in test/gate-runner-lib.f, test/run-worker-stdlib.f, and test/gate-tail-process.f; update the exact scheduler name in test/match-factor-pin.f and the affected FILEMAP descriptions. Keep every shared scheduler helper and state at its existing owner. Do not add a file, alias, duplicate scheduler, scanner exception, copied inventory, compatibility global, new helper state, or in-process CHECK enrollment. This scheduler leaf and habu-close-verify-src-d77c01b6 form one atomic delivery: implement both in the verify-source workspace after the stdlib worker package lands, and commit them only when the combined production schedule is green. Prerequisite: habu-pkg-stdlib-gate-fb28fe63. Owned result and files: the process-group block in test/gate-stdlib-inline-lib.f; caller and documentation updates in test/gate-runner-lib.f, test/run-worker-stdlib.f, test/gate-tail-process.f, test/match-factor-pin.f, and FILEMAP.md; and the verify-prim suite row in test/gate-stdlib-cases.f. Acceptance: only TAIL-PROCESS:RUN resolves; the former global name and every qualified private TAIL-PROCESS helper reject; adding only the suite row makes suite-coverage report verify-prim as orphaned, and adding the real fork row clears it; the focused fixture then executes once through test/gate-tail-process.f, the `tail-process` runner entry, and resident worker identifier 36; all existing rows retain their order and exact outcomes. Exact package, typed-local, suite-coverage, file-map, and host checks pass on the combined verify-source tree. Claim: agent=verify_parity workspace=.jj-ws/habu-verify-parity.
