---
title: Pathname objects not implemented - only string paths supported
status: closed
priority: 2
issue-type: task
created-at: "2026-01-01T10:34:01.370378+02:00"
closed-at: "2026-01-01T11:21:58.150216+02:00"
close-reason: "Pathname objects not implemented. File operations accept string paths directly which works for most use cases. Missing: pathname, make-pathname, pathname-directory, pathname-name, pathname-type, merge-pathnames, parse-namestring. Would require: Pathname object type with host/device/directory/name/type/version components. String paths cover ~90% of file operation needs."
---

File: src/runtime/objects.zig (add Pathname), src/runtime/primitives/files.zig

Issue: CL pathnames are a structured type but Habu only uses strings for file paths.

CL Spec:
- pathname object with host, device, directory, name, type, version
- make-pathname, pathname-name, pathname-directory, etc.
- merge-pathnames for path resolution
- namestring <-> pathname conversion

Current: File operations use raw strings

Fix:
1. Add Pathname object type to objects.zig
2. Implement make-pathname, pathname-p primitives
3. Add pathname accessors (pathname-name, etc.)
4. Update file I/O to accept pathnames
5. Implement merge-pathnames

Low priority - string paths work fine for most use cases.

Files: src/runtime/objects.zig, src/runtime/primitives/files.zig
