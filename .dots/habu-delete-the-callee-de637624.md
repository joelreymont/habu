---
title: Delete the callee staging path from the migration entry
status: open
priority: 2
issue-type: task
created-at: "2026-08-07T11:43:01.378759+02:00"
---

src/compiler/native/migrate.f still publishes NMIGRATE:CALLEE (spelling, entry address, in, out) and NMIGRATE:DEFINE-CALLING, with the CALLEES-MAX ceiling of 4, the CALLEE-BUF/-U/-ADDR/-IN/-OUT tables, CALLEE-AT, CALLEE$, CALLEES-CLEAR, CALLEES-NONE-CK, RESOLVES-TO-ENTRY, CALLEE-PRI/-PUB, CALLEES-SCOPE!/-CK, DECLARE-CALLEE1 and DECLARE-CALLEE behind them. Since name resolution landed (elaborate.f RESOLVE-SCAN) none of that is needed for a name the engine can resolve and the checker can size: the body names the word and the row is built from the engine's own answers. Two roads to one row is one road too many, and the staged one is the one that can lie - a caller may still state an effect that is not the certified one, which is exactly the mutation dot habu-resolve-a-callee-0340dfde was opened for (': LIE-W ( n n -- n n ) LIE-DBL ;' with a one-in one-out callee declared two-in two-out selects, allocates, passes the register-allocation validator, emits and publishes with no throw, because the selector builds the save run, the restore run and both byte counts from the one stated number). Resolution closes that class only for names nobody stages. THE CONVERSION IS PROVEN BYTE-NEUTRAL: tools/codegen-compare-migrated5.f was converted whole on 2026-08-07 - all seven CALLEE stagings deleted, DEFINE-CALLING replaced by DEFINE - covering a tail call, work-before-tail, a non-tail call, a call chain, arity 2-to-2 and two calls in one body, and codegen-compare answered 0 findings with emitted sizes identical row for row (TAIL-BIG 4, TAIL-MID 4, TAIL-CHAIN 4, TAIL-PAIR 4, TAIL-WORK 16, TAIL-AFTER 24 bytes) and identical outputs. Scope: 87 remaining NMIGRATE:CALLEE sites and 76 NMIGRATE:DEFINE-CALLING sites across test/compiler/native-inline.f (59), test/compiler/native-migrate.f (32), tools/codegen-compare-migrated3.f (16), tools/codegen-compare-migrated4.f (13), test/compiler/native-clobber.f (14), tools/codegen-compare-migrated2.f (4), test/compiler/codegen-tail-probe.f (4), tools/codegen-compare-test.f (3), tools/codegen-spill-probe.f (2), test/compiler/native-reach.f (2). Watch for cases whose SUBJECT is the staging's own refusals (native-migrate.f CALL-REFUSAL-CASES, the CALLEES-MAX and scope-drift guards) - those cases go with the mechanism rather than being re-pointed. Gate: full test/run.f, maki, codegen-compare 0 findings, four lints.
