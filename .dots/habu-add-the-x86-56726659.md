---
title: Add the x86_64 Linux OS seam and ELF64 writer
status: active
priority: 2
issue-type: task
created-at: "2026-09-17T17:32:42.539125+03:00"
---

Problem: docs/x86-64.md (campaign habu-campaign-c6-targets-86bb56bb): src/os/linux/{layout,elf,sys,repl-term,sign}.f carry aarch64 image constants, syscall numbers and an ELF writer; x86_64 needs the same file set with the same word names so the image builders select a seam instead of branching. Acceptance: src/os/linux-x86-64/ with layout.f (the same DATA and guard-page model), elf.f (ELF64 for EM_X86_64, program headers, the relocation site kind for mov r64, imm64), sys.f (x86_64 syscall numbers and the syscall instruction sequence), repl-term.f and sign.f, selected by the target predicates docs/porting.md describes; a test that writes a minimal x86_64 ELF from the arm64 engine and checks its headers by field; docs/porting.md gains the x86_64 seam row. The x86_64 target contract itself (src/compiler/target.f arch variant x86-64, SysV ABI, little-endian, 8-byte pointers, its feature mask) is declared here too, since nothing else can name the target before the seam exists. Fixed at the 2026-09-17 checkpoint: predicate HB-TARGET-LINUX-X86-64?, target name linux-x86-64, every target.f defining all three predicates; the site kind MOVABS (10 bytes, patch offset 2 width 8, MOVABS-SITE? MOVABSV SET-MOVABS) lives in package SNAP-RELOC in src/habu/aot-decl.f, the declared owner of the recorded-site shape, not aot-lib.f; formal/Common/Reloc.v gains the model and three theorems with rows in test/compiler/reloc-axioms.txt; arch x86-64 and abi sysv-amd64 take wire code 5 each with exhaustive-MATCH arms in src/compiler/ir/schema.f, attr.f, type.f and test/compiler/target-policy.f; src/compiler/native/abi.f answers sysv-amd64 for the host (no refusal: the ABI records host identity, the missing backend is the registry's E-CTGT-UNLOADED) and src/habu/prof.f refuses the host at load with E-PLATFORM; the instruction-emitting parts of the seam (SYS, OS-OPEN-RD OS-OPEN-FLAGS OS-MMAP-FLAGS, the SYS-EMIT stencils, proc-control.f, proc-watch.f) need X64ASM and land with a second worker after habu-write-the-x86-fbaf3086, with the carry polarity of the -errno check chosen against SYS-PUSH's C-CS consumers (x86 sets CF on borrow, aarch64's cmp x0, #-4095 sets C on no borrow); the engine body (src/habu/habu1.f, bootstrap/cg/*.fs) belongs to habu-cross-build-the-d25a959d and crash.f's frames and the lib platform gates to habu-port-the-ffi-676f745d. Files: src/os/linux-x86-64/, src/compiler/target.f (the contract row only; the registry is habu-bind-compiler-targets-ff970b99's), src/habu/aot-decl.f (site kind), formal/Common/Reloc.v and test/compiler/reloc-axioms.txt, the source-list owners docs/porting.md names, docs/porting.md, test/. Verify: the ELF test on arm64; the file runs on the Intel machine once a backend emits code. Worker 1 handed back ec182203 (42 files; ELF header validated with readelf; formal rows built with rocq; target-policy domain 484 -> 516; 428/428 on its gen); on the line as d55021af (the predicate alone, the bridging generation every earlier engine needs) + 5e05cbfd (the seam), chains AN and AO. Deviations recorded: prof.f refuses the host with 'hb: this target's signal frame is not modelled' 76 die rather than E-PLATFORM, because it loads inside the engine-build window where lib/ is absent; SNAP-RELOC:MOVABS-IMM-OFF (2) and X64ASM:MOV-RI64-IMM-OFF (2) state one fact in two files and the site kind should read the assembler's constant now that X64ASM is in the tree (the emitter worker's first edit). Emitter worker handoff: sys.f SYS, ( n -- ) = ENC-MOV32-RI32 eax + ENC-SYSCALL plus the -errno reconciliation chosen against SYS-PUSH's C-CS readers (x86 CF is borrow); OS-OPEN-RD (openat with AT_FDCWD), OS-OPEN-FLAGS, OS-MMAP-FLAGS; the SYS-EMIT-WRITE/EXIT/SVC stencils become a byte string (5 + 2) the emitter appends, so the stencil surface changes from three constants to a buffer; proc-watch.f BPROCWATCHOPEN (pidfd_open), proc-control.f BKILLERRNO and BEXECVE publishing rax; then flip tools/build-fixpoint.f BF-APPEND-TARGET-SYS/PROC-WATCH/PROC-CONTROL from BF-TARGET-NO-EMITTERS to the files (delete the refusal with the last), tools/native-emit.f LOAD-IMAGE, tools/hb-build-lib.f HBB-KEY-LINUX-X86-64-SOURCES, tools/lint/shadow-lint.f; recovery stays a cross-build. Residuals: habu1.f (25) and crash.f (11) two-arm forms -> habu-cross-build-the-d25a959d; lib platform gates -> habu-port-the-ffi-676f745d; engine-size.f and imgdump.f admit only EM_AARCH64 images. Depends: none for the seam; habu-write-the-x86-fbaf3086 for the emitters. Ownership: hazel runs it on the arm64 host. Seam integrated 2026-09-17 23:48 (chains AN 69cfd0da + AO ae935de1, gate 434/434, line head 285941b3). Claim: agent=hazel-x64-emit workspace=.jj-ws/hazel-x64-emit (worker 2, the emitters).
