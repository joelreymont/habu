---
title: Cut over CHECK session
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-23T02:10:26.520974+02:00\""
blocks:
  - habu-own-boundary-lint-0cff8730
---

Why: checker callers mutate global cells, borrow caller spans, eagerly materialize source, and split selection from execution. This is the one atomic ownership and public-session cutover; splitting that seam would require a second authority or forwarding globals.

Owner and files: package CHECK owns tools/check-core.f and is reopened by tools/check-test-lib.f. Update tools/check.f, tools/check-main.f, and package GATE-DIAGNOSTICS in test/gate-diagnostics-lib.f. CHECK calls only the six-word qualified CHECKED-BOUNDARY-LINT API; provider behavior and storage remain exclusively provider-owned by habu-own-boundary-lint-0cff8730. In shared tools/check-core.f and tools/check-test-lib.f, the provider dot owns provider qualification and removal of private UB accesses; this dot owns session logic and public CHECK tests. Keep the six primitive TRUST axioms for CHECK!, TYPE-RESERVED?, CHECKER-DEFLINEAR, CHECKER-DEFRECORD, CHECKER-SCOPE-START, and CHECKER-SCOPE-DONE before package CHECK so their checker namespace does not change. All executable words, constants, buffers, and cells in check-core.f are private except MAIN ( -- ), RESET ( -- ), CAPTURE-OFF ( -- ), OPT ( ptr u8 n -- ), SOURCE ( ptr u8 n ptr u8 n -- ) with source then label, FILE ( ptr u8 n -- ), and RUN ( -- n ). Existing CHECK:TEST ( -- ) remains the sole test-library export. Do not rename unrelated private CHK-prefixed internals in this leaf.

Session contract: the process-local session is non-reentrant. RESET disables capture; clears capture pointers, capacities, and lengths; clears options, selection, copied paths/source/label, argument state, counts, and every materialized or run-temporary span. The private selection discriminator is exactly none, source, file, or list; zero length is data, never a sentinel. None means remaining stdin at RUN time. OPT accepts only json-errors, strict-signatures, all-errors, and source-list without leading dashes. The first three are idempotent booleans. source-list is idempotent, changes none to an empty list, changes one selected file to a one-entry list, and rejects source mode. Unknown options and selection collisions emit the existing usage line and throw 64. SOURCE is accepted only from none, copies source bytes and label before returning, permits zero bytes and a zero-byte label, performs no file or engine work, reports source capacity with the existing source-exceeds-capacity diagnostic and code 66, and rejects a label beyond FS-PATH-CAP with E-FS-CAPACITY. FILE copies each path before returning; from none it selects one file, from list it appends up to CHK-MAX-POS, and from source or a second non-list file it emits usage and throws 64. A path beyond FS-PATH-CAP throws E-FS-CAPACITY. An empty list rejects at RUN with the existing usage diagnostic and 64.

Execution contract: RUN alone checks bin/hb availability, creates and cleans temporary files, materializes the selection, executes every current static, preverify, child, and diagnostic phase, and returns the exact caught result instead of throwing it. Missing bin/hb stays code 69 with its current diagnostic; missing files and expanded source overflow stay code 66 with their current diagnostics. RUN always makes one final real cleanup attempt after the execution and private test-hook outcomes; the earliest nonzero execution or hook result remains the returned result. Successful cleanup clears every temporary owner registration, run buffer, counter, dependency record, and checker scope while preserving options and the copied selection. If real cleanup fails, retain exactly the root, source-path, and run-path ownership required to retry cleanup, clear all capture, option, selection, run, counter, dependency, and checker state, and return the primary result or cleanup result under the same earliest-error rule. RESET retries real cleanup, unconditionally clears every non-owner field, and rethrows a cleanup failure while retaining only that retry ownership. A source rerun after successful cleanup reuses the copied bytes and label; file and list reruns reread their copied paths; none consumes the remaining stdin on each run. Capture configuration is private test support. A completed RUN leaves its captured output readable until the next RUN, RESET, CAPTURE-OFF, capture reconfiguration, or cleanup failure; those operations clear capture lengths, and CAPTURE-OFF also clears borrowed capture pointers and capacities.

Checked-boundary integration contract: empty CHECK:SOURCE reaches the real boundary scan through CHECKED-BOUNDARY-LINT:FILE. Provider qualification exists only on the same atomic tree that publishes the provider API. No CHECK word reads provider-private state or publishes a test bridge.

Command contract: MAIN calls RESET, parses the existing CLI spellings --json-errors, --strict-signatures, --all-errors, --source-list, and -- exactly as today through the public operations, calls RUN once, and throws any nonzero result. tools/check.f and tools/check-main.f invoke only CHECK:MAIN. GATE-DIAGNOSTICS configures checks only through public CHECK words. tools/check-test-lib.f uses bare private access only while package CHECK is reopened; do not export a test bridge.

Acceptance: prove caller-buffer mutation cannot change pending source, label, or file selections; empty source differs from none and reaches public RUN; option repetitions and order are stable; every mode collision, path/source/label capacity, empty list, missing file, and missing engine outcome is exact through the public or command entry point; repeated success and ordinary failure leave no temporary owner or run state and preserve selection; injected pre/post cleanup errors still trigger final real cleanup; a persistent real cleanup failure retains only retry ownership, clears every other session field, preserves primary-error precedence, and is recoverable through RESET after the filesystem obstruction is removed; captured bytes have the specified lifetime. The public empty SOURCE/RUN path traverses the real qualified provider rather than bypassing the static phase, and a mutation removing CHK-RUN-BOUNDARY fails. The actual stdin, one-file, source-list, preverify, all-errors, JSON, nominal, package, and diagnostics entry points remain byte-exact. Implement and squash this dot with habu-own-boundary-lint-0cff8730 in the same workspace and green commit; neither may land or close independently. Run tools/check-test.f, the checked-boundary focused provider cases, real tools/check.f stdin/file/list/missing-engine probes, gate diagnostics diag-undef-primary, exact ownership/type checks, host lint, and file-map lint. Forbidden: borrowed selection spans, eager SOURCE materialization, sentinel lengths, inferred modes, a public list or capture-on API, aliases, exported storage, compatibility globals, provider qualification before the provider API exists on the same tree, losing ownership of an undeleted temporary tree, private UB access, or a second session authority. Claim: agent=check_cutover workspace=.jj-ws/habu-cut-over-check-ac1b7cdf.
