---
title: Propagate CAD store to replay child
status: active
priority: 2
issue-type: task
created-at: "\"2026-07-19T08:21:55+02:00\""
---

Why: maki/cad-test.f writes selection 7 through the real durable schedule store, then RPL-CHILD-TILE$ starts a fresh engine with RUN-ARGV-CAPTURE. That primitive intentionally supplies an empty environment. When the parent has a private HABU_CAD_STORE, the child silently falls back to tmp/cad-store, exits successfully, and renders the default schedule instead of replaying the parent's row. The canonical private-store production test fails at assertions 108 and 109. A strace of the exact path proves the parent opens the configured schedules.rows while the child's execve receives zero environment rows and never opens that file. Owner: maki/cad-test.f only, inside package MAKI. Exact result: require the existing process environment module, reset the argument and environment builders together, pass exactly one HABU_CAD_STORE row whose value is the already-resolved STORE-ROOT$, and use RUN-ARGV-ENV-CAPTURE for the existing replay child. Preserve the explicit engine path, arguments, output/error capture, timeout, store format, replay keys, cleanup, and every assertion. Forbidden: raising the timeout, using the ambient default store, inheriting the whole environment, hard-coding a path, copying rows, weakening replay assertions, adding a fallback, changing production store or process primitives, or adding an alias. Production failure: with HB_TMP and HABU_CAD_STORE set to distinct private roots, bin/hb --load maki/cad-test.f exits 1 after the child exits 0 because its output contains the default schedule and omits the stored-selection replay line. Acceptance: the same command is red on the parent and green on the change; strace of the changed child shows one explicit HABU_CAD_STORE environment row and an open of the parent's schedules.rows; the default-store invocation remains green; maki/test.f, typed-local diff lint, package diff lint, and the focused process-environment suite pass. Depends: none. Claim: agent=cad_child_env workspace=.jj-ws/habu-fix-cad-child-f9222292.
