---
title: Keep the recovery stack fixture checked
status: active
priority: 2
issue-type: task
created-at: "\"2026-09-16T14:18:00.812359+03:00\""
---

Problem: test/bootstrap-engine-stack.fs:26-44 (commit 37a58f9e) prefixes every baked program with `0 set-check`, which clears the checker AND the compile preflight (bootstrap/cg/forth.fs:1410-1416 BSETCHECK) for BES-ABI, BES-RETURN, BES-LOOP, BES-LIFECYCLE and BES-DEBUGGER, all of which compiled checked before; the justifying comment says an axiom would be harder, but the deleted fixture used exactly `PRIM: bes-preserve PRIM;`. BES-SRC-BUF (:33-39) is a 4096-byte buffer copied into with no length check; BES-DEBUG-RUN already reaches 3208 bytes and overflow corrupts the gforth dictionary silently. bootstrap/cg/forth.fs:163 spells the refusal code STACK-ABI:E-UNGUARDED while the owner src/habu/stack-abi.f:54 spells STACK-ABI:E-STACK-UNGUARDED (also forth.fs:1358,1382 and test/bootstrap-engine-stack.fs:79,108); tools/error-code-lint-core.f does not walk bootstrap/ so nothing enforces parity. forth.fs:345,353 RSTK-CELLS and LOOP-STK-FRAMES lost their only consumers in 85149742. bootstrap/cg/crash.fs:95-98 claims DATA is undefined when it loads, but forth.fs:91 defines `20 constant DATA` and `require crash.fs` is at :173, so the register number is a bare literal. bootstrap/cg/jit.fs carries a dead JIT-STACK block (flagged by the mirror-fold lane). bootstrap/cg/walk.fs:13 `require crash.fs` fails in gforth at crash.fs:146 (RBASE-VA undefined), so bootstrap/habu-cg.fs cannot load (pre-existing). Acceptance: every baked program in the fixture compiles checked; a test-only primitive gets `PRIM: name <effect> PRIM;` in the source that calls it, spelled like src/core/checker.f:6867 run-in-stack; the `0 set-check` prefix and BES-SRC-BUF are gone (or the copy is bounded with a named abort); the mirror constant is STACK-ABI:E-STACK-UNGUARDED at every site; the two dead constants are deleted or their parity purpose is stated beside them; crash.fs spells `DATA 8 *` like src/habu/crash.f:91 and its comment is true; the dead JIT-STACK block is deleted; walk.fs loads or habu-cg.fs is shown dead and deleted. Regression: `gforth test/bootstrap-engine-stack.fs` and `gforth test/bootstrap-wide-memory.fs` green; the stage0 chain `tools/bootstrap.sh` reaches "bootstrap check OK". Files: test/bootstrap-engine-stack.fs, bootstrap/cg/forth.fs, bootstrap/cg/crash.fs, bootstrap/cg/jit.fs, bootstrap/cg/walk.fs, bootstrap/habu-cg.fs. Verify: the two gforth fixtures; tools/bootstrap.sh on a quiet machine. Depends: none. Ownership: bootstrap/cg/*, bootstrap/habu-cg.fs, test/bootstrap-*.fs. Claim: agent=hazel-audit-seed workspace=.jj-ws/hazel-audit-seed. Source: Opus audit of 37a58f9e and 85149742 (2026-09-16), findings M2 M3 and minors.
