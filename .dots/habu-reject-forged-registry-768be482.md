---
title: Reject forged registry cycles
status: open
priority: 2
issue-type: task
created-at: "2026-07-27T11:31:56.115960+02:00"
---

Follow-on capability split out of the family-schema round-4 correction (codex-d3dd/codex-2234 ruling): the family-schema query is pure and defends nothing - a mutual pointer ring among family schemas is unexpressible through production declarations (the declaration-order registry is a DAG outside direct pointer self-edges), so a ring forged through trusted registry-level writes is CORRUPT METADATA, and its owner must reject it at the write, not oblige every downstream query to tolerate it. Owned result: identify the owning registry validator for trusted family-schema mutation (the seam the deleted forged-ring fixture used), add an exact rejection of cycle-creating writes with a named error, and a negative regression proving the forged-ring construction from the deleted type-family-suite fixture now refuses at the write site. If no owning validator seam exists for trusted writes, that finding comes back to the orchestrator before any implementation - do not invent a new authority ad hoc. Depends on the family-schema round-4 atomic commit landing (it deletes the fixture this dot's regression reuses).

RESHAPED (2026-07-27, codex-1394 objection accepted): this is a BLOCKED DESIGN PARENT with NO implementation claim, and it is NOT part of the repair train - round four does not depend on it. The original "identify the owning validator; if none exists, report back" text was a research assignment, which a dot must never be. Forged trusted registry writes are outside the engine critical path. An implementation leaf may be added under this parent only after design freezes ALL of: the exact write owner, the interface, production reachability (or the explicit ruling that only trusted tooling can reach it), the named failure code, and the real failing command that witnesses the rejection. Until then this dot only preserves the design finding: mutual pointer rings among family schemas are unexpressible through production declarations, so a ring constructed through trusted registry writes is corrupt metadata whose rejection belongs to the write owner, never to downstream queries.
