---
title: Protect TYPE-FIELD private state
status: active
priority: 1
issue-type: task
created-at: "2026-07-17T12:11:01.362381+02:00"
---

Review finding: src/core/type-family.f:712-1130 leaves PF constants, variables, arenas, and raw record helpers in the global data namespace; src/core/internal-mark.f exempts data records, so bare source can obtain and mutate registry cells beside the documented TYPE-FIELD package. Fix: place all PF storage/constants/helpers in TYPE-FIELD private scope, provide the smallest colon-only internal builder seam needed by sumtype/type-family/tests, retain only COUNT/NO-VARIANT/FIND/EACH/reflection as checker-visible public API, protect both WIDs, migrate every consumer atomically, and add bare-load/internal-word negatives for raw state and builder names. No compatibility aliases. Acceptance: raw PF data/mutator names reject on --load and stdin; documented TYPE-FIELD API remains checked; type-family, declaration, rollback, internal-word, seal, fixpoint, trust, host, filemap gates green. Files: src/core/type-family.f:712-1154, src/core/sumtype.f, src/core/checker.f, test/type-family-suite.f, test/type-family-rollback-suite.f, test/internal-word-gate.f, docs/type-families.md. Claim: agent=ptf-opus workspace=.jj-ws/habu-protect-type-field-04d91409 (claimed 2026-07-18 by the Mac planner; dot already active, dot-on NOT re-run per protocol)

RE-BLESS 2026-07-18 (Mac planner, after ptf-opus STOP report; supersedes the Option-2 seam): the name-visibility seam is UNIMPLEMENTABLE as written. Proven rule M1: a word can name a private word only if its own definition sits inside an open package block — no qualified escape, no post-close access. The PF data-record names are reached from globally-pinned cross-file API (CHECKER-DEF*/-NOEND via verify-source.f:448-505 and check-core.f:718-798, PRIM rows checker.f:4772-4776; TFAM-RESET type-family.f:1342; TFAM-CON-LIN-XT execute sites checker.f:1231-1277), so the reopen surface cascades to ~40 relocated words + 8-plus xt bridges and cannot terminate at ~4 words. NEW BLESSED DESIGN = friend-band memory protection (ptf-opus Design C), matching internal-mark.f:34's own architecture ("the truly dangerous engine cells are owned by the PROT-GUARD friend bands, not by name visibility"): (1) place the PF writable cells (PF-CAP-V PF-A-BOOT PF-A-P PF-N PF-COMMIT-N PF-TX-CAP-V PF-TX-BOOT PF-TX-P PF-TX-DEPTH PF-TX-SERIAL; type-family.f:781-919) in a PROT-GUARD band; (2) declaration-time writes run inside a narrow internal-marked bracket (RW window) riding the existing LPROT/PROT-GUARD machinery — bare source cannot open the bracket (colon internal-mark min-in is proven fail-closed by the E3 probe); (3) names may stay global: the WRITE is what is guarded. ACCEPTANCE RESTATED: bare checked source cannot mutate any PF registry cell (bare write traps with a named error, negative fixtures on --load and stdin); the bracket words themselves reject bare (internal-word negatives); documented TYPE-FIELD public API unchanged; type-family/declaration/rollback/internal-word/seal/fixpoint/trust/host/filemap gates green; byte-fixpoint proven. PHASE 1 deliverable: the band design + a BF-BUILD-ALL temp-engine PROTOTYPE proving a guarded PF cell traps a bare write while admitting the bracketed declaration path; full rollout only after prototype review. Investigate en route: whether TDECL-MARK/RESTORE's PF snapshot is redundant given PF transactions (drop if proven). The sibling-registry pattern is now the BAND, not a package seam (see habu-protect-sibling-registries dot). Accessor renames (PF-N@ collision) are moot under this design.

PLAN REVISED 2026-07-18 (Joel: a check-time reject is not discarded without proof). Phase 1 proved the band is buildable, but it also exposed that the bare-name gap is a MECHANISM LIMIT, not a design choice: the internal-word mark rejects bare use by checking minimum stack inputs, so zero-input words (variables, arenas) are invisible to it. Primary enforcement therefore moves to CHECK TIME: extend the internal-word mark to key on the mark itself rather than the arity, so a marked zero-input word rejects at bare interpret and inside user checked definitions while core still compiles it (the PF-ADD precedent shows the mechanism already separates core compilation from user use). Mark the ten PF cells. The runtime band (reg-cell! + REG-BAND, designed in docs/registry-band.md) is DEMOTED to a conditional defense-in-depth option: build it only if an audit shows a checker-visible public word returns a pointer into PF storage (a laundered address the name-level reject cannot catch) and the leak cannot be fixed by returning copies instead. The TDECL-MARK/RESTORE snapshot drop (proven redundant) plus its negative regression stay in scope.

SEAM BLESSED (orchestrator, 2026-07-18, from the fable-spark wave's proven analysis): OPTION 2 — colon builders stay in-package (uniform bare-reject; defers are UNSAFE for din=0 builders — proven bare-executable via missing min-in mark). Narrow explicit reopen wrapper in sumtype.f limited to the ~4 PF-touching words, plus colon accessors PF-N@/PF-N!/PF-COMMIT-N@/PF-COMMIT-N! for the TDECL-MARK/RESTORE high-water snapshot. "Smallest colon-only seam" wording is CORRECTED to this; read-only PF constants may stay global (not a mutation vector) — document which. CONFIRMED EXPLOIT driving this: bare `99 PF-COMMIT-N !` corrupts TYPE-FIELD:COUNT through the public API; internal-mark.f:28-34 exempts data records by design. The seam set here is the PATTERN for the 6 sibling registries (TFAM-N, SUMV-N, TF-STR-U, TF-PK-N, LAY-N, SCH-N, SCH-ROOT-N — all equally exposed); mint the sibling dot when this lands. High fixpoint risk: BF-BUILD-ALL temp-engine prototype mandatory.
