---
title: Split gate size test entry
status: active
priority: 2
issue-type: task
created-at: "2026-07-27T10:44:48.847564+02:00"
---

Invariant: reusable SIZE-ATTR manifest definitions load once through a side-effect-free library; executable test entries require that library and run assertions exactly once. Cause: test/gate-runner-support.f requires test/gate-engine-lib.f, which required executable test/gate-size-attribution-test.f; tail-pure later fork-includes the same file and the inherited dictionary rejects duplicate private CODE-OFF with rc 78. Fix: add test/gate-size-attribution-lib.f for the manifest and public API, make test/gate-size-attribution-test.f an exact require plus SIZE-ATTR:RUN entry, require the library from test/gate-engine-lib.f, keep all new size helpers private in package GATE-SIZE behind public BUILD-FIXPOINT, package the changed build caller as GENG:BUILD-SLICE, package the public engine entry as GATE-ENGINE:MAIN, update test/gate-engine.f to call it, prove the preloaded library plus exact two-statement entry executes once, and update the canonical path references. The lexer-contract child under habu-migrate-row-scanners-0be97401 owns the structural proof that the production scheduler still contains exactly one live fork-include row. Files: test/gate-size-attribution-lib.f, test/gate-size-attribution-test.f, test/gate-engine-lib.f, test/gate-engine.f, test/gate-runner-entry-test.f, STATUS.md, docs/size-rca.md, docs/worker-briefing.md, test/gate-build-size.f, test/match-factor-pin.f, LESSONS.md. Forbidden: a legacy GE-BUILD-FIXPOINT alias, package-prefixed tails inside their owning package, changing GSI-FORK-INCLUDE to required, removing or skipping the test row, or reordering dependencies to hide the collision. Acceptance: the size library bare-loads silently; the exact two-statement entry prints one SIZE-ATTR:RUN completion; test/gate-engine.f resolves only GATE-ENGINE:MAIN; GENG:BUILD-SLICE and GATE-ENGINE:MAIN call GATE-SIZE:BUILD-FIXPOINT with no global compatibility alias; tail-pure, runner-entry, typed-local diff, package diff, host, dot-dependency, and match-factor-pin gates pass. Base: d380e286cc577463e623f0ac20c9cbbd7e650405. Claim: agent=gate_size workspace=.jj-ws/gate-size-entry
