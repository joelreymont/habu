---
title: Adopt wide and parametric typed locals across the codebase
status: open
priority: 2
issue-type: task
created-at: "2026-09-19T18:23:03.521709+03:00"
---

Problem: Joel (2026-09-19): the wide/multi-cell and parametric typed-local features exist to improve the codebase, so every site that destructures a layout value only because a typed local could not hold it must be found and converted. Today src/, lib/, tools/ and test/ carry the workaround shapes the handoff names: unpack-then-repack around a use (X:UNMAKE ... X:MAKE), locals exploded into scalar cells ({: a b c d :} over a two-field value), adapter words that exist only to name pieces, swap/rot juggling around MATCH payloads. Acceptance (alder, after each feature dot lands and its engine is integrated): (1) an audit listing every candidate site by file:line with the shape it exhibits, measured by reading, over src/ lib/ tools/ test/ and, read-only, the consumer trees (loom, maki, kiba, radar, Tender - reported to their owners, never edited here); (2) migrations in commit-sized groups per subsystem, each keeping behaviour (its suites unchanged in what they assert), each reviewed on the line before it chains, converted sites named in the commit body; sites in a live hazel lane (span band 1 files, the native compiler, checker.f) are handed to that lane's owner instead of edited; (3) the card (docs/forth-card.md) and docs/forth.md state the idiom: a domain value stays a named local, destructure only to compute. Depends: habu-bind-a-wide-bc67d207, habu-parse-local-annotations-50be4d43. Ownership: alder (audit and migrations), hazel (review, the lane-owned files). Claim: agent=alder workspace=.jj-ws/alder-review-fixes. Hazel-owned native sites (alder's audit, 39d9a399 line numbers; each unmakes a domain value into scalar locals and rebuilds it unchanged): src/compiler/native/tape.f:232 MK (span repack), :587 VERIFY (digest repack); src/compiler/native/regalloc-verify.f:1029 SLOT-CK (the 14-field routine rebuilt per slot), :2344 ACCEPT (the same for readers and functions); src/compiler/native/select.f:3469 SELECT and regalloc.f:2180 ALLOCATE (entry explosion, repeated remakes); src/compiler/native-effect.f:959 CHECK-SLOT (drop chain to frame/machine); tests test/compiler/native-chain-fixture.f:69 SELECTED (routine transport), test/compiler/native-tape.f:699 DG-BODY, :721 DGB-BODY, :743 DGE-BODY, :769 VFX-BODY (digest transport). NOT a substitution site: regalloc.f:2053 SLOTS-CK rebuilds the routine with the measured frame size in place of the declared one on purpose. These migrate inside the x86-64 lanes that touch those files, after the wide-local engine is integrated (a routine local is 14 cells).
