---
title: Protect DECL-EVENT publication owners
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-24T16:57:44.992204+02:00\""
---

Why: DECL-EVENT owns mutable declaration publication state, but its public and private wordlists are absent from the production protected-wordlist registry. After the friend latch closes, hostile source can reopen the package or publish through either wordlist, bypassing the package boundary around the declaration-event transaction. Owner: src/core/decl-event.f and the existing production hostile-package fixture test/seal-package.f only. Exact result: after every public definition exists and DEV-PART-INSTALL has installed the generated-declaration participant, switch to the private wordlist and register get-current with prot-wid-add, switch to the public wordlist and register get-current with prot-wid-add, then close the package. Add one focused SPK-DECL-EVENT test owner that drives the existing load, stdin, and direct-subject process seams. It must prove package DECL-EVENT reopening, publication after set-current to the namespace record's public wordlist, publication after set-current to its private wordlist, and a qualified DECL-EVENT:HACK definition all exit with ENGINE-ERROR:SEAL-PACKAGE before a dictionary record appears. Resolve both wordlists only through the existing production XREF namespace/record API; add no test bridge. Preserve test/decl-event-suite.f byte-for-byte and run it as the complete public lifecycle and failure-atomicity proof. The generic protected-wordlist AOT/snapshot suites remain the persistence owner; the rebuilt-candidate hostile test proves these exact two registrations reach the produced engine. Forbidden: changing any declaration-event API or transaction body; exposing COMMIT, FINALIZE, private words, wordlists, or registry membership; adding TRUSTED, set-check, raw declaration families, a registry, state flag, alias, forwarding word, runtime guard, copied state machine, or private-storage assertion; requiring read-only tick/bracket-tick/postpone behavior to change. UNDEFINE, raw XREF readers, execute, and other protected-wordlist sink hardening remain owned by habu-tfam-2b-iii-545a815f and are not evidence for this registration leaf. Production failure: on verified master, all four hostile publication paths succeed and can add HACK to a DECL-EVENT wordlist. Acceptance: each hostile path is red on the parent and exits 84 with the canonical seal diagnostic on the change through load, stdin, and direct-subject execution; removing either registration makes its matching set-current attack fail the test; package-diff, typed-local, trust, protected-wordlist inventory, declaration-event, seal-package, AOT/snapshot registry, candidate-validation, and full native gates pass. Depends: the existing protected-wordlist registry and publication/reopen guards on verified master. This leaf blocks the declaration-event publication guard and unified ENUM finalization.

Claim: agent=seal-decl-event workspace=.jj-ws/habu-seal-decl-event-corrected
