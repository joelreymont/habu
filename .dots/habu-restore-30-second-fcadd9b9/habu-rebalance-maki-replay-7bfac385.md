---
title: Rebalance Maki replay slice
status: active
priority: 1
issue-type: task
created-at: "\"2026-07-24T02:49:48.836499+02:00\""
blocks:
  - habu-run-focused-cad-ba7e3860
---

Why: on verified master 9a7c94eb, one quiet exact run measured maki/test-core.f at 19.74 s; its fresh-process maki/cad-replay-test.f member alone costs 4.365 s. The existing parallel maki/test-eval-emit.f slice is 9.58 s. Candidate build followed by the 19.74 s sequential core slice is the cold gate long pole. Owned result: after habu-run-focused-cad-ba7e3860 lands and gives the focused replay case and wrapper private CAD stores, move the exact SUITE maki/cad-replay-test.f registration, unchanged and exactly once, from maki/test-core.f to maki/test-eval-emit.f after its existing three emit suites. Update only the two slice headers to describe the scheduling partition accurately. The full maki/test.f inventory, test body, runner mappings, labels, temporary roots, thresholds, result caching, and every other suite stay byte-identical. Dependencies: the focused replay repair, verified master, and the existing four-slice runner; no type-system dependency. Package owner: these files contain only TEST package suite DSL calls and no definitions. Checkpoint before edit: both exact standalone slices green; record core, emit, and replay timings; run the existing native suite-coverage lint and show the replay member is counted once. Run the two slices concurrently with distinct HB_TMP roots and prove their private CAD stores do not overlap. Stop on any hidden order dependency, shared store path, package-gate rejection, or need to edit run-lib, the coverage linter, a test body, or a threshold. Acceptance: both modified slices pass through bin/hb with private HB_TMP roots; suite-coverage lint reports zero findings and mutation by leaving the replay member in core reports a duplicate while removing it reports missing coverage; three quiet paired runs keep every test and show the median longest slice at least 3.0 s below the 19.74 s baseline without increasing the sum of the two slice walls by more than 10%; the orchestrator runs the combined exact cold native gate only after review and integration. Forbidden: copied tests, reduced loops, skipped device paths, cache changes, threshold changes, retries, new slices, runner changes, or CAD implementation edits. Files: maki/test-core.f and maki/test-eval-emit.f only. Claim: agent=maki_rebalance workspace=.jj-ws/habu-rebalance-maki-replay-7bfac385.
