---
title: Bound the record stride by a reach
status: open
priority: 2
issue-type: task
created-at: "2026-09-18T19:55:50.507015+03:00"
---

Problem (adversarial review of 534734bb..a6ce705f, HIGH, probe recat.f): the DERIVE addr surface's F:AT (src/core/structure-make.f record-at row; src/core/checker.f RECORD-AT-EXACT? / RECORD-AT-STEP; generated ': F:AT ( ptr F n -- ptr F ) W * record-at ;') reopens unbounded arithmetic on a record pointer that the layout fence and TYPED-BUFFER's own index accessor refuse: with '2 TYPED-BUFFER TB sdaddr', '[: 5 TB drop ;] catch .' answers 7122 (index refused) but '0 TB 1000 SDADDR:AT SDADDR:A @' answers 0 and '12345 0 TB 1000 SDADDR:AT SDADDR:A !' then reads 12345 - a certified body reads and writes 8 KB past a 16-byte buffer. Before ba63866e a ptr F only came from a bounds-checked accessor; docs/type-system.md says in one paragraph that + and cell+ refusing layout pointers is what makes a record pointer safe and that record-at's bounds remain the caller's, and the second undoes the first. Acceptance: the stride is bounded by a reach the checker can see - decide between (a) generating F:AT over a reach-carrying value (a span over records: measure whether span<F> is declarable for a layout pointee - type-system.md 10.2 says a layout pointee does not unify with a one-cell var - and if not, a records family or the TYPED-BUFFER's own index as the only stride) and (b) removing F:AT and the record-at row entirely, so a record pointer again comes only from a bounds-checked accessor and iteration goes through the buffer's index (i TB), which today loses nothing in the tree (no tree user of F:AT; measure); take (b) unless (a) is measured sound within the lane; red-first fixture: the recat.f shapes refused (7122 or by name) in test/structure-decl-suite.f case 15; docs/type-system.md 10.4 rewritten to the landed shape. Files: src/core/structure-make.f, src/core/sumtype.f (TDECL-ADDR-AT-WORD), src/core/checker.f, test/structure-decl-suite.f, docs/type-system.md, docs/type-families.md. Verify: the probe; three generations with cmp; test/run.f. Depends: none. Ownership: type system. Parent: habu-campaign-c2-mem-c3d7662b. Claim: unassigned.
