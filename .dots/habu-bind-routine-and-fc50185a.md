---
title: Bind routine and digest values whole in the native passes
status: active
priority: 2
issue-type: task
created-at: "2026-09-19T19:42:15.490287+03:00"
---

Problem: the native compiler's transport sites destructure a layout value into scalar locals and rebuild it unchanged, only because a typed local could not hold it before 648f4ce7 (wide locals) and b4efad25 (parametric annotations). Sites from the adoption audit (habu-adopt-wide-and-12aa121b, hazel section): src/compiler/native/tape.f:232 MK (source span unpacked into three locals and reconstructed unchanged), tape.f:587 VERIFY (four digest words preserved only for reconstruction), src/compiler/native/select.f:3469 SELECT (routine exploded at entry and remade for later consumers: keep one value and project only the fields used), src/compiler/native-effect.f:959 CHECK-SLOT (long drop sequence reaches frame size and machine: a named routine uses the existing readers), test/compiler/native-chain-fixture.f:69 SELECTED (fourteen routine locals survive setup only to reconstruct the selector argument), test/compiler/native-tape.f:699 DG-BODY, :721 DGB-BODY, :743 DGE-BODY, :769 VFX-BODY (digest rebuilt for comparisons). regalloc.f:2180 ALLOCATE and regalloc-verify.f:1029 SLOT-CK / :2344 ACCEPT are the same shape but belong to the allocator-vocabulary lane (habu-bind-the-register-623e83ff) and follow it. Acceptance: each listed site binds the value whole ({: r:fam :} or {: r:fam<..> :}) and projects fields through the family's existing readers; reconstruction sites in those files 0, DISCARD-style drop runs 0 (state the before/after counts per file); no behavior change: every suite unchanged in what it asserts; the arm64 engine rebuilds through three generations with gen2 == gen3 and the full suite green. Files: src/compiler/native/tape.f, src/compiler/native/select.f, src/compiler/native-effect.f, test/compiler/native-chain-fixture.f, test/compiler/native-tape.f. Verify: test/compiler/native-tape.f, native-chain.f, native-select.f, native-effect suites; test/run.f. Depends: none. Ownership: hazel. Claim: agent=hazel-native-adoption workspace=.jj-ws/hazel-native-adoption.
