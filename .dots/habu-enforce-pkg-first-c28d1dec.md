---
title: Enforce package-first Forth modules
status: active
priority: 2
issue-type: task
created-at: "2026-07-15T21:54:27.612398+02:00"
blocks:
  - habu-lint-diff-share-486c2d86
---

Full context: docs/forth.md:91-123 requires real package namespaces and forbids raw global prefix stems, but tools/namespace-lint-core.f only enforces maki and exempts tests, so generated tools/tests/libs can still introduce LRD-MISSING$/TST-* globals. Fix: add a checked Habu diff gate that parses each complete current Forth file, correlates added lines to enclosing definitions through DIFF's canonical section/old-new identity/form/body and hunk events, and rejects every new or changed definition outside package scope in lib/, tools/, test support, maki/, and src subsystem files. Exempt only an explicit versioned core/prelude path manifest and global `constant E-*`; do not infer policy from word prefixes or exempt whole directories/files. Use the canonical compiler definer registry case-insensitively; recognize colon, atomic, structure, enum, and legacy block spans; fail closed on malformed source, stale context/add bytes, unsafe or unreadable identities, missing files, malformed package scopes, new/copied files without hunks, and unsupported diff forms. Pure delete/rename/mode-only changes do not touch surviving definitions; unrelated existing globals in a touched file remain legacy debt. Require package-local short private names and public NAME:WORD APIs by construction; integrate the gate into the native commit/full-gate path; migrate every new word touched by this goal rather than allowlisting stems. Acceptance: fixtures reject raw-prefix and unprefixed global defs, upper/lowercase instances of every definer, changed body lines whose opener is outside the hunk, new/copied global files, package underflow/nesting/unclosed scope, fake tokens in comments/strings, and stale/malformed change artifacts; valid private/public/reopened packages, error constants, explicit core/prelude rows, unrelated legacy globals, and pure delete/rename/mode-only changes pass. Live diff is clean; typed-local, focused lint tests, host/filemap/dot, maki, ptx-stdlib, fixpoint, and full native gates pass. Files: new package-diff lint core/runner/tests, explicit core/prelude manifest, owning gate manifest, and FILEMAP after active owners release them; docs/forth.md rule is already normative.
