---
title: Fence a base address at every pointee depth
status: open
priority: 1
issue-type: task
created-at: "2026-09-18T12:50:13.874470+03:00"
---

Problem (adversarial review of the 2026-09-18 batch, engine 3e56365e, probes p1-p30 in hazel's scratchpad): the TVK-BASE fence landed by e69cb928 stops at the FIRST pointee (src/core/checker.f BASE-BLOCK?, the CUR-STRICT permissive arm; FENCE-WHY), so a declared '( -- ptr ptr T )' over data-base or NULL-PTR forges both a nominal identity and an address: 'D1 ( -- ptr ptr n ) data-base 8 +' certifies, 'D2 ( -- ptr ptr dthing ) data-base 8 +' certifies, 'D7 ( -- ptr ptr n ) NULL-PTR' certifies, while the RAW rule refuses the same depth ('( -- ptr ptr n ) DRV' refused); run end to end ': P6B ( -- ptr ptr ptr n ) data-base 8 + ; : W ( n -- ) data-base 8 + ! ; : BOOM ( -- n ) P6B @ @ @ ; 1 W BOOM' certifies and SIGSEGVs, and ': P6A ( -- ptr ptr pthing ) data-base 8 + ; : RD ( -- pthing ) P6A @ @ ;' mints the headline nominal out of engine DATA. Second hole, same lane: NP-KIND-EXCUSED? (checker.f, the input-only quantifier excuse) hands a data-base-derived pointer to a caller's CONCRETE pointee: ': P1STASH ( ptr ptr a -- ) data-base 8 + swap ! ;' certifies (a occurs only in inputs), then 'TYPED-VARIABLE P1PP ptr ptr n : P1CALL ( -- ) P1PP P1STASH ;' certifies and 'P1PP @ @' is a ptr n built from an arbitrary DATA word (1 W P1PP @ @ @ SIGSEGVs; with ptr pthing it forges pthing); the quotation shape ': FEED ( [ ptr a -- ] -- ) data-base 8 + swap execute ;' + '[: ( ptr fthing -- ) @ drop ;] FEED' certifies too. Cause: the permissive arm exists only so the honest null uses keep certifying (ptr thing NULL-PTR =, NULL-PTR X !, -, 0=), and the excuse cannot tell the null RELEASE stores from a data-base offset because both wear TVK-BASE. Acceptance: split the kind — TVK-NULL (NULL-PTR: permissive inside a pointee, the honest uses kept) and TVK-DBASE (data-base: fenced at EVERY pointee depth, the chain followed through ptr ptr … T), the lattice ANY < NULL < DBASE < RAW or whatever the meet needs, the input-only excuse limited to TVK-NULL (a data-base-derived pointer is never excused; RELEASE's null store still certifies); every probe above refused by name (E-RAW-CELL-PTR family, own reason), the D4/D5-style RAW controls unchanged, the fixture test/compiler/base-pointer-arith-refusals.f gaining the depth-2/3 and excuse shapes red-first plus the null controls; the 28 accessor declarations and lib/genio.f ENGINE-ROW ( n -- ptr [ ptr u8 n -- ] ) re-verified; a rule-hosted generation build; docs/effects.md section corrected. Files: src/core/checker.f, src/core/render.f, src/habu/prims.f (A-BASE split), src/core/cell-effects.f (NULL-PTR row), test/compiler/base-pointer-arith-refusals.f, docs/effects.md. Verify: the fixture; three generations with cmp; bootstrap check; test/run.f. Depends: none. Ownership: checker. Parent: habu-campaign-c2-mem-c3d7662b. Claim: unassigned.
