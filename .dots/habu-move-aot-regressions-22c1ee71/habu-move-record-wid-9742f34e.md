---
title: Move record WID proof
status: open
priority: 1
issue-type: task
created-at: "2026-07-24T03:18:55.696411+02:00"
blocks:
  - habu-separate-stdin-build-18628757
---

Why: ACAP-WID-SELFTEST fabricates one compact record and executes during every production stdin metabuild. Its invariant belongs in a selectable test running inside the real metabuild host. Dependency: habu-separate-stdin-build-18628757 supplies the parser-free alternate entry seam. Owned result: delete only ACAP-WID-SELFTEST and its immediate call from src/habu/aot-capture.f. Add a private AOT record-WID builder that emits an alternate STDIN-BUILD entry, captures the real REPL, drives the real ACAP-COMPACT-RECS and ACAP-PROVE-RECS over one synthetic dictionary record whose u32 word-list id is 1000, proves every compact/expanded field including the id, calls the real ACAP-RESET, proves AOT-REC-N and AOT-NAMES-LEN return to zero, and exits before EMIT-FORTH. Register its positive and committed corrupt-record cases through the existing AOT WID suite. The negative changes the compact id after the real encoder and before the real prover; it must fail with RECORD EXPANSION MISMATCH. No copied codec, source parser, production test hook, public helper, engine emission, or protected-WID behavior change. Checkpoint: on the exact entry-seam tree, baseline real codec proof and representative alternate entry through package/typed-local gates; stop on any inaccessible production word, need to publish STDIN-BUILD or ACAP internals, source splice, changed record format, or file outside the list. Acceptance: positive exits zero with proof and reset markers; corrupt case exits the exact nonzero path and names the real prover mismatch; truncating the production u32 codec to u8 makes the committed negative or positive fail; removing either real codec call, the id assertion, any record field, or either reset assertion fails. Normal stdin build output stays byte-identical after self-test removal. Files: src/habu/aot-capture.f, new test/aot-rec-wid-build.f and its private generated entry, test/aot-wid-suite.f, test/gate-stdlib-cases.f and suite-coverage inventory only if a new suite registration is required, test/run-files.f, FILEMAP.md, and TRUSTED.md only if exact inventory requires. Smallest checks: bin/hb --load test/aot-rec-wid-build.f and bin/hb --load test/aot-wid-suite.f. Run exact package, typed-local, trust, filemap, suite coverage, positive/negative AOT, candidate, fixpoint, and touched native gates; root batches the full native gate. Claim: unassigned.
