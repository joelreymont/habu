---
title: Bind transaction COMMIT-ALLOCATED
status: closed
priority: 2
issue-type: task
created-at: "2026-07-26T13:44:33.324532+02:00"
closed-at: "2026-08-02T15:42:53.260121+02:00"
close-reason: "Ancestor 5b0ebb070a5b deleted the unused GPT2LOAD/GPT2TX/WSTORE/MODELPROV host path and suites; retaining this task would resurrect deleted architecture."
---

S6b2 of the frozen intake redesign (rev-4 Correction 3). Two-commit lane: FIRST implement habu-add-wstore-builder-606aaa1c (BUILDER-DISPOSE and BUFFER-DISPOSE, the WSTORE owner exits - read that dot; it is the prerequisite the destruction review named for this leaf's resource-exhaustion cleanup), THEN COMMIT-ALLOCATED ( GPT2TX:prep -- gpt2-model ) in package GPT2TX. Derives the arena-domain table from the prep's plan (PREP-COUNT/PREP-SUM/per-row readers - the prefix sums are pre-cleared by PREPARE, so table population is infallible; arena size comes ONLY from the prep block, never process statics - reviewer finding 3 was measured, a refused second PREPARE overwrites the statics beside a live prep); allocates the arena via MEM typed allocation; copies every span via the census COPY; releases the census; store = allocated(arena, table); mints the model (store, nlayer, cfgkey) through the same gpt2-model type S6b3 defines (coordinate: S6b3 lands first or the type lands in whichever leaf lands first with the other rebasing - the type is ONE definition, one owner, no duplication). FOREIGN-MODEL REJECTION identical to S6b3: cfgkey compare with named refusal BEFORE any resource moves. The only throws after validation are resource exhaustion, and each disposes the prep completely: arena allocation failure -> TABLE-DISPOSE (arena table if built) + prep ABORT semantics, census still released exactly once - prove with forced-allocation-failure legs at EVERY fallible step, counters at baseline after each (the S6b1 FIX-2 discipline; BUFFER-DISPOSE from commit 1 is what makes the buffer path expressible). Copy correctness: after commit, probe at least two weight spans byte-identical to file bytes (one vector, one Conv1D matrix - the reviewer's S6b4 risk note about single-probe blindness applies here first). Mutation kills: drop the cfgkey compare; drop one dispose in one exhaustion path (its leg reds); size the arena from a static (the two-live-preps fixture reds). Acceptance: focused suite + maki/test.f + both diff lints + refine-lint + error-code-lint; weight-store suite green with the new exits. STOP on any checker miss or unplanned interface. Claim: agent=s6b2 workspace=.jj-ws/habu-s6b2-alloc

Redesign 2026-07-26 (mirrors the S6b3 redesign in habu-bind-txn-commit-ccf652d2 - read it): COMMIT-ALLOCATED ( GPT2TX:prep MDLCFG:mcfg -- GPT2TX:commit-result ) reusing the commit-result ENUM, gpt2-model type, and MODEL-DISPOSE that S6b3 defines; sequence strictly behind S6b3 commits (rebase, reuse, never redefine). Exhaustion cleanup now also covers the resident/model path: a failure after HOLD disposes via RESIDENT-DISPOSE.

Redesign 2 (2026-07-26, mirrors ccf652d2): COMMIT-ALLOCATED ( GPT2TX:checked-prep -- gpt2-model ) consuming the same CHECK/check-result/checked-prep surface S6b3 defines; its resource-exhaustion throws remain (arena allocation), each disposing everything including the checked prep - CHECK does not move resources, so refusal-then-cleanup stays fully expressible.

Redesign 3 (2026-07-26, from the S6b3 destruction review forward-risk): the landed checked-prep is MAPPED-ARM-SPECIFIC - CHECK detaches the mapping and releases the census, and COMMIT-ALLOCATED needs the census live to copy spans. S6b2 therefore gets its own gate: CHECK-ALLOC ( GPT2TX:prep MDLCFG:mcfg -- GPT2TX:check-alloc-result ) with payload ENUM check-alloc-result 0 = matched(FIELD c checked-prep-alloc) | refused(FIELD p prep, FIELD code n); checked-prep-alloc is a DEFLINEAR witness = the prep after comparison with the census STILL HELD (no resources move in CHECK-ALLOC - it is compare-and-retype only, total refusal); ABORT-CHECKED-ALLOC for a decliner. COMMIT-ALLOCATED ( checked-prep-alloc -- gpt2-model ) then copies spans from the retained census, releases it exactly once, and its only throws remain resource exhaustion, each disposing everything (arena, table, census, witness) - the forced-failure legs cover every fallible step. The gpt2-model type, MODEL-DISPOSE, and the arm-name refused convention come from the landed S6b3 surface.

Redesign 3 correction (2026-07-26, S6b2 checkpoint): the per-row plan readers do not exist yet - the prep block carries only aggregates. Blocked by the plan-in-prep prerequisite (habu-carry-validated dot, first commit of this lane); after it lands the per-row readers in this contract mean the prep-block readers, never the ROWS static. Option B (public WSTORE row reader) and option C (re-walk in the commit) evaluated and rejected: B breaches a deliberate seal and lacks census ids anyway; C restores the fallible-validation-in-the-terminal-half failure the witness type exists to eliminate.
