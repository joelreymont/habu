---
title: Bind transaction COMMIT-ALLOCATED
status: active
priority: 2
issue-type: task
created-at: "2026-07-26T13:44:33.324532+02:00"
---

S6b2 of the frozen intake redesign (rev-4 Correction 3). Two-commit lane: FIRST implement habu-add-wstore-builder-606aaa1c (BUILDER-DISPOSE and BUFFER-DISPOSE, the WSTORE owner exits - read that dot; it is the prerequisite the destruction review named for this leaf's resource-exhaustion cleanup), THEN COMMIT-ALLOCATED ( GPT2TX:prep -- gpt2-model ) in package GPT2TX. Derives the arena-domain table from the prep's plan (PREP-COUNT/PREP-SUM/per-row readers - the prefix sums are pre-cleared by PREPARE, so table population is infallible; arena size comes ONLY from the prep block, never process statics - reviewer finding 3 was measured, a refused second PREPARE overwrites the statics beside a live prep); allocates the arena via MEM typed allocation; copies every span via the census COPY; releases the census; store = allocated(arena, table); mints the model (store, nlayer, cfgkey) through the same gpt2-model type S6b3 defines (coordinate: S6b3 lands first or the type lands in whichever leaf lands first with the other rebasing - the type is ONE definition, one owner, no duplication). FOREIGN-MODEL REJECTION identical to S6b3: cfgkey compare with named refusal BEFORE any resource moves. The only throws after validation are resource exhaustion, and each disposes the prep completely: arena allocation failure -> TABLE-DISPOSE (arena table if built) + prep ABORT semantics, census still released exactly once - prove with forced-allocation-failure legs at EVERY fallible step, counters at baseline after each (the S6b1 FIX-2 discipline; BUFFER-DISPOSE from commit 1 is what makes the buffer path expressible). Copy correctness: after commit, probe at least two weight spans byte-identical to file bytes (one vector, one Conv1D matrix - the reviewer's S6b4 risk note about single-probe blindness applies here first). Mutation kills: drop the cfgkey compare; drop one dispose in one exhaustion path (its leg reds); size the arena from a static (the two-live-preps fixture reds). Acceptance: focused suite + maki/test.f + both diff lints + refine-lint + error-code-lint; weight-store suite green with the new exits. STOP on any checker miss or unplanned interface. Claim: agent=s6b2 workspace=.jj-ws/habu-s6b2-alloc
