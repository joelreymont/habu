---
title: Make LINT-LEX fail closed
status: active
priority: 2
issue-type: task
created-at: "2026-07-27T17:28:09.294220+02:00"
blocks:
  - habu-fix-signature-comment-2e17e2b7
---

Invariant: LINT-LEX has one scanner, and an ordinary consumer can never use a truncated token table after malformed source. Public contract: add SCAN ( ptr u8 n -- ) as the sole diagnostic mode. SCAN resets all lexer state, scans once, stops at the first defect, returns normally, retains only prefix tokens, and leaves ERROR-KIND@ plus byte, line, and column readable. SOURCE ( ptr u8 n -- ) calls SCAN and throws public LINT-LEX:E-DEFECT at verified-free code -4807 iff ERROR? is true; the diagnostic record remains readable after catch. Clean SOURCE and SCAN yield identical tokens. Extend WORD CONTENT to return exact raw string-body bytes excluding the source delimiter and closing quote; escaped openers honor and validate engine-supported escapes, plain openers do not. Add MALFORMED-ESCAPE and UNTERMINATED-COMMENT diagnostics; make comment, string, row, and end-of-file scans match engine token boundaries and stop safely. Migrate only diagnostic-aware callers to SCAN so their existing named reports remain intact: tools/package-diff-lint-core.f, tools/check-all-errors-core.f, tools/lint/shadow-lint.f, and tools/lint/ptx-emitter-lint.f. All ordinary callers keep SOURCE and become fail-closed by default; the parent dot retains ownership of verify-source registry consumption and its named ordinary-consumer regressions. Scheduler proof: test/gate-runner-entry-test.f scans test/gate-stdlib-inline-lib.f with LINT-LEX and accepts both case-insensitive normal s" and escaped s\" opener tokens whose case-sensitive CONTENT is exactly test/gate-size-attribution-test.f. Each matching path literal sets or replaces one pending candidate; COMMENT tokens are transparent; the next non-comment WORD counts exactly once only when it is case-insensitive exact GSI-FORK-INCLUDE, otherwise it clears the candidate, and any other non-comment token also clears it. Thus path,path,verb counts 1; path,verb,path,verb counts 2; path,verb,verb counts 1. Assert exactly one production row. Hostile cases cover both openers, the three adjacency sequences, line and parenthesis comment fakes, an enclosing string fake, wrong path, wrong verb, comments and newlines between literal and verb, two live rows, malformed escape, unterminated quote, unterminated comment, malformed registry row, and clean end-of-file. Files: tools/lint/source-lex.f, tools/lint/text-foundation-test.f, tools/package-diff-lint-core.f, tools/package-diff-lint-test.f, tools/check-all-errors-core.f, tools/check-all-errors-test.f, tools/lint/shadow-lint.f, tools/lint/shadow-lint-test.f, tools/lint/ptx-emitter-lint.f, tools/lint/ptx-emitter-lint-test.f, test/gate-runner-entry-test.f, tools/typed-local-diff-lint-test.f, and LESSONS.md. Forbidden: a second scanner, raw substring or physical-line matching, a catch that masks E-DEFECT, a compatibility alias, mutable public lexer state, changing or skipping the production scheduler row, or folding parent-owned verify-source and consumer migrations into this leaf. Acceptance: text-foundation proves SCAN diagnostics and SOURCE throws for every defect while preserving the record; package-diff-lint-test proves MALFORMED-ESCAPE and UNTERMINATED-COMMENT throw E-PKGDIFF-LEX through the production diff entry; the other diagnostic callers retain their exact failure class under their focused suites; runner-entry proves all hostile scheduler cases, exactly one production row, and preload plus entry execution once; tail-pure passes; typed-local, package, error-code, host, and dot-dependency gates pass. Claim: unassigned.
