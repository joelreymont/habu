---
title: Give a does> row its value boundaries in the native chain
status: active
priority: 2
issue-type: task
created-at: "2026-09-18T15:34:58.864891+03:00"
---

Problem: a create ... does> definer whose clause yields a multi-cell value is refused at tier 1 and AOT whatever its arguments: '1 set-tier require lib/span.f' dies 'ncomp: cannot compile SPAN-BUFFER:' -8519 E-NELAB-BUNDLE (measured 2026-09-18 on the open-row engine, reduced probe /tmp/hazel-open-row/probes/p-sb.f; pre-existing on the release engine too). Cause by reading (open-row lane): src/compiler/native/compiler.f:257 CHECK-DOES-SPLIT throws E-NELAB-BUNDLE whenever CHECKER-OWNER:DOES-WIDE? is true, and compiler.f:444 hands NELAB:DOES GLUE-NONE GLUE-NONE because the does> row crosses the checker-owner ABI as cell counts only (DOES-IN, DOES-OUT, DOES-WIDE? in src/core/checker-owner-abi.f, a versioned table whose last offset is $228 and whose size comes from CHECKER-FETCH-ABI:BYTES) - there is no DOES-IN-SLOT/DOES-OUT-SLOT, so no glue can be computed; removing the refusal without the glue fails closed at elaborate.f:898 E-NELAB-JOIN. Consequence: every lib/span.f consumer (SPAN-BUFFER:, SPAN-CELLS:) is blocked at tier 1 and in AOT images - Tender's band 2 waits on it. Acceptance: the does> row reaches the native chain with per-cell slot numbers the way a definition's DIN/DOUT rows do (a DOES-IN-SLOT/DOES-OUT-SLOT pair in checker-owner-abi.f, the ABI version bumped, CHECKER-FETCH-ABI:BYTES covering it, the checker filling them from the does> row's expanded cells), compiler.f CHECK-DOES-SPLIT computes the glue through dict.f ROW-GLUE from those slots instead of refusing, NELAB:DOES receives it; '1 set-tier require lib/span.f' loads; lib/span-test.f passes under 1 set-tier; a native-build gate case for lib/span.f in tools/hb-build-test.f (a stripped image requiring lib/span.f exercising SPAN-BUFFER:, MEM:ALLOC-SPAN, SKIP/TAKE/SUB, COPY, $, printing a value, modelled on BUILD-AOT-DIV-REFUSAL); red-first fixtures in test/compiler/native-rename-rows.f (a does> clause yielding a two-cell product compiles and runs at tier 1; a does> clause whose width reads an open argument stays refused by name); docs/forth.md's 'open family instance' bullet loses its does> limitation sentence and states the rule. Files: src/core/checker-owner-abi.f, src/core/checker.f (or where DOES-IN/DOES-OUT are filled), src/compiler/native/compiler.f, src/compiler/native/checker-owner.f, test/compiler/native-rename-rows.f, tools/hb-build-test.f, lib/span-test.f, docs/forth.md. Verify: the probe; span-test at tier 1; hb-build-test; three generations with cmp; test/run.f. Depends: habu-place-an-open-d7bcba49. Ownership: checker-owner ABI / native chain. Claim: agent=hazel workspace=.jj-ws/hazel-does-glue.
