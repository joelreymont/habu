# Compiler identity: what the proofs rest on

Regenerated by `bin/hb --load test/compiler/ir-id-proof.f` and committed,
so a change in what the machine-checked identity proofs assume shows up as
a diff to this file. The gate rebuilds it and compares it byte for byte; a
run that disagrees with this file fails.

Frozen schema digest: e085bfac3fce293bbf7c02708b48ab5134177efd7e9bb7ac2a6acace05a0d013

Statements bound by name and pinned type: 74
Closed under the global context: 65
Resting on an external assumption: 9
Admitted: 0

The whole external assumption set, and nothing else:

- host_atomic_cas : cas_impl
- atomic_cas_linearizable : cas_linear host_atomic_cas

`host_atomic_cas` is the host compare-and-swap the module allocator calls,
left as a parameter rather than modelled. `atomic_cas_linearizable` is its
one law: a call behaves as the atomic transition at its linearization
point. Every `host_*` refinement theorem is proved from that law alone, so
nothing else about the host operation is assumed anywhere. The exact
per-statement expectation lives in `test/compiler/ir-id-axioms.txt`; this
file is the reviewed summary of it.

That file also carries a written copy of what each bound statement says,
and the gate makes Rocq accept every proved statement at exactly that
type, so a theorem cannot be rewritten to say less while keeping its name
and its assumption set.
