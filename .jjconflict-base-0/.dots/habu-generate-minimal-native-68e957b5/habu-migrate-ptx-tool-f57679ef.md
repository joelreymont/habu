---
title: Migrate PTX tool CUDA lifecycles
status: closed
priority: 1
issue-type: task
created-at: "2026-07-19T20:52:53.426911+02:00"
closed-at: "2026-07-21T06:39:53.635318+02:00"
close-reason: "Landed 771d921a: 13 PTX tool lifecycles migrated onto CUDA-SCOPE (18 files +329/-216) - per-run SCOPE frames for the launchers and device tests (fixing five pre-existing buffer leaks the census found: sum/softmax launchers, acc/redadd device tests, gradcheck DX/DY), per-kernel scopes in device-gold, nested per-burst event frames in the bench engine with timing unchanged, and a stated mechanism decision: modules reloaded mid-frame stay explicit mutable slots (the LIFO ledger cannot individually release a mid-frame row), while acquire-once modules are scope-owned. New public PTXBENCH:OWN-* transfer API opens the remaining bench consumers to mechanical migration. Both-direction leak proof committed host-only (cuda-scope-leak-proof-test.f, registered): unfixed shapes log ZERO releases on a mid-pipeline throw, migrated shapes unwind in exact reverse order exactly once. Real-device: rope+rmsnorm ran the migrated pipelines on the GB10 with goldens+gradchecks green before AND after the reboot. Full cold gate exit 0 at the merged tip (perf pass under the Mac's new load-calibrated budget). Follow-ups dotted: remaining bench/sweep consumers, ad-gradcheck-launch cross-tree contract"
---

tools/ptx/bench.f OPEN/LOAD/event creation/device allocation and its consumers, plus raw launchers including cuda-launch.f, sum-launch.f, softmax-launch.f, softmax-gradcheck.f, gradcheck.f, device-gold.f, acc-device-test.f, redadd-device-test.f, ad-gradcheck-launch.f, indexed/scatter gradchecks, benchmark runners, and new autotune-sweep.f, use happy-path cleanup. Exact source census finds acquisition and release in the same files but no catch/unwind boundary. Any RC0 throw during a later allocation, copy, parameter bind, launch, sync, readback, benchmark event, emit, ptxas, clock probe, or report step leaks earlier resources; UNLOAD/CLOSE/FREE stop at the first cleanup failure; many top-level paths also skip PTXTC:CLEAN on failure. In autotune-sweep.f:112-122 three device allocations are sequential, while SW-CANDIDATE at 274-300 and AT-SWEEP at 328-345 release device buffers, module, context, and temp root only on normal fallthrough, so one failed candidate poisons the remaining sweep. Migrate PTXBENCH first to habu-add-exc-safe-74d2f76e, then make each tool own its allocations/modules/events/temp root through that scope instead of open-coded release lists. Keep device skips and rejected candidates allocation-free. Add host-injected failure coverage for every acquisition and sweep stage plus representative real device tests for scalar launch, multi-buffer golden, events, repeated benchmark shapes, and a failed candidate followed by a clean successful one. Acceptance: zero outstanding resources and no temp root after every success/failure, reverse-order exactly-once cleanup, primary/cleanup error preservation, no stale handle reuse, and unchanged device numerical/performance verdicts. Files: tools/ptx/bench.f/tests and listed CUDA tools including autotune-sweep.f. Depends: habu-add-exc-safe-74d2f76e. Ownership: PTX tools/bench lifecycle migration only; no Maki runners, kernel codegen, or CUDA ABI redesign.

Claim: agent=ptxcuda workspace=.jj-ws/fable-ptxcuda machine=spark (owns migrating tools/ptx launchers/bench/gradchecks + lib/ptx device tests CUDA lifecycles onto the landed CUDA-SCOPE f8ec546f)
