---
title: Seal raw-storage nominals on the load path
status: open
priority: 1
issue-type: task
created-at: "2026-07-28T15:47:42.326735+02:00"
---

Claim: agent=seal_raw workspace=.jj-ws/habu-seal-raw-storage-a0e59205 (RELEASED 2026-08-21: workspace gone, no live lane - gc)

Full context: the raw-storage nominal seal does not hold on the bin/hb --load path, so a nominal identity can be forged through checked code. Independently reproduced twice: a file declaring 'DEFTYPE region' plus ': N>ID ( n -- PKG:region ) V ! V @ ;' over a 'variable V' certifies at exit 0 under bin/hb --load - a plain n becomes a sealed nominal with no converter. The same forge through 'here' ('here ! here @') is rejected at exit 70 because 'here' carries a baked primitive effect. Cause: the seal for create/variable/constant/PTR-VARIABLE is registered through the verify-source definer registration (RAW-TRUST-NEXT, src/habu/verify-source.f:750-753), which the --load path does not run; the TVK-RAW machinery that exists precisely to close identity forging through raw storage is inert on --load. Note --load is the PRIMARY path - every tool and gate runs it - so checked guarantees on the main path are weaker than believed for any code reading raw storage into a nominal. Static invariant: reading back a raw storage cell must never manufacture a nominal type; the read effect is the erased scalar unless the store site proved the nominal. Required fix shape, decided at review: (1) registration belongs at the DEFINER, not a scanner - when the engine compiles variable/create/constant/PTR-VARIABLE on any path, that act records the cell as raw in checker state; one authority at the point of definition, no path dependence. (2) Default-closed: a read of storage the checker cannot prove nominal-carrying refuses nominal binding, so a path that forgets to register produces a spurious rejection, never a forged identity. Typed storage (TYPED-VARIABLE etc.) registers properly and keeps working. (3) The fallout is part of the fix: flipping to default-closed may red existing checked code that relies on the leniency - reading a raw cell into a role type IS the forge pattern; each such site gets a converter, not an exception. Run the full checked tree and fix every surfaced site in the same change or dot each with its owner. Miss class: wrong primitive/boundary effect on one load path. Fail-closed proof recorded for both routes: forge-via-variable exit 0 (the miss), forge-via-here exit 70 (baked route closed). Acceptance: the variable-forge reproducer is rejected on bin/hb --load with the wrong-family diagnostic; negative regressions cover variable, create, constant and PTR-VARIABLE forges on --load; the verify-source path stays green; docs/effects.md's raw-storage section (which currently records the measured gap) is updated in the same change to state the seal holds unconditionally.
