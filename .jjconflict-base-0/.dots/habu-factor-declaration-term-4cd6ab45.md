---
title: Factor declaration term parser
status: open
priority: 2
issue-type: task
created-at: "2026-07-31T04:21:39.526679+02:00"
---

Why: STRUCTURE and ENUM duplicate the surviving declaration-term parser, and carrier NEWTYPE would create a third authority. Owner: existing package DECL-HEAD. Result: add public TERM ( ptr u8 n [ -- ptr u8 n ] -- n ), taking the first token plus a caller-owned next-token quotation and returning one schema node. Grammar is exactly n, f, r, a declared binder, a concrete type, ptr TERM, or a closed arity-zero family. The provider is called only after ptr and returns exactly 0 0 at exhaustion; the two caller quotations normalize their existing zero-length spans at this seam without changing SD-NEXT, ED-NEXT, or replay state. TERM borrows token spans and retains none. It returns a schema node, never a root index. Move the parser and its parser-private schema/family boundaries from structure-decl.f and enum-decl.f into generated-declaration.f once; delete both resolver copies. Shared, plain-English TERM rejection text replaces caller-specific copies, and the exact STRUCTURE and ENUM diagnostic expectations move with that product change. Move each surviving parser-private TRUST to the shared source boundary with a source-local rationale, this dot as retirement owner, and a focused production-path test; do not duplicate boundaries. STRUCTURE and ENUM keep SD-NEXT/ED-NEXT, live/replay state, roots, widths, self-recursion checks, registration, and field publication. Files: src/core/generated-declaration.f, src/core/structure-decl.f, src/core/enum-decl.f, test/decl-replay-verify-source.f, test/structure-decl-suite.f, and test/enum-decl-suite.f. Dependencies: none. Forbidden: lookahead, pushback, lexer or replay state in DECL-HEAD, token buffers, application or quotation grammar, SUMTYPE work, NEWTYPE work, carrier constraints, new files, compatibility, caller-specific parser logic, duplicate trust boundaries, or a third parser. Checkpoint before edits: canonical duplicate-resolver census and one representative provider call shape. Pre-M17 proof: exactly one TERM definition, exactly two current provider calls, zero resolver copies in STRUCTURE/ENUM, no retired SUMTYPE scratch, exact six-file write set, exact zero-pointer exhaustion fixtures, and source review. M17 acceptance: scalar consumes no provider token; ptr ptr n consumes exactly two and returns exact nested schema; binder order maps by declaration ordinal; closed family succeeds; unknown family, bare parametric family, missing pointee, and pointer-to-linear reject with existing exact codes and the shared exact messages; forced rejection restores schema/root watermarks; production live/replay and both owning declaration suites pass. No bin/hb load or engine refresh before M17. Claim: agent=term_impl workspace=.jj-ws/habu-factor-declaration-term-4cd6ab45 (RELEASED 2026-08-21: workspace gone, no live lane - gc).
