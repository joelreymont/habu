---
title: "public-signatures publishes the seed's whole registry"
status: open
priority: 2
issue-type: task
created-at: "2026-08-17T04:46:27.202410+02:00"
---

ROOT CAUSE, measured 2026-08-17 (bake-chain-22, lane habu-seeded-words-invisible-c7505a49). tools/public-signatures-core.f PS-EMIT-REGISTRY walks TFAM-N@ over the LIVE engine and emits a ctor/derive row for every public family, stamping each with the SCANNED FILE's path, line 0, column 0. On a seeded engine the chain's families are live at boot, so a scan of a 13-definition file reports 386 definitions. SAME-TREE CONTROL: 'bin/hb --load tools/public-signatures-core.f tools/public-signatures.f -- examples/llm/good.f' = 60,491 bytes / 386 defs, tail rows A64SEL-CMPKIND:GPR..EQ; the identical command on bin/hb-host = 1,982 bytes / 13 defs, exactly the file's own words. LLM.md:41 states the contract the 373 extra rows break: the tool exposes the typed public words OF THAT FILE. TWO BATTERY REDS, ONE CAUSE, both -2201 E-STR-CAPACITY (NOT E-FS-OPEN -2102, which two earlier handoffs recorded): 'native checker diagnostics repair slice' dies entering test/gate-diagnostics-lib.f PUBLIC-SIGNATURES (GT-OUT-CAP 32768 in lib/test/runner.f, overflowed at ~60KB) - the earlier note naming FILE-ORIGIN was wrong, FILE-ORIGIN is in the file-unsafe slice; and 'GROUP: stdlib/tool-doc' = tools/public-signatures-test.f, 24 failures on the product and GREEN on bin/hb-host. NOT pre-existing: the host control is the same tree. RULING NEEDED, two shapes and neither is free. (a) HIGH-WATER BASE: latch TFAM-N when tools/public-signatures-core.f loads and emit only families at or above it - structural, cheap, keeps tools/public-signatures-test.f's own 'ENUM pstcolor' rows (declared after the tool loads), and excludes everything the engine was born with. Wrinkle to measure first: in the diag gate the tool loads early among many gate requires, so families declared by LATER gate files would still be admitted and the golden could move. (b) DECLARED-IN-FILE: emit only families the scanned file or its prescanned closure declares - faithful to the manifest's contract, but PS text-scans no family declaration today (no ENUM/SUMTYPE/STRUCTURE/NEWTYPE token scan exists in the file), so it is new parsing. Growing GT-OUT-CAP is refused: the output would still be wrong. ACCEPTANCE for whichever lands: the product's manifest for examples/llm/good.f equals the host's; public-signatures-test green on both engines; the diag repair slice green; mutation - reinstating the unfiltered walk reds by name on the product.
