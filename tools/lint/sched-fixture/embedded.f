\ embedded.f - reached by one WORD of a longer literal in root.f. Must not
\ report; it is the case a whole-payload compare would miss.
package SCHED-FIXTURE-EMBEDDED
: EMBEDDED ( -- n ) 3 ;
;package
