\ snap-build.f — one-shot checker installer for snapshot construction.
\ Generated snapshot sources define this tail-owned helper before SEAL-FRIEND,
\ then retire it with SNAP-TAIL-MARK before writing the image.

package SNAP-BUILD

public

' LOWER-CERT-HOOK:HOOK constant HOOK-XT

end-package
